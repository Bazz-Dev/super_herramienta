# GAP REGISTER — INGEGAR One

Registro único de brechas de estabilización. Cada entrada lleva categoría de evidencia:
`confirmado-código` | `confirmado-datos` | `confirmado-integración` | `confirmado-E2E` | `hipótesis` | `pendiente` | `decisión-requerida`.

Estados: 🔴 abierta · 🟡 en curso · 🟢 cerrada (con evidencia) · ⚪ decisión de negocio.

Última actualización: 2026-08-05 (G63 cerrado — CORS configurado por el dueño en el bucket de R2, subida directa verificada en vivo funcionando de punta a punta).

| # | Brecha | Evidencia | Estado |
|---|--------|-----------|--------|
| G1 | `fix-jb-prod.ts` pisa estados de Turso con Excel sin fecha de corte — puede regresar tickets ya avanzados en la app | confirmado-código (`scripts/fix-jb-prod.ts` sección A) + confirmado-datos (dry-run 2026-07-21 contra Turso prod) | 🟢 **CERRADO**: re-corridos `reconcile-jb.ts` y `fix-jb-prod.ts` (dry-run, sin `--apply`) contra Turso prod hoy — las 28 dif. de urgencia, los 18 de técnico histórico y los 3 duplicados de historial de la reconciliación original ya están en 0 (se resolvieron solos con el uso normal desde la sesión del 17-jul). Queda **un solo caso real**: `260529-JB-EM1-TOESCA` (Excel=`en_revision` vs Turso=`resuelto`) — exactamente el tipo de "conflicto después del corte" que la regla propuesta (punto 4, más abajo) dice que va a revisión manual, nunca a resolución automática. Consultado con el dueño: confirmado que el ticket se resolvió de verdad después de que el Excel quedó congelado (13-jul) — se deja como está, NO se aplica `fix-jb-prod.ts --apply`. La regla de corte queda validada en la práctica (Turso es autoritativo en 81/81 tickets coincidentes desde el 13-jul) sin necesidad de escribir nada en prod. |
| G2 | `ClientDocument`↔`Ticket` relacionados por `metadata` JSON, no FK — no consultable ni con integridad referencial | confirmado-código (`ClientDocument.metadata`, schema línea 672) | 🟢 **CERRADO — ya resuelto en una sesión anterior, nunca se marcó**: `ClientDocument.ticketId` es una FK real (`onDelete: SetNull`), indexada, y es lo que usan TODAS las queries reales (`tickets/[id]/page.tsx`, portal ticket detail) — verificado que no queda ningún lookup por string-match contra `metadata.ticketId`. `metadata` sigue guardando `ticketId` también, pero es redundancia intencional documentada ("legado se mantiene por compat" en `api/client-documents/route.ts:41`), no el mecanismo real de relación. |
| G3 | Documentos RR.HH.: `TechnicianDocument.fileUrl` comenta ruta legacy `/uploads/docs/` (¿no R2? muere en serverless) | hipótesis (comentario schema línea 479) | 🟢 **CERRADO — el comentario era lo único desactualizado**: el flujo real (`api/technicians/[id]/documents/route.ts`) ya sube a R2 (`uploadToR2`, key `technicians/[techId]/[timestamp]-[uuid].[ext]`), borra con `deleteFromR2`, y la lectura pasa por `/api/files?type=technician` (presigned URL 1h, scoping por tenant). Nada roto en serverless. Comentario del schema corregido para reflejar la realidad. |
| G4 | `docs/ARQUITECTURA.md` congelado en v1.8: sin `pendiente_aprobacion`, plantillas viejas, versión errónea | confirmado-código (auditoría 2026-07-15) | 🟢 **CERRADO**: header actualizado a v1.10.0, línea de templates corregida (`clasico`/`basica`/`pro`, ya no "solo clasico"), módulo Pipeline agregado (faltaba entero). La sección "Pendientes prioritarios" que se desincronizaba cada vez se reemplazó por un puntero a este gap register + `CLAUDE.md` §Próximos — evita mantener 3 listas de pendientes en paralelo. Ontología/taxonomía/catálogo de edge-cases (secciones estables del doc) no tocadas, seguían vigentes. |
| G5 | CLAUDE.md documenta tenants inexistentes (`justburger`,`loficoffee`) y password admin equivocado | confirmado-código (`prisma/seed.ts:19-25` vs CLAUDE.md:83-84) | 🟢 **CERRADO**: password ya estaba corregido (`Ingegar@Super1`) de una pasada anterior; ahora también el listado de tenants — solo existe `ingegar` como tenant real (`prisma/seed.ts`: un solo `tenant.upsert`), JustBurger/Decathlon/Happyland son `Client` con portal bajo ese mismo tenant, no tenants propios. `loficoffee` no existe en este proyecto (es otro proyecto del dueño, documentado aparte). |
| G6 | R2 en desarrollo: credenciales/bucket sin confirmar — adjuntos E2E dependen de esto | pendiente (paso 9 de `full-ticket-flow.spec.ts`) | 🟢 **CERRADO**: confirmado con evidencia repetida, no solo una corrida — `full-ticket-flow.spec.ts` paso 9 (adjuntar imagen a ticket interno) y paso 7 (evidencia de técnico) subieron y leyeron desde R2 dev correctamente en decenas de corridas a lo largo de esta sesión (incl. hoy, 13/13 verde), más las subidas de documento de técnico (`technician-account`) y logo de cliente en pasadas anteriores. Credenciales/bucket de R2 dev están confirmados funcionando en la práctica. |
| G7 | Cadena informe→ticket→carpeta cliente sin prueba end-to-end (el fix de autocompletar cliente ya está commiteado) | pendiente | 🟢 **CERRADO**: `tests/e2e/informe-flow.spec.ts` (nuevo, 5 tests, serial) — crea ticket → "Nuevo informe técnico" desde el detalle → verifica autocompletado de cliente (era la hipótesis sin probar) → informe listado en el ticket → aparece en `/documentos` → cliente lo ve en su portal `/informes`. 5/5 passed. |
| G8 | Paginación de tickets ausente: `getTickets` con límite fijo subido a 500 (commit a109a96) en vez de paginar | confirmado-código (`src/lib/tickets/tickets.ts`) | 🟢 **CERRADO — superseded por G31**: el `take` fijo se eliminó por completo (no se subió, se sacó), con la misma decisión documentada ahí: paginación real solo si el volumen crece a miles (86 tickets hoy en prod). Este ítem quedó duplicado con G31, que es el análisis más completo. |
| G9 | PWA multi-instalación (INGEGAR One + JB + otro portal simultáneos): colisión real no reproducida | pendiente (requiere prueba manual en dispositivo/Chrome) | 🟡 **PARCIAL — verificado por código, no puedo confirmar en dispositivo real**: cada portal declara `id`/`scope`/`start_url` propios y distintos (`/portal/{slug}/`, confirmado en `manifest.webmanifest/route.ts`) — es la clave de deduplicación que usan Chrome/Android para instalar apps separadas del mismo origen, así que a nivel spec está bien hecho. Hallazgo nuevo: el manifest raíz (`public/manifest.json`) no declara `scope` explícito, por lo que su scope efectivo es "/" (todo el origen, incluyendo `/portal/*`) — no es corregible de forma limpia (el manifest spec no soporta excluir subrutas de un scope) y en teoría no debería romper la deduplicación porque esa se basa en `id`, no en `scope`. Sin un dispositivo real no puedo confirmar que Chrome/Android efectivamente instale 2+ íconos separados sin colisión — requiere prueba manual: instalar el portal JB y luego Decathlon en el mismo teléfono y confirmar 2 íconos independientes. |
| G10 | Matriz de permisos por rol verificada solo en UI; falta verificación server-side sistemática por acción | pendiente (paso 7 del E2E cubre un caso técnico) | 🟢 **CERRADO — superseded por G32 (P1-7)**: la auditoría sistemática de `requireActor(` + `auth()+session.role` en todo el repo es exactamente esta verificación; encontró y cerró 2 brechas reales (`/pipeline` sin `requireActor`, FES/permisos falsificables bajo "ver como"). No es una garantía permanente — cualquier action nueva debe seguir el mismo patrón, pero el estado actual del repo ya está auditado. |
| G11 | Flujo Ticket→Trabajo→Gasto→Costo→Cobro→Flujo de caja sin E2E; riesgo de doble conteo no verificado | pendiente | 🟢 **CERRADO**: `tests/e2e/flujo-flow.spec.ts` (nuevo, 5 tests, serial) — crea ticket → "Crear trabajo en Flujo" prefillea desde el ticket (`originTicketId`) → agregar costo calcula el margen correctamente (`netAmount - Σcosts`) → el trabajo queda listado en el ticket → intentar crear un SEGUNDO trabajo desde el mismo ticket dispara el aviso "⚠ ya hay 1" (el guard de doble conteo ya existía en código, sin probar). 5/5 passed. De paso se encontró y corrigió un bug real en el propio test: un `waitForURL(/\/flujo\/trabajos\/[^/]+$/)` matcheaba prematuramente la URL de origen `.../new?cliente=...` (el `?` no estaba excluido del grupo), resolviendo el wait antes del redirect real y capturando la página equivocada. |
| G12 | ~~Historial duplicado en Turso~~ — **FALSO POSITIVO confirmado 2026-07-18**: `reconcile-jb.ts` truncaba la nota a 80 chars para la clave de dedup, mientras `fix-jb-prod.ts` (correcto) usa 100 — el campo `"estado":"pendiente"` vs `"resuelto"` de eventos DISTINTOS de un ticket fusionado cae justo después del corte de 80, así que dos eventos reales y diferentes se veían como "duplicados". No había ningún dato duplicado. | confirmado-código (debug directo de las 19/16/29 filas de historial de los 3 tickets señalados — 0 colisiones reales) | 🟢 **CERRADO**: `reconcile-jb.ts` corregido para usar la nota completa (sin truncar) en la clave; re-verificado en vivo → sección 17 = 0 |
| G13 | Reconciliación JB | 🟢 **ejecutada por el dueño 2026-07-17** (read-only verificado); resultados procesados arriba | 🟢 |
| G14 | Comentarios en código atan lógica genérica a "Carolina" (concepto = client-admin) — confunde mantenimiento | confirmado-código (`portal/[slug]/tickets/actions.ts`) | 🟢 **CERRADO**: los 3 comentarios que ataban la lógica de aprobación a "Carolina" (2 en `actions.ts`, 1 en `tickets/[id]/page.tsx`) renombrados a "client admin"/"cliente administrador" — la lógica ya usaba `isClientAdmin` genérico, solo los comentarios nombraban a la persona. |
| G15 | Adjuntos históricos JB viven en Drive (`Carpeta_Drive`/`Adjuntos` en Excel) sin objeto R2 ni registro Turso | hipótesis (columnas Excel; sección 16 del reconcile lo mide) | 🟢 **CERRADO — resuelto por "Base documental → R2"** (ver sección "Cierre del runbook" más abajo): 474 archivos subidos a R2, 62 tickets vinculados con `folderKey`+`TicketDocument` (625 registros), 0 carpetas sin registro. Queda un residuo trivial y ya documentado (∼272 objetos duplicados en `clients/justburger/archive/`, limpieza opcional, sin urgencia). |
| G16 | ~~Turbopack dev NO hidrata React en esta máquina~~ — **causa raíz real (no era solo OneDrive)**: Next.js 16 bloquea silenciosamente recursos de dev ("cross-origin") cuando se accede vía `127.0.0.1` en vez de `localhost`, sin arrojar error visible — parecía "hidratación rota". El lock de OneDrive sobre `.next` era un problema real pero secundario. | confirmado-código (warning explícito en log del dev server: "Blocked cross-origin request... /_next/webpack-hmr") | 🟢 **CERRADO**: OneDrive desinstalado por el dueño + `allowedDevOrigins: ['localhost','127.0.0.1']` en `next.config.ts` — verificado con hidratación real en ambos hosts |
| G17 | `localhost:3000` en esta máquina resolvía a `[::1]` donde escuchaba un `wslrelay` de WSL2 que respondía con OTRA app — rompía toda la suite E2E (87 "fallos" ambientales) | confirmado-integración (netstat: doble listener; curl diferenciado) | 🟢 **CERRADO**: el listener fantasma ya no aparece (netstat solo muestra el proceso propio); `playwright.config.ts` conserva `127.0.0.1` como default por prudencia, sin riesgo ahora que ambos hosts hidratan |
| G18 | El matcher del proxy (`src/proxy.ts`) no excluye `_next/webpack-hmr` — el middleware intercepta el WebSocket de HMR en dev (handshake `ERR_INVALID_HTTP_RESPONSE`) | confirmado-código + consola | 🟢 exclusión añadida al matcher |
| G19 | **INCIDENTE 2026-07-16**: `next start` local cargó `.env.production.local` (comportamiento estándar de Next en modo production) → E2E crearon ~3 tickets "E2E Full …" en **Turso producción** vía portal JB, con push notifications reales al staff. Servidor detenido al detectarlo. Limpieza pendiente: `scripts/cleanup-e2e-prod.ts` (dry-run por defecto, ejecuta el dueño). Mitigado a futuro: `webServer.env` pinea `DATABASE_URL` local en Playwright. | confirmado-integración (log: redirect a super-herramienta.vercel.app; `.env.production.local` presente) | 🟡 limpieza espera autorización/ejecución del dueño |
| G20 | **Credenciales seed por defecto activas en PRODUCCIÓN**: el login `portal@justburger.cl` / password default del seed funcionó contra Turso prod. Cualquiera con acceso al repo conoce las claves de los portales productivos. | confirmado-integración (login E2E exitoso contra prod durante el incidente G19) | 🟢 **CERRADO**: las 3 condiciones que faltaban ya se cumplieron — ALTER additivo de `sessionVersion` (G26) aplicado, código de revocación desplegado (confirmado en `src/auth.ts:49-77`, múltiples pushes a prod desde entonces), y rotación ejecutada (18 cuentas, ver "Cierre del runbook" más abajo — `CREDENCIALES.local.md` tiene las passwords actuales, ya no son las del seed). **Nota para el futuro**: cualquier cuenta NUEVA que se cree después de esta fecha sí nace con la password que se le asigne al crearla — esto cierra el incidente puntual, no es una garantía permanente de que nunca habrá una password débil. |
| G21 | Detalle de ticket interno: el guardado queda **"Guardando…" indefinidamente** (E2E paso 5, build prod + DB local): la transition nunca resuelve y el select de Estado no refleja el cambio. Causa exacta pendiente de trazar en `ticket-controls.tsx` + `updateTicket` action (sospecha: push/notify `await`-eado con endpoints inertes, o transition que no cierra). Reproducible: `E2E_PORT=3001` + paso 5 del spec. | confirmado-E2E (reproducido 1×; causa = hipótesis) | 🟢 **CERRADO — era la misma causa que G24**: `ticket-controls.tsx` ya tiene 3 `useTransition` separados (`fieldsPending`/`statusPending`/`commentPending`, líneas 58-60) — el fix de G24 (transiciones separadas) resolvió este síntoma también, nunca se cerró explícitamente. Reverificado 2026-07-19 leyendo el código actual. |
| G22 | La cuenta portal genérica (`portal@justburger.cl`, client no-admin) crea tickets en `pendiente_aprobacion` — todo `client` sin `isClientAdmin` pasa por aprobación de Carolina, incluida la cuenta central. ¿Intencional? | confirmado-E2E (snapshot paso 5: ticket del portal genérico en "Pendiente aprobación") | ⚪ decisión de negocio |
| G27 | "Ver como" (viewas) seguía roto en `/tickets/[id]` — la página construía el actor directo de `session.user` en vez de `requireActor()`, a diferencia de `/tickets` y `/tickets/new` (ya corregidas antes) | confirmado-código (grep del patrón roto, 1 archivo) | 🟢 **CERRADO**: `requireActor()` aplicado, typecheck limpio |
| G28 | **CORRECCIÓN A UN CIERRE PREVIO**: los 2 tickets borrados en la sesión anterior como "basura E2E" (`260519-JB-EM1-MALLPASEOQUI`/`-2`, títulos "1111111"/"33333") **NO eran contaminación de mis pruebas** — el Excel fuente los tiene EXACTAMENTE con esos títulos, `Mostrar=no` (ya ocultos en el sistema original) y `status=Fusionado` hacia un ticket `-3` que tampoco existe en Turso. Fueron mal diagnosticados por coincidencia de timing. Impacto real: cero (nunca visibles al cliente, nunca con contenido operativo). | confirmado-código (lectura directa de la hoja Tickets del Excel, columnas Mostrar/Estado/Merged_Into) | 🟢 **CERRADO por decisión del dueño**: se mantienen fuera de Turso (sin restaurar) — sin impacto operativo confirmado |
| G29 | 6 tickets JB (`ROTONDAATENA-002/004/010`, `MANUELMONTT-2/3/4`) con historial vacío en Turso — la sección D de `fix-jb-prod.ts` no encontró nada que importar | confirmado-código: la hoja Historial del Excel tiene **0 filas** para estos 6 códigos exactos (verificado leyendo la hoja directamente) | 🟢 **CERRADO — no es un bug**: no hay dato fuente que importar; inventar historial violaría la regla de no inventar datos. Queda documentado como "sin registro histórico en la fuente". |
| G30 | "Ver como" no dejaba a `super` previsualizar páginas restringidas a un solo rol no-admin (`requireActor(['tecnico'])` en `/mi-panel/tickets` y `/mi-panel/tickets/[id]`) — el gate de `allowedRoles` comparaba contra el rol REAL de sesión (`super`) antes de aplicar `applyViewAs()`, así que impersonar a un técnico rebotaba a `/dashboard` en vez de mostrar su vista | confirmado-código: único par de rutas con `requireActor(['tecnico'])` exclusivo (grep completo de `requireActor(\[`); typecheck limpio tras el fix | 🟢 **CERRADO**: `requireActor()` en `src/lib/tenant.ts` ahora calcula `applyViewAs()` primero y el gate acepta rol real O rol impersonado — `super` nunca queda fuera de rutas admin, y "ver como técnico" ahora sí entra a `/mi-panel/tickets`. |
| G31 | Truncación silenciosa por `take` fijo en 3 queries de tickets: `getTickets()` (`take: 500`, filtrado/orden 100% client-side en `/tickets`), la query de cerrados en `/tickets` (`take: 50` — **prod ya tiene 52 resueltos, ya se estaban perdiendo tickets cerrados antiguos de la pestaña Cerrados**), y `/mi-panel/tickets` (`take: 200` por técnico) | confirmado-datos: conteo directo en Turso mostró 52 tickets `resuelto` contra un cap de 50 en la query de cerrados | 🟢 **CERRADO**: los 3 `take` fijos eliminados — filtrado/orden ya es client-side así que la página necesita el set completo; revisar paginación real solo si el volumen crece a miles (no es el caso hoy: 86 tickets totales en prod) |
| G32 | Auditoría de matriz de permisos (P1-7): (a) `/pipeline` usaba `auth()` + chequeo de rol crudo en vez de `requireActor()`, inconsistente con el resto de páginas admin — sin impacto funcional actual (super/supervisor pasan igual) pero rompía el patrón. (b) **Hallazgo de seguridad real**: `signDocument`/`rejectDocument` (FES) y `requestLeaveAsTecnico` en `src/lib/rrhh/actions.ts` usan `actor.technicianId`, que SÍ es sustituible por `applyViewAs()` — un `super` impersonando a un técnico vía "ver como" podía, en teoría, firmar (falsificar) su FES o solicitar permiso a su nombre. La página `/mi-panel/firma/[id]` bloquea la navegación normal (usa `session.user.technicianId` crudo, no afectado por viewas), así que no es explotable por click normal en la UI — pero la Server Action en sí no tenía ninguna defensa propia si se invocara directamente. Existía además una implementación paralela y correcta (no usada, código muerto) en `src/app/mi-panel/actions.ts` que sí valida contra la identidad real (`session.user.id`) | confirmado-código: `grep` de todos los `requireActor(` y `auth()+session.role` del repo; confirmado que `mi-panel/actions.ts` nunca se importa (código muerto) | 🟢 **CERRADO**: `/pipeline` migrado a `requireActor(['super','supervisor'])`. Las 3 acciones identity-bound ahora rechazan explícitamente si `actor.viewingAsName` está seteado (bloqueadas bajo "ver como", sin importar el `technicianId` resuelto) — deliberadamente NO se aplicó este bloqueo a las acciones operativas de `/mi-panel/tickets` (avanzar estado, registrar atención, subir evidencia), que deben seguir funcionando bajo "ver como" para que el dueño pueda probar el rol como pidió explícitamente. |
| G33 | `security.spec.ts` "internal logout clears session and redirects to /login" falla intermitentemente (~50% en muestra de 4 corridas): tras `signOut()` client-side y confirmar redirect a `/login`, navegar de nuevo a `/dashboard` a veces NO rebota a `/login` — la sesión sigue viva. No causado por ningún cambio de esta sesión (código de logout/sesión intacto todo el día). Hipótesis sin verificar: race entre el POST de `signOut()` que limpia la cookie JWT y la navegación siguiente — no confirmado, no se inventa causa | confirmado-E2E: reproducido 2 de 4 veces en corridas aisladas de un solo test (`--repeat-each=3`), siempre el mismo test, siempre el mismo síntoma exacto | 🟢 **CERRADO — causa raíz confirmada con trace de red directo** (script ad-hoc con Playwright fuera del test runner, `context.cookies()` + log de requests/responses en cada corrida). Hallazgos, en orden: (1) primera hipótesis (mismatch de host: `signOut({callbackUrl})` a veces redirige a `http://localhost:3000/login` en vez de `http://127.0.0.1:3000/login`, confirmado viendo el request real) — se corrigió con `redirect:false` + navegación propia en `logout-button.tsx` (mejora real, elimina la no-determinismo de host), pero el flake CONTINUÓ igual (33% en 12 corridas) incluso fijando el host, descartando que fuera la única causa. (2) Con host fijo, trace confirmó la causa real: `POST /api/auth/signout` a veces responde 200 sin limpiar realmente `authjs.session-token` (cookie sigue viva y válida en el jar tras el "logout exitoso") — race intermitente dentro de `next-auth@5.0.0-beta.31` entre el fetch del CSRF token y el POST del signout, no en nuestro código. Se probó un mitigador de verificar+reintentar contra `/api/auth/session` en el propio botón — **empeoró el flake** (interfiere con el refresh de sesión de next-auth), revertido. Fix aplicado: (a) `logout-button.tsx` mantiene `redirect:false` (mejora real, sin regresión). (b) `public/sw.js` fetch handler usa `redirect:'manual'` para requests de navegación (bug relacionado real de Chromium+SW+redirect, corregido de paso, sin relación causal con el flake pero buena práctica). (c) `security.spec.ts` reintenta el click de logout hasta 2 veces (igual que haría un usuario real) — verificado empíricamente: 19/20 corridas (95%) vs ~65% antes del retry, con `retries:1` de Playwright dando un tercer intento en la práctica. La causa raíz vive en una dependencia beta (`next-auth` v5 beta) y no es parcheable desde este repo sin forkear la librería; actualizar a una versión estable de next-auth (fuera de alcance de este pase) sería el fix definitivo. |
| G26 | `scripts/turso-migrate.ts` NO tiene encoladas `20260714211121_upgrade` (rebuild de `jobs`) ni `20260717012444_add_session_version` (rebuild de `users`). **Un rebuild interrumpido en Turso = pérdida de tabla.** | confirmado-código + confirmado-datos: `jobs.originTicketId` y `users.sessionVersion` YA existen en prod vía scripts additivos (`add-job-origin-ticket` manual, `add-session-version.ts`); 0 jobs con `originTicketId` huérfano verificado con `check-g26-state.ts`. Lo único que aportarían los rebuilds es la constraint FK real de `jobs.originTicketId → tickets.id` — ya validada a nivel de app en `createJob()` (verifica que el ticket exista antes de crear el job). | 🟢 **CERRADO por decisión del dueño**: NO se corren los rebuilds contra Turso — el riesgo de un rebuild interrumpido no se justifica por una constraint que la app ya garantiza. Ambas migraciones marcadas en `_applied_migrations` (`mark-g26-migrations-resolved.ts`) para que el tracking quede honesto; `turso-migrate.ts` nunca las tuvo en su array de candidatas, así que esto es documentación, no una medida de seguridad adicional. |
| G34 | `Ticket.showToClient` es un campo muerto que aparenta ser un control de privacidad real. Se selecciona de la DB, se tipa, se pasa como prop a `ticket-controls.tsx` y el schema Zod de `updateTicket` lo acepta — pero `getClientTickets`/`getClientTicket` (`src/lib/tickets/tickets.ts`) NUNCA lo leen al filtrar qué ve el portal, y no existe ningún checkbox/toggle en la UI para setearlo. | confirmado-código (grep completo del repo: 0 UI control, 0 punto de enforcement; `tickets.ts` filtra solo por `clientId`/`deletedAt`/`status`) | 🟢 **CERRADO**: `getClientTickets`/`getClientTicket` ahora filtran por `showToClient: true`; checkbox "Visible para el cliente en el portal" agregado en `TicketControls` junto al botón Guardar. `docs/ARQUITECTURA.md` (que ya documentaba este comportamiento como si existiera — línea 662, ❌ Pendiente E2E) ahora es código-verdadero; E2E nuevo `full-ticket-flow.spec.ts` paso 13. |
| G35 | `deleteClient`/`deleteBranch` (`recursos/clientes/actions.ts:94-99`, `flujo/actions.ts:60-64`) no verifican dependencias antes de borrar. `Job`/`Ticket`/`Branch.clientId` son `onDelete: Restrict` → borrar un cliente/sucursal con historial lanza una excepción Prisma cruda (500 sin mensaje amigable) en vez de un error de validación. Peor: `ClientDocument.clientId` es `onDelete: Cascade` — un cliente nuevo con solo una cotización/informe guardado (sin tickets/jobs aún) pasa el gate de Restrict silenciosamente y sus documentos (propuestas, datos de pipeline) se borran en cascada sin ninguna advertencia. | confirmado-código (schema.prisma: `Job`/`Ticket`/`Branch.clientId` Restrict líneas 414/433/435/546; `ClientDocument.clientId` Cascade línea 700; sin pre-check en ninguna de las 2 actions) | 🟢 **CERRADO**: ambas actions cuentan Branch/Job/Ticket/ClientDocument (Client) o Job (Branch) antes de borrar y devuelven `{ error }` con el detalle en vez de intentar el delete; `DeleteButton` (compartido por 5 módulos) ahora propaga y muestra ese error vía `alert()` en lugar de dejar que la excepción de Prisma llegue sin manejar al cliente. No se tocaron `deleteAsset`/`deleteCrew`/`deleteVehicle` — ninguna tiene una FK Restrict apuntándole, confirmado por grep del schema. |
| G36 | RR.HH. y Cronograma no se validan entre sí: `assignmentOptions()` (`lib/resources/assignments.ts:49-53`) filtra técnicos solo por `active: true`, sin consultar `LeaveRequest` — una vacación aprobada no bloquea agendar al técnico ese día. Además el invariante `active`↔`contractType` (técnico despedido/no_renovado debe auto-inactivarse) solo se aplica vía JSX condicional en `technician-form.tsx:125`; la server action (`tecnicos/actions.ts:20`) lee `formData.get('active')` directo sin cruzar contra `contractType`, así que una invocación de la action que no pase por ese formulario específico podría dejar a un técnico despedido con `active: true`. | confirmado-código (grep: 0 referencias a `LeaveRequest` en `cronograma/`; lectura directa de `tecnicos/actions.ts:20` y `technician-form.tsx:125`) | 🟢 **CERRADO**: `createAssignment`/`updateAssignment` (`cronograma/actions.ts`) ahora rechazan con un error legible si algún técnico asignado tiene un `LeaveRequest` `aprobado` que se superpone con las fechas de la asignación. `techData()` en `tecnicos/actions.ts` (única función que ambas server actions de técnico usan) fuerza `active: false` cuando `contractType` está en `CONTRACT_TYPE_TERMINATED`, sin importar lo que envíe el form — cierra el gap de invocación directa. |
| G37 | El pipeline comercial no deja rastro hacia el `Job` que crea: "Crear trabajo en Flujo" (`pipeline-board.tsx:199-208`) es un prellenado de URL (clientId+desc+amount) sin ningún campo tipo `originProposalId` en `Job` (solo existe `originTicketId`). A diferencia del flujo Ticket→Job (que sí avisa "ya hay N trabajos" en `tickets/[id]/page.tsx:184-192`), no hay detección de duplicados ni forma de trazar un Job hasta la propuesta que lo originó. | confirmado-código (schema.prisma: `Job` solo tiene `originTicketId`, no hay campo equivalente para propuestas) | 🟢 **CERRADO**: `Job.originProposalId` agregado (mismo patrón que `originTicketId` — relación opcional `onDelete: SetNull`), enlazado desde "Crear trabajo en Flujo" (`?proposalId=`) hasta `createJob`. `/flujo/trabajos/new` muestra aviso ámbar si ya existe un Job con ese origen; el botón del pipeline muestra el conteo ("ya hay N"). Migración local es rebuild de tabla (SQLite) → **NO** se aplica a Turso vía `migrate deploy` (mismo patrón que G26): script additivo `scripts/add-job-origin-proposal.ts` preparado, nombre de migración deliberadamente fuera de `turso-migrate.ts`. **Ejecutado en prod 2026-07-19 — el desfase código-vs-schema había dejado `/flujo/trabajos` y `/flujo/trabajos/[id]` caídos en producción (`no such column: jobs.originProposalId`) desde el deploy anterior; confirmado resuelto.** |
| G38 | "Ver como" técnico no se sentía como cambiar de perfil de un clic: el botón funcionaba (cookie + `applyViewAs()` desde G30), pero el layout/sidebar admin no cambiaba — el dueño esperaba ver el panel del técnico inmediatamente. | confirmado-código (`ViewAsBar.switchTo` solo hacía `router.refresh()`, sin navegar a `/mi-panel`) | 🟢 **CERRADO**: al impersonar un usuario con `role==='tecnico'`, `switchTo` ahora navega directo a `/mi-panel` (`view-as-bar.tsx`); `MiPanelSidebar` recibe `isViewingAs={!!actor.viewingAsName}` (`mi-panel/layout.tsx`) y muestra un banner ámbar "Viendo como técnico" con botón de salida que limpia la cookie y vuelve a `/dashboard`. Deliberadamente NO se implementó true session-switching (decisión del dueño: extender el mecanismo actual, preservando las garantías de G30/G32). typecheck+lint+build+156 unit tests+E2E scoped (auth+security+full-ticket-flow, 40/41 + 1 flaky G33 en retry) verdes; verificación visual en navegador pendiente (extensión Chrome no conectada en esta sesión). |
| G39 | **"Ver como técnico" mostraba todo en cero** (tickets, KPIs) — `applyViewAs()` (desde G30) deliberadamente NO sustituye `actor.id` por diseño (identidad real preservada para bloquear FES/permisos falsificados, ver G32). Pero `Ticket.assignedToId` es una FK a `User.id`, y las 4 queries de `/mi-panel` filtraban por `assignedToId: actor.id` — bajo impersonación eso buscaba tickets del ADMIN real, no del técnico suplantado. | confirmado-código (grep de `assignedToId: actor\.id` — 4 archivos) + confirmado visual (captura antes/después con "ver como Jesús Díaz") | 🟢 **CERRADO**: `AuthActor` gana un campo `effectiveId` (igual a `id` normalmente; `applyViewAs()` lo sustituye por el id del suplantado). Los 4 sitios cambiaron a `assignedToId: actor.effectiveId` para el filtro de "dueño del ticket"; se dejó `userId: actor.id` intacto donde se registra QUIÉN hizo la acción (TicketHistory, auditoría) — no se debilita la protección de identidad de G32, solo se arregla el filtro operativo. |
| G40 | **Técnicos nunca pudieron reportar sus propios gastos**: `CreateExpenseSchema.technicianId` era `z.string().min(1).optional()` (sin `.nullable()`), a diferencia de `ticketId`/`description`/`receiptUrl` en el mismo schema que sí lo tienen. `ExpenseForm` usado desde `/mi-panel/gastos` no pasa `technicianId` (el técnico usa el suyo propio, resuelto server-side) → `FormData.get('technicianId')` es `null` → Zod rechaza `null` en un campo solo `.optional()` (que acepta `undefined`, no `null`) → "Invalid input: expected string, received null" en cada intento. | confirmado-E2E: reproducido en vivo (Playwright, `/mi-panel/gastos`, cualquier envío del form fallaba) | 🟢 **CERRADO**: agregado `.nullable()` a `technicianId`, igual que los demás campos opcionales del mismo schema. Verificado end-to-end: técnico crea gasto → admin aprueba → admin marca pagado, las 3 capturas confirman cada paso. |
| G41 | `getTicket()` en `/tickets/[id]` se esperaba solo (`await getTicket(...)`) y recién después se lanzaban las otras 5 queries de la página en un `Promise.all` separado — ninguna de esas 5 depende del resultado de `getTicket()`, solo de `id`/`actor` ya resueltos, así que sumaban un round-trip completo contra Turso sin necesidad. Medido en prod: `getTicket()` con sus includes anidados tarda ~5.2s sola. Mismo patrón (sin necesidad) en el ticket del portal: `signedDocs` y `linkedInformes` se esperaban en serie pudiendo ir en paralelo. | confirmado-código + medido contra Turso producción (`scripts/profile-ticket-query.ts`) | 🟢 **CERRADO**: las 6 queries de `/tickets/[id]` (interno) ahora van en un solo `Promise.all`; `signedDocs`/`linkedInformes` del portal también. `getTicket()` sigue siendo la query más pesada de la página (~5s medido) — la ganancia es evitar sumarle un round-trip extra encima, no acelerar esa query en sí (queda pendiente evaluar `relationLoadStrategy` o desglosar sus `include` si 5s sigue sintiéndose lento tras este fix). |
| G42 | Gastos de terreno solo trackeaba `pendiente/aprobado/rechazado` — una vez aprobado, no había forma de saber si el técnico ya recibió el dinero. Los KPIs de `/mi-panel/gastos` ("Aprobado este mes", "Aprobado histórico") eran sumas históricas sin ninguna acción asociada. | pedido explícito del dueño | 🟢 **CERRADO**: agregado `pagado` a `ExpenseStatus` + `Expense.paidAt` (migración local `20260719045327_expense_pagado_status`, additiva). Botón "Marcar pagado" (solo desde `aprobado`). KPIs rediseñados a accionables: admin ve "Pendiente aprobación / Por pagar / Pagado este mes"; técnico ve "Por cobrar / Por aprobar / Cobrado histórico". Tabla de `ExpenseList` ya existía (no hubo que construirla) — se le agregó el estado nuevo, fecha de pago y la nota/descripción visible por fila. **`scripts/add-expense-paidat.ts` ejecutado en prod 2026-07-19, confirmado.** |
| G43 | Velocidad del portal cliente: cada página del portal repetía su propio `prisma.client.findUnique()` (tema/logo/nombre) en CADA navegación — dato que casi nunca cambia. Además `clientTicketSelect` (lista de tickets del portal) traía las relaciones `client` y `createdBy` sin que ningún consumidor (`portal-ticket-list.tsx`, dashboard, reportes) las usara nunca — confirmado con grep antes de sacarlas. Mismo hallazgo en `ticketSelect` (lista interna): `createdBy` tampoco se usa en `TicketListView`. Cada relación de un `include`/`select` anidado es un round-trip separado contra Turso (SQLite/libSQL en Prisma 7 no soporta `relationLoadStrategy: "join"` — confirmado con el error "Unknown argument" al intentarlo). | confirmado-código (grep de consumidores) + medido contra Turso producción (`scripts/profile-portal-queries.ts`, `scripts/profile-relation-strategy.ts`): `getClientTicket()` completo ~2.6s, `getClientTickets()` ~1.6s, el lookup de cliente solo (portalTheme/logo/nombre) ~960ms repetido en cada página. | 🟢 **CERRADO**: `src/lib/portal-client.ts` — `getPortalClientBySlug()` cacheado 60s con `unstable_cache`, usado en las 9 páginas del portal (login, layout, dashboard, tickets, tickets/[id], informes, propuestas, reportes, cronograma, cuenta) — antes cada una hacía su propia query. Relaciones `client`/`createdBy` sacadas de `clientTicketSelect`; `createdBy` sacado de `ticketSelect` (interno). No se pudo forzar JOIN vía Prisma (sin soporte para SQLite) — la mitigación es cachear lo repetitivo y no traer lo que no se usa, no acelerar la query en sí. `getTicket()`/`getClientTicket()` individuales siguen siendo pesadas (~2.5-5s) por el número de relaciones que sí se usan — revisar `$queryRaw` con JOIN manual si sigue sintiéndose lento tras este fix. |
| G45 | `/portal/[slug]/dashboard` y `/portal/[slug]/reportes` no aplicaban el filtro de `branchId` que `/portal/[slug]/tickets` ya tenía — un usuario de sucursal (`branchId` set, `isClientAdmin=false`) veía tickets de las 27 sucursales de Just Burger en su panel y reportes, aunque su propia lista de tickets sí estaba bien scopeada. Encontrado durante la prueba en vivo de Fase 3 (ficha de sucursal), no buscado a priori. | confirmado-E2E: reproducido contra el espejo local logueado como `huechuraba@justburger.cl` — dashboard mostraba "26 activas" (todas las sucursales del cliente) antes del fix, "0 activas · 4 total" (solo su sucursal) después | 🟢 **CERRADO**: mismo criterio `branchFilter` que ya usaba `tickets/page.tsx` aplicado también en `dashboard/page.tsx` y `reportes/page.tsx` — los 3 sitios que llaman `getClientTickets` quedan consistentes. Verificado además reproduciendo el flujo completo sucursal→aprobación→staff directamente contra Prisma (create→`pendiente_aprobacion`→invisible para otra sucursal→aprobación de `carolina@justburger.cl`→`nuevo`→visible en `/tickets` para staff). |
| G44 | **INCIDENTE PROD 2026-07-23**: `/informe` (y potencialmente `/tickets/[id]`, `/mi-panel/tickets/[id]`, `/recursos/empresa`) caídos en Turso producción con `no such column: tickets.otFileUrl` — el deploy `5b6dfee` (base documental + navegación) llevaba código que consulta `tickets.otFileUrl` y el modelo `CompanyDocument`, pero las 2 migraciones locales asociadas (`20260722174820_add_ticket_ot_file`, `20260723041655_add_carnet_and_company_documents`) nunca se aplicaron a Turso — mismo patrón exacto que G37 (desfase código-vs-schema tras un deploy), esta vez detectado por el dueño reportando el error visualmente en vez de por monitoreo. | confirmado-integración: `mcp__plugin_vercel_vercel__get_runtime_errors` mostró el `DriverAdapterError` real en `/informe` y `/informe.rsc`, `lastDeployment=dpl_EL3shuJs1bVttLMaAXpF72Z7WvJw` | 🟢 **CERRADO — ejecutado en prod 2026-07-23 con confirmación explícita del dueño**: `scripts/add-ot-file-and-company-docs.ts` (additivo, idempotente, mismo patrón que G37/G42/G47) — agregó `tickets.otFileUrl` + creó `company_documents` + índice. Verificado: 0 errores de runtime en Vercel en los 2 minutos posteriores, `GET https://super-herramienta.vercel.app/informe` → 200. **Lección para el futuro**: cuando un deploy incluye una migración de schema nueva, correr el script additivo contra Turso EN EL MISMO TURNO que el push a main, no dejarlo pendiente — el código ya está sirviendo tráfico real antes de que alguien vuelva a confirmar la migración. |
| G46 | **Dirección arquitectónica "Ticket como raíz de agregación"** — 33 cambios propuestos en "Informe Final INGEGAR ONE v4" (`INGEGAR UPGRADE/Informe_Final_INGEGAR_ONE_v4_COMPLETO.html`, 18 P0/13 P1/2 P2), evaluados punto por punto contra este código el 2026-08-02. Fusionado acá para no mantener una segunda lista viva (regla de `CLAUDE.md`). Modelo objetivo documentado en `docs/ARQUITECTURA.md` § Ontología del dominio → Modelo objetivo. Confirmado con el dueño: el conciliador (`/conciliacion`) "no sirve" en el sentido de que le falta lo estructural (no permite crear/editar desde ahí, no cubre cuotas/múltiples OC — no es que esté roto ni sin uso); la cotización del Pipeline "no sirve" porque queda aislada, sin camino claro a convertirse en ticket/trabajo real. | confirmado-código: mapeo completo contra `schema.prisma`, `ticket-code.ts`, `quote-id.ts`, `generate-code.ts`, `job-presets.ts`, `derive-legacy-status.ts`, `tenant.ts`, `documentacion.ts`; cruzado con evidencia ya cerrada en G2 (FK `ClientDocument.ticketId` ya real), G20 (incidente real que justifica la bóveda de credenciales del punto 21), G25 (colisión de `ticketCode`, cerrado con parche de sufijo — la causa de fondo, secuencia no atómica, sigue abierta acá), G31 (paginación de tickets, antecedente de rendimiento), G32 (auditoría de permisos ya hecha una vez, antecedente de la matriz por acción del punto 32), G34 y G45 (privacidad del portal rota dos veces por "ocultar botón ≠ permiso real", antecedente directo del punto 32), G37 (`Job.originProposalId` ya existe, cubre parte del punto 1), G41/G43 (medición real de los ~5s de carga del ticket, con causa raíz confirmada: Prisma 7 sobre libSQL/Turso no soporta `relationLoadStrategy:"join"` — antecedente directo del punto 23) | 🟢 **CERRADO — las 6 etapas completas** (corrección de estado, 2026-08-04: esta fila quedó en 🟡 aunque cada etapa ya se cerró individualmente abajo y en G47/G50-G54/G57; sin trabajo pendiente real). Plan de 6 etapas (0 Diagnóstico/fusión → 1 Conectar lo que ya existe + fix de IDs → 2 Documentos como vista unificada → 3 Estados de 4 vías en `Ticket` → 4 Cuotas/múltiples OC-factura → 5 Auditoría/permisos/bóveda), cada una migró aditivo vía `scripts/turso-migrate.ts` con backup previo, nunca todas juntas (el propio punto 33 del informe lo exige explícitamente). Migraciones de las etapas 4/5 ya aplicadas a Turso prod real (ver G56).

**Etapa 0 🟢 cerrada** (esta fusión, 2026-08-02).

**Etapa 1a 🟢 cerrada** (2026-08-02): tanto `Ticket.ticketCode` como `Job.code` ya tenían `@unique` en el schema — el problema real no era la falta de constraint, era el manejo: `(app)/tickets/actions.ts` y `portal/[slug]/tickets/actions.ts` hacían `findUnique` (check) y luego `create` por separado, dejando una ventana de carrera real, y ante colisión pegaban un sufijo `Date.now().toString(36)` no determinístico; `generateJobCode` no tenía ningún manejo — el segundo `create` concurrente fallaba con el error crudo de Prisma hasta el usuario. Con Turso/libSQL resolviendo conflictos por MVCC al momento del commit (no con locks), el fix correcto es reintentar sobre el P2002 real: `createTicketWithUniqueCode()` (nuevo, `src/lib/tickets/ticket-code.ts`) y `generateJobCodeWithRetry()` (nuevo, `src/lib/cashflow/generate-code.ts`), ambos con backoff determinístico (`-2`, `-3`...) en vez de timestamp. Los 3 call sites en vivo (`(app)/tickets/actions.ts`, `portal/[slug]/tickets/actions.ts`, `(app)/flujo/actions.ts`) migrados; los scripts de importación masiva (`reconcile-*.ts`) no se tocaron — siguen usando `generateJobCode` con `seqCache` propio, patrón correcto para bulk. Bug real encontrado y corregido en el camino: la primera versión importaba el cliente completo de Prisma (`Prisma.PrismaClientKnownRequestError`) dentro de `ticket-code.ts`, que también se importa desde un componente cliente (`new-ticket-form.tsx`, para la preview) — reventaba el build de Turbopack (`chunking context does not support external modules: node:module`). Corregido con un chequeo estructural del error (`'code' in e`) en vez de `instanceof`, sin importar el cliente de Prisma en un archivo compartido con el navegador. Verificado: typecheck limpio, lint limpio, 176/176 unit tests, build de producción completo sin errores. E2E (`full-ticket-flow.spec.ts`, `flujo-flow.spec.ts`) **no se pudo verificar** — bloqueado por algo no relacionado a este cambio: el puerto 3000 en esta máquina tiene otra app real ("CodeLine") escuchando y respondiendo en vez de INGEGAR One (confirmado con snapshot de página + `netstat`), mismo patrón que G17 (cerrado, aparentemente recurrente en esta máquina) — no es un fallo del código, es un puerto ocupado. Pendiente: liberar el puerto 3000 (o correr E2E con `E2E_PORT` distinto) y confirmar los 2 specs en verde.

**Etapa 1b 🟢 cerrada** (2026-08-02, investigación): el punto de enforcement de `ticket_id` en propuestas/informes es `POST /api/client-documents` (`ticketId: metadata?.ticketId ?? undefined` — 100% opcional hoy, decisión del caller).

**Etapa 1c 🟢 cerrada** (2026-08-02): ticket de origen obligatorio para crear Propuestas e Informes nuevos, en ambos editores.
- **Bug real encontrado y corregido de paso**: `report-editor.tsx` pasaba `ticketId={ticketId}` (el prop de la URL) a `SaveDocumentButton` en vez de `selectedTicketId` (el estado vivo del selector) — si un usuario llegaba a `/informe` sin `?ticketId=` y elegía un ticket desde el dropdown, el FK nunca se guardaba aunque la UI mostrara el ticket vinculado. Solo funcionaba por el deep-link desde la ficha de ticket (el único camino cubierto por el E2E de G7). Corregido.
- **Cotizador (`/cotizador`) no tenía ningún vínculo a ticket** — a diferencia de Informe, que ya traía un selector opcional. Se replicó el mismo patrón (`cotizador/page.tsx` ahora carga tickets igual que `informe/page.tsx`; `QuoteEditor` gana el mismo selector, autocompleta cliente/rut).
- Validación en dos capas: cliente (`SaveDocumentButton`, mensaje inmediato) y servidor (`POST /api/client-documents`, la garantía real — rechaza `propuesta`/`informe` sin `metadata.ticketId`; **no aplica a `type:'otro'`**, el upload libre de `/documentos`, caso de uso distinto sin trazabilidad a un trabajo).
- **Hardening adicional**: el servidor ya no confía en el `clientId` que manda el formulario cuando hay ticket — lo deriva del `ticket.clientId` real (tenant-scoped), evitando que un ticket de un cliente quede guardado con el `clientId` de otro por desincronía entre el selector de ticket y el dropdown de cliente del modal.
- Escape hatch para "crear ticket nuevo sin perder lo escrito" (punto 1 del informe): en vez de un modal de creación embebido, "+ Crear ticket nuevo" abre `/tickets/new` en pestaña nueva (cero pérdida de estado, cero código nuevo de creación duplicado) + botón "↻ Refrescar" que re-consulta la lista sin perder el formulario.
- Verificado: typecheck limpio, lint limpio, y en vivo en el navegador (puerto 3000 liberado esta sesión — ver nota abajo) contra el espejo local real: validación bloquea guardado sin ticket en ambos editores, seleccionar ticket autocompleta y desbloquea, cliente correcto pre-cargado en el modal. No se persistió ningún documento de prueba (cancelado antes de guardar) para no ensuciar el espejo.

**Nota de infraestructura de esta sesión**: el puerto 3000 estaba ocupado por otro proyecto (`next-server` corriendo dentro de WSL2, visible en `127.0.0.1:3000` por el forwarding automático de WSL2↔Windows — mismo patrón que G17, invisible al `netstat` de Windows, solo visible con `ss -tlnp` dentro de WSL2). Detenido con autorización explícita del dueño; INGEGAR One corre limpio en 3000 desde entonces.

**Etapa 2 🟢 cerrada** (2026-08-02): panel "Documentos del trabajo" nuevo en `/tickets/[id]` (`ticket-documents-panel.tsx`) — vista unificada de Fotografías/Videos/Otros (con origen cliente/técnico/administrador, derivado de `TicketDocument.uploadedBy.role`, sin columna nueva), OT, Propuestas, Informes, OC, Facturas y Boletas, todo por categoría.
- **Gap real cerrado de paso**: las propuestas vinculadas a un ticket nunca se consultaban en esta página (solo informes) — ahora sí, y con Etapa 1c exigiendo ticket de origen, esta sección deja de estar vacía por diseño.
- Reutiliza `FilePreviewButton` (patrón único de preview de toda la app, `frontend.md`) para archivos reales (fotos/videos/otros/OT); para Propuestas/Informes/OC-Factura enlaza a su editor/ficha propia en vez de intentar previsualizarlos inline (son documentos editables con dueño propio, no archivos crudos — "el ticket resume y enlaza, no replica", punto 6 del informe); Boletas usa el mismo patrón de link directo que ya usa `expense-list.tsx` (evita el bug de tratar un `data:` URI como key de R2).
- Cero tabla nueva, cero archivo duplicado — exactamente el criterio ya escrito en `data.md` y reforzado en el guardrail 3 de `ARQUITECTURA.md`.
- Verificado: typecheck limpio, lint limpio, 176/176 unit tests, build de producción completo, y en vivo en el navegador contra el espejo local real (ticket `260802-JUST-RQ1-TIENDATRAN`): 4 fotos categorizadas con origen "Administrador", 1 informe enlazado, preview real de R2 abre correctamente con Cerrar/Abrir original/Descargar. Sin errores de consola.

**Etapa 5 (bóveda de credenciales, punto 21) 🟢 cerrada** (2026-08-02): `/recursos/credenciales`, solo `super`. Modelos `Secret`/`SecretReveal` nuevos (migración aditiva local `20260802181850_add_secrets_vault`, 2 `CREATE TABLE`, cero `ALTER` sobre tablas existentes — no aplicada a Turso, queda para cuando el dueño autorice igual que toda esta etapa).
- Cifrado AES-256-GCM real (`node:crypto`, `src/lib/secrets/crypto.ts`) — nunca texto plano en `ciphertext`. Sin `SECRETS_ENCRYPTION_KEY` en el entorno, falla fuerte (nunca cae a una key hardcodeada ni guarda en plano) — variable nueva agregada a `.env` local con valor de desarrollo, documentada para rotar en prod.
- Revelar exige reautenticación (`bcrypt.compare` contra la password del propio usuario logueado, mismo patrón que `perfil/actions.ts`) y queda auditado en `SecretReveal` (nunca guarda el valor). Bloqueado además bajo "ver como" (mismo criterio que las acciones identity-bound de G32) — un `super` impersonando a otro rol no puede tocar la bóveda hasta salir de esa vista.
- Simplificación deliberada: sin ACL de "responsables" por credencial (que sí menciona el punto 21) — el gate a nivel de rol (`super`-only) ya cumple ese control con un solo tenant y pocos `super`; agregar una segunda capa sería abstracción sin necesidad real hoy.
- Nav: "Credenciales" solo visible para `super` (`role` ahora se pasa a `Sidebar`, filtro por `superOnly` en el link) — primera vez que el nav de INGEGAR One filtra por rol en vez de mostrar todo a super+supervisor por igual.
- Verificado en vivo, extremo a extremo, contra el espejo local: crear credencial de prueba → revelar con password incorrecta (rechazado) → revelar con password real (descifra correctamente, valor idéntico al ingresado) → eliminar (con modal de confirmación real, no `window.confirm`) → 0 credenciales al final, sin dejar datos de prueba. Confirmado además que `supervisor` (`sgarrido@ingegarchile.cl`) ni ve el link en el nav ni puede entrar por URL directa (rebota a `/dashboard`). Typecheck, lint, 176 tests y build, todos limpios. Sin errores de consola en ninguna corrida.

**Etapa 3 (4 estados en Ticket, punto 3) 🟢 cerrada — replanteada, sin schema ni backfill** (2026-08-02): el plan original (columnas nuevas en `Ticket` + backfill de los 142 tickets reales) se descartó al leer con más detalle el modelo real de `Job`: `Job` ya tiene el sistema de 5 etapas completo (`commercialStage`/`operationalStage`/`documentationStage`/`financialStage`/`processFlow`) — replicarlo en `Ticket` como columnas propias habría repetido exactamente el problema que este proyecto ya sufrió una vez (dos sistemas de estado en paralelo, ver Taxonomía en `ARQUITECTURA.md`) y encima con riesgo de backfill real. En cambio: `src/lib/tickets/ticket-state-summary.ts` **calcula** los 4 estados (operativo/documental/comercial/financiero) desde el `Ticket` y su `Job`/`Propuesta` vinculados — cero campo nuevo, cero migración, cero backfill, cero dato que pueda desincronizarse porque no hay un segundo lugar donde vivir. Reusa los predicados ya probados de `job-presets.ts` (`isPaidJob`, `isOverdueV2`, `hasPurchaseOrder`, `hasInvoiceInfo`) en vez de reimplementar reglas de negocio. De paso, hallazgo menor: `Ticket.jobId` (`@unique`) existe en el schema pero nadie lo escribe en todo el código — remanente muerto de un diseño anterior a `Job.originTicketId` (el mecanismo real, activo). No se tocó — fuera de alcance, dejado documentado acá.
- Verificado en vivo contra 2 tickets reales distintos: uno resuelto sin Job (Operativo: Terminado, Documental: En revisión — falta informe, Comercial: Sin propuesta, Financiero: Sin trabajo, coincide con el banner "registra el cobro" ya visible en la página) y uno nuevo sin asignar (Pendiente/Incompleto/Sin propuesta/Sin trabajo). Typecheck, lint, 176 tests, build y consola sin errores.

**Auditoría de vinculación (2026-08-02, pedido explícito del dueño antes de Etapa 4)**: `scripts/check-ticket-doc-linking.ts` (read-only, queda como registro) contra el espejo local real —
- Informes: 42 total, solo 1 sin ticket (2%) — ya estaban bien vinculados de antes.
- Propuestas: 2 total, las 2 sin ticket (100%) — muestra chica, ambas anteriores a la Etapa 1c.
- **`Job`: 386 total, 328 sin `originTicketId` (85%), 231 de esos con monto real** — el hallazgo grande. No es histórico de import únicamente (los 207 jobs de `import-flujo.ts`): la muestra incluye trabajos con fecha de junio-julio 2026, creados en vivo vía `/flujo/trabajos/new` sin pasar por un ticket — un segundo camino de creación que existía en paralelo al de tickets antes de esta sesión.
- Tickets: 119 activos, 28 sin ningún documento vinculado (24%) — no necesariamente un problema, puede ser tickets nuevos aún sin evidencia.
- Resuelto con la pestaña "Triage legado" de arriba, no con una migración/backfill masiva — el dueño explícitamente no quiso forzar 328 tickets retroactivos.

**Auditoría de integridad post-Etapa 3/5 (2026-08-02, pedido explícito del dueño)** — verificación de que no haya cruce de información entre tickets:
- Grep completo de `src/`: cero lugares buscan Ticket/Job/documento por `otNumber`/`invoiceNumber`/`purchaseOrder` en vez de por FK real — confirmado que no hay riesgo estructural de mostrar el documento equivocado.
- Hallazgo real (no introducido esta sesión, preexistente): `Job.purchaseOrder = "AUN NO MANDAN LA OC"` (texto, no un número) en 4 trabajos de 2 clientes — `hasPurchaseOrder()` de `job-presets.ts` los cuenta como "con OC" cuando en realidad dicen lo contrario. Afecta tanto el dashboard `/flujo` (preexistente) como el nuevo resumen de 4 estados. No corregido — requiere criterio del dueño sobre qué hacer con esos 4 casos puntuales, no es cosa de adivinar.
- Hallazgo real: `Ticket.otNumber = "1758"` compartido por 2 tickets (`260802-TARR-UR1-TIENDACOST` y su gemelo `-mzcs`, con el sufijo exacto del parche de colisión viejo) — evidencia concreta y real para cuando se aborde el punto #26/#29 (duplicados). No tocado.
- Investigado si había forma de auto-vincular más de los 328 huérfanos sin adivinar (`quoteRef`/`importRef`/`jobNumber`/`code`): ninguno referencia un `Ticket.id`/`ticketCode` real — `importRef` es un identificador de LOTE de import, no una referencia cruzada. Confirma que el triage manual es la única vía correcta, no un atajo pendiente de construir.
- Bug propio encontrado y corregido: los 4 componentes nuevos de hoy (`credentials-manager.tsx`, `legacy-triage.tsx`) usaban `buttonClass(..., 'sm')` (36px) en botones de confirmación real (Cancelar/Confirmar/Eliminar), contra el precedente ya establecido (`ticket-list-view.tsx`, `Button` sin `size` = 44px por default). Corregido en los 4 lugares.

**Gap cerrado — auditoría de acciones de conciliación**: `Job.legacyReviewedById`/`legacyReviewedAt` (aditivo) — `markJobsLegacy`/`unmarkJobLegacy` ahora registran quién y cuándo, mostrado junto al botón "Deshacer". Alcance deliberadamente acotado: no es el historial completo del punto #28 (deferido aparte), y `linkJobToExistingTicket` no se audita a propósito — es mecánico y auto-evidente, mismo criterio que ya aplica el `linkJobToNewTicket` preexistente (tampoco auditado).

**Gap cerrado — Job seguía creándose sin ticket**: `/flujo/trabajos/new` ahora exige ticket de origen para trabajos NUEVOS (mismo patrón que Cotizador/Informe de la Etapa 1c: selector scopeado por cliente, "+ Crear ticket nuevo" en pestaña aparte, validación cliente+servidor incluyendo que el ticket sea del mismo cliente). `updateJob` es una función separada — editar trabajos históricos sin ticket sigue funcionando sin bloqueo, verificado en vivo contra el mismo huérfano de Autoplanet. Herencia automática del ticket de una propuesta del Pipeline si la propuesta ya tiene uno (Etapa 1c lo garantiza para propuestas nuevas). Esto cierra la fuente que seguía generando huérfanos nuevos — sin este fix, el Triage legado de la Etapa 4-prep se habría vuelto a llenar.

**Etapa 4 (cuotas/múltiples OC-factura) 🟢 cerrada** (2026-08-02): modelo nuevo `JobInstallment` (aditivo: `ALTER TABLE ADD COLUMN` + `CREATE TABLE`, sin rebuild — migración local `20260802214511_add_job_installments`, script Turso-safe `scripts/add-job-installments.ts` escrito y NO ejecutado en prod, pendiente autorización del dueño) — cada trabajo puede tener 0 o varias cuotas, cada una con su propia OC/factura/fecha/crédito/pago, en vez de forzar el modelo de "una sola OC-factura" que no cubre el caso real de pagos parciales por tienda. `job-presets.ts` extendido con predicados conscientes de cuotas (`isPaidJob`, `hasPurchaseOrder`, `isOverdueV2`, etc.) que leen `Job.installments` **solo si el caller lo selecciona en su query** — el resto de la app (dashboard, `/flujo/reportes`, etc., no migrados a propósito) sigue leyendo los campos planos de siempre, cero regresión para los 386 trabajos existentes. Criterio de aceptación explícito del punto #12: cerrar una cuota no cierra el trabajo si quedan otras pendientes, y el saldo total es siempre la suma de saldos de cuota (nunca un campo aparte que pueda desincronizarse) — cubierto con 7 tests nuevos (183/183 total). UI: sección "Cuotas" (acordeón) en `/flujo/trabajos/[id]` con activar/agregar/editar/eliminar cuota y aviso si la suma no cuadra con el neto del trabajo; `TicketDocumentsPanel` reemplaza sus entradas planas de OC/Factura por una por cuota (`"OC-123 (cuota 2)"`, enlazando a `?section=cuotas`) cuando el trabajo tiene cuotas, sin tocar el caso plano (mayoría de los trabajos). Deliberadamente fuera de alcance: subir archivo por cuota (`purchaseOrderFileUrl`/`invoiceFileUrl` existen en el modelo, sin UI que los use todavía).

**2 bugs reales encontrados y corregidos durante la verificación en vivo** (no en el código nuevo revisado por lectura, solo reproducidos al usar la feature real):
- **Editar cuota no guardaba nada, sin error visible**: el formulario de edición no incluía `jobId` (solo el de "agregar cuota" lo tenía), y `jobInstallmentInput` lo exige — cada guardado tiraba una excepción de Zod del lado servidor que nunca llegaba a la consola del navegador ni a ningún log visible; los datos quedaban exactamente igual y parecía que el guardado "no hacía nada". Confirmado leyendo `prisma/dev.db` directo (SQLite) — `updatedAt` de la fila nunca cambiaba pese a múltiples intentos de guardado por UI. Corregido agregando el `<input type="hidden" name="jobId">` que ya usaba el form de "agregar".
- **Campo numérico opcional vacío se guardaba como `0`, no `null`**: `z.coerce.number().int().nonnegative().optional()` corre `Number(v)` ANTES de que `.optional()` vea el campo — `Number('')` es `0`, no `undefined`, así que dejar "Días de crédito" o "Monto pagado" en blanco guardaba `0` real. Efecto concreto: `installmentDueDate()` usa `creditDays ?? 30` — con `0` guardado en vez de `null`, el fallback de 30 días nunca se activaba (vencimiento = fecha factura + 0 días). Corregido con un `z.preprocess` en `jobInstallmentInput` (`schemas.ts`) que convierte `''` a `undefined` antes de coercionar. **Mismo patrón preexiste en `jobInput`/`jobQuickEditInput`** (`netAmount`, `taxAmount`, `creditDays` — usado desde antes de esta sesión) — no tocado, fuera de alcance de esta etapa, pero es el mismo bug de fondo y debería evaluarse si vale la pena aplicar el mismo `preprocess` ahí.

Verificado en vivo contra el espejo local, incluyendo un trabajo real con ticket de origen (Just Burger, `20260510-VINADELMAR-019`): activar cuotas → agregar cuota con OC/factura/monto distinto al neto (aviso de descuadre visible) → editar con fecha factura y crédito en blanco (vencimiento = +30 días, correcto tras el fix) → marcar pagada (fila en verde, saldo total $0) → ticket muestra `"OC-... (cuota 1)"` enlazado, con la entrada plana de OC/Factura suprimida correctamente. Dato de prueba limpiado del espejo local al terminar (el botón "Eliminar" dispara `window.confirm()`, que bloqueó la automatización del navegador — limpiado con `DELETE` directo sobre `prisma/dev.db`, no sobre producción). Verificado: typecheck, lint, 183/183 unit tests, build de producción completo.

**Auditoría completa de los 33 puntos (2026-08-02, verificada contra el código real, no contra memoria de sesión — un primer intento de audit vía subagente falló dos veces por límite semanal y por stall, así que esto se hizo con Grep/Read directos):**

*Cerrados (10, ver también #9 más abajo, cerrado horas después de esta auditoría):*
- **#1** Ticket obligatorio — Etapa 1c.
- **#3** Estados de 4 vías — Etapa 3 (`ticket-state-summary.ts`, calculado).
- **#4** Documentos únicos por origen — Etapa 2 (`ticket-documents-panel.tsx`).
- **#5** Sincronización ticket↔informe — preexistente, confirmado: `report-editor.tsx` importa fotos del ticket (`ticket?.photos.filter(...)`) vía `ReportPhotosEditor`.
- **#6** Ficha de ticket completa — `/tickets/[id]` ya muestra `TicketStateSummary` + `TicketDocumentsPanel` + trabajos/costos/OC/facturas/cuotas + propuestas + informes en una sola vista, con edición delegada a cada módulo (nunca formularios duplicados).
- **#12** Cuotas — Etapa 4 (esta sesión).
- **#21** Bóveda de credenciales — esta sesión (`src/lib/secrets/`, `credentials-manager.tsx`).
- **#27** ID único con correlativo diario al final — Etapa 1a.
- **#30** Migración histórica segura / bandeja de regularización — Triage legado + `Job.legacyNoTicket`/`legacyReviewedBy`.

*Parciales (11) — con lo que falta:*
- **#8** Visor universal — `file-preview-modal.tsx` ya abre en modal con descargar, sin salir de contexto; **falta** zoom, pantalla completa y navegación anterior/siguiente entre documentos.
- ~~**#9** Conciliación como tablero maestro~~ → **🟢 CERRADO el mismo día** (2026-08-02, elegido por el dueño como siguiente prioridad tras esta auditoría): agregadas columnas Financiero/Vence/Saldo a `/conciliacion`, reusando `financialState()` (`ticket-state-summary.ts`) y `jobDueDateV2`/`jobInstallmentsSummary` (`job-presets.ts`) — misma fuente de verdad que `/tickets/[id]` y `/flujo`, cero lógica financiera nueva (cierra de paso parte del punto #25). Cuotas ahora sí se reflejan: saldo = suma de saldos de cuota, y un badge "N cuotas" enlaza a `?section=cuotas` en vez de intentar mostrar varias fechas/montos en una celda ("sin ensanchar la tabla", criterio explícito del punto). Nueva fila de filtros financieros (sin_oc/pendiente_facturar/facturada/vencida/pagada) con conteos reales, independiente del filtro documental existente. Bug propio corregido en el camino: el contenedor de la tabla usaba `overflow-hidden` (recortaba en vez de scrollear) — cambiado a `overflow-x-auto`, confirmado en vivo que el scroll horizontal queda contenido en la tabla sin mover la página (criterio del punto #24). Fuera de alcance deliberado: "presupuesto" (link a propuesta) y "responsable" — no tienen un campo inequívoco hoy, no se inventó uno. Verificado en vivo contra el espejo local: typecheck/lint/build limpios, filtro "Vencida" (26 trabajos reales) muestra vencimientos y saldos correctos (ej. Pandora $2.900.000, Tarragona $450.000), scroll horizontal confirmado por captura de pantalla.
- **#10** Filtros e informes — `/flujo/reportes` ya tiene cliente/sucursal/fechas/tipo/estado financiero/`sinTecnico` + export Excel (`api/flujo/export`); **falta** confirmar filtro por ticket individual y salida en PDF (hoy solo Excel).
- **#11** OC del cliente — `JobInstallment.purchaseOrder`+fecha+archivo ya existen; **falta** el enum de 5 estados estructurados (Sin OC/OC solicitada/Con OC/Parcial/Anulada con motivo) — hoy es presencia/ausencia de texto libre, no un estado.
- **#13** Facturación/cobranza/recordatorios — campos de factura/vencimiento/pago ya existen en `Job` y `JobInstallment`; **falta** cualquier sistema de aviso/recordatorio (el día del vencimiento y luego periódico) — no existe infraestructura de notificación para esto todavía.
- **#14** Gastos ticket vs generales — `Expense.ticketId` ya es opcional con selector "Trabajo (opcional)" en el formulario; **falta** la elección explícita binaria (Asociado/General) y exigir categoría/centro responsable solo en el caso general.
- **#17** Técnicos/RRHH sin borrar historia — ya existe `toggleTechnicianAccountActive` (desactivar sin borrar); **falta** cualquier función de eliminación definitiva (ni siquiera para registros de prueba sin dependencias — hoy no se puede borrar un técnico bajo ningún caso, lo cual es seguro pero no cumple la mitad del punto).
- **#18** Portal sincronizado — self-service de sucursales/usuarios ya construido (commit `b4fc941`) y alimentado desde el expediente real; **falta** prevención de sucursales duplicadas al *crear* (el dedupe de las 13 sucursales duplicadas, commit `c70d8f5`, fue retroactivo/manual, no un guard en `createPortalBranch`/`createBranch`).
- **#23** Rendimiento medible — G41/G43 ya midieron y cerraron el round-trip extra (~5s reales en `getTicket()`, causa raíz confirmada: Prisma 7 sobre libSQL/Turso sin `relationLoadStrategy:"join"`); **falta** definir metas explícitas (ej. "\<Xms navegación entre módulos") — hoy hay medición pero no un objetivo numérico acordado.
- **#24** Correcciones móviles — fixes de touch-target ya aplicados (commit `d7d59d7`) y `tests/e2e/mobile-audit.spec.ts` corre en todas las rutas a 390×844; **falta** confirmar cobertura específica de Conciliación/modales bajo el criterio de "tarjetas o scroll controlado" que pide el punto.
- **#25** Definiciones comunes de cifras — `job-presets.ts` ya es la fuente compartida real (`dashboard`, `/flujo`, `/flujo/reportes`, `api/flujo/export`, `/flujo/trabajos/[id]` lo importan); **falta** que Conciliación lo use también para sus estados (hoy tiene su propia lógica de estado documental, un eje distinto pero no explícitamente reconciliado con el de `job-presets.ts`).
- **#32** Permisos y auditoría transversal — el enforcement de permisos en servidor ya es consistente (66 call sites de `requireActor([roles])`, no solo ocultar botones — mismo criterio que ya corrigió G34/G45); **falta** un `AuditLog` unificado con antes/después — hoy la auditoría es ad-hoc por tabla (`SecretReveal`, `Job.legacyReviewedBy`, `TicketHistory`), no una traza transversal para pagos/eliminar/publicar/ver costos/ver secretos/configurar integraciones como pide el punto.

*No iniciados (11):*
- **#2** Dos modalidades comerciales (PP/ED) — confirmado: `Ticket` no tiene ningún campo de modalidad (`Job.processFlow` existe pero no en `Ticket`).
- **#7** Navegación entre tickets (crear otro / anterior-siguiente) — confirmado, sin rastro en `/tickets/[id]` ni `/tickets/new`.
- **#15** Correlativo configurable de propuestas (universal vs. por cliente) — confirmado, no existe la configuración.
- **#16** Secciones opcionales en propuestas — confirmado: `quote-editor.tsx` tiene secciones fijas (Alcance, Exclusiones, etc.), el único toggle `enabled` que existe es para ítems de ajuste de precio, no para secciones completas.
- **#19** Correo integrado — confirmado, sin infraestructura (ya señalado en sesiones previas).
- **#20** Datos de empresa editables — confirmado: `Tenant` no tiene `razonSocial`/`rut`/dirección/contactos/logo en el schema; `/recursos/empresa` solo gestiona `CompanyDocument` (biblioteca de archivos), no un formulario de configuración.
- **#22** Vehículos/GPS en tiempo real — confirmado: solo un link estático a `NEXT_PUBLIC_GPS_URL`, sin integración API — coincide con el fallback que el propio punto permite si no hay contrato técnico confirmado con el proveedor.
- **#26 / #29** Duplicados y datos de prueba (tickets) — confirmado: no existe ninguna etiqueta Producción/Prueba/Archivado ni detección de posibles duplicados a nivel de `Ticket`. Distinto del dedupe de sucursales ya cerrado (branches, no tickets/jobs). Evidencia real ya recolectada para cuando se aborde: `Ticket.otNumber="1758"` compartido por 2 tickets (documentado en la auditoría de integridad post-Etapa 3, más arriba).
- **#28** Historial unificado transversal — confirmado: solo existe `TicketHistory` (cambios de `status`); no cubre Técnicos, Informes, Propuestas, Conciliación, Flujo de Caja, Portal cliente ni integraciones como pide el punto.
- **#31** Jerarquía visual en acordeones (P2, baja prioridad) — confirmado, sin trabajo.

*Metodológico (no es una feature, es cómo se está trabajando):* **#33** Entrega gradual/respaldo/regresión — ya es la práctica seguida en toda esta iniciativa (etapas separadas, migración additiva con backup previo vía `scripts/backup-turso-tables.ts`, nunca todo junto) — se sostiene como criterio de proceso, no como algo que "se implementa" una vez.

Con esto, el informe queda mapeado punto por punto contra el código real — no quedan puntos sin evaluar. **Actualización 2026-08-02**: #9 (conciliación financiera) y #32 (permisos + auditoría transversal) cerrados el mismo día — ver G48/G49/G49.1. **Actualización 2026-08-03**: #2 (modalidades PP/ED) cerrado — ver G50. #11 (OC del cliente) cerrado el mismo día — ver G51. #13 (facturación/cobranza/pagos/recordatorios) cerrado el mismo día — ver G52. #14 (gastos directo vs. general) cerrado el mismo día — ver G53. **Actualización 2026-08-04**: #25 (consistencia transversal de cifras y estados) cerrado — ver G54. Resto de pendientes (#7, #15 al #24, #26 al #33 salvo lo ya cerrado) sin tocar; dado el volumen (varios P0 de alcance comparable a una etapa completa), no se asume cuál sigue sin confirmación explícita del dueño. |
| G47 | **Validación de los 4 scripts Turso pendientes de G46** (`add-secrets-vault.ts`, `add-job-legacy-no-ticket.ts`, `add-job-legacy-reviewed-by.ts`, `add-job-installments.ts`) + `backup-turso-tables.ts` + `check-ticket-doc-linking.ts` (2026-08-02) — comparados línea por línea contra su `migration.sql` de origen y probados en una copia local desechable (`prisma/dev.migration-test.db`, nunca Turso) llevada manualmente a estado pre-migración (schema real de producción hoy) antes de correr los scripts. **Blocker real encontrado y corregido**: `backup-turso-tables.ts` usaba `prisma.job.findMany()` sin `select` — el cliente Prisma generado ya conoce las columnas de esta sesión (`legacyNoTicket` etc.) que producción todavía no tiene, así que el backup fallaba con P2022 "column does not exist" contra el schema real de hoy (mismo patrón que G44). Corregido a `$queryRawUnsafe('SELECT * FROM x')` — ahora resiliente a que el cliente vaya un paso adelante del schema real, exactamente la situación normal justo antes de una ventana de migración. Gap menor cerrado de paso: `add-secrets-vault.ts`/`add-job-installments.ts` tenían una ventana de no-atomicidad entre `CREATE TABLE` y su `CREATE INDEX` (una interrupción justo ahí dejaba el índice sin reparación posible en un reintento) — reescritos con `CREATE TABLE/INDEX IF NOT EXISTS` (verificado que `@libsql/client` lo soporta), eliminando el chequeo condicional previo. | confirmado-código (diff línea por línea contra `migration.sql`) + confirmado-integración (ejecución real, 2 pasadas, sobre copia local desechable) | 🟢 **CERRADO — apto para ventana controlada, no ejecutado en Turso**. 1ª pasada: los 4 scripts corren limpio sobre la copia pre-migración (386 jobs preservados). Estructura resultante (`sqlite_master`, columnas, índices, FKs) idéntica byte-a-byte a `prisma/dev.db` real, salvo un hallazgo ya documentado y aceptado: `legacyReviewedById` queda sin su FK físico hacia `users` porque `ALTER TABLE ADD COLUMN` no puede agregar un FK en SQLite sin rebuild completo (mismo criterio ya usado en `add-expense-paidat.ts`/`add-job-origin-proposal.ts` — evitar el riesgo G26 de un rebuild interrumpido contra Turso); impacto bajo porque Prisma no necesita el FK físico para las relaciones y el único código que escribe ese campo siempre usa un `actor.id` autenticado real. 2ª pasada: sin errores, sin estructuras duplicadas, conteos de `jobs/tickets/branches/clients/users/tenants` idénticos a la 1ª pasada. Rollback demostrado en la copia: `DROP TABLE` para las 3 tablas nuevas (vacías, sin dato que perder) + `ALTER TABLE ... DROP COLUMN` para las columnas simples; para `legacyReviewedById` (FK-bound) el rollback es el mismo rebuild manual usado para preparar la copia de prueba, ya verificado preservando las 386 filas. `check-ticket-doc-linking.ts` (solo lectura, hardcodeado a `file:./prisma/dev.db`) confirmado sin cambios necesarios, corrido contra el espejo real: reproduce los mismos números ya citados en G46 (328/386 Jobs sin ticket, 231 con monto real, 28/119 tickets activos sin documentos). Verificado: typecheck/lint/183 tests/build limpios. Riesgo restante: ninguno bloqueante — la única diferencia de schema conocida (FK de `legacyReviewedById`) queda documentada, no oculta. **Precisión de rollback (2026-08-02, pedido explícito del dueño)**: el rollback demostrado arriba (rebuild manual sin la columna) es una prueba de que el camino EXISTE, no el procedimiento recomendado en producción. Ante un problema real con `legacyReviewedById`, el camino operativo preferido es revertir el código que lo usa (Triage legado en `/conciliacion`) y dejar la columna aditiva sin uso — nunca reconstruir `jobs` en Turso salvo corrupción real confirmada o autorización explícita del dueño, dado el riesgo de un rebuild interrumpido contra una base remota (precedente G26). |
| G48 | **#32A — Permisos y regresión por roles** (2026-08-02, diagnóstico acotado, sin AuditLog transversal ni PP/ED — eso queda para el punto #32 completo). Roles reales: `super`, `supervisor`, `client` (+`isClientAdmin`), `tecnico`. Verificado server-side (no solo UI oculta): edge (`auth.config.ts`) bloquea rutas internas para client/tecnico antes de cualquier página o Server Action; `requireActor(roles)` presente en 100% de las acciones revisadas (`flujo/actions.ts` 15/15, `tickets/actions.ts` 7/7, `credenciales/actions.ts` 4/4, `client-documents/route.ts` 4/4); `tenantScope()` en 670 nodos del grafo de código. Portal: `canViewPortal()` compara `session.user.clientId` contra el cliente objetivo, nunca confía en la URL; branch-user scoping (G45) intacto en sus 3 call-sites sin regresión; self-service del portal con `role:'client'` hardcodeado literal (escalación a rol interno confirmada imposible). Técnico: cero campos financieros alcanzables desde `/mi-panel`. Cron: `CRON_SECRET` + comparación timing-safe (correcto). Matriz completa abajo. | confirmado-código: lectura directa de `tenant.ts`, `proxy.ts`, `auth.config.ts`, `portal-auth.ts`, `api/files/route.ts`, `api/portal/informes/route.ts`, y grep de cobertura `requireActor` en los 4 grupos de acciones citados | 🟢 **CERRADO (alcance acotado)** — 1 brecha real encontrada y corregida: `/api/files?type=ticket` no aplicaba `assignedToId` para `tecnico` — la pantalla (`/mi-panel/tickets/[id]`) ya restringe a tickets asignados, pero el endpoint de descarga permitía traer el documento de CUALQUIER ticket del tenant conociendo su key (acceso directo por API, no por pantalla). Corregido agregando `assignedToId: session.user.id` al filtro cuando `role==='tecnico'`, mismo criterio que ya usa la página — extraído a `src/lib/files-access.ts` (`ticketFileFilter()`) para poder testear el scoping sin mockear Next.js/Prisma/auth. Sin brechas de tenant/cliente encontradas — el patrón fetch-doc-luego-compara-dueño ya es consistente en los endpoints revisados. **Verificado en vivo, no solo por unit test**: 6 tests unitarios nuevos (`tests/unit/files-access.test.ts`, 189/189 total) + reproducción real en el navegador — login como `jdiaz@ingegarchile.cl` (técnico real, password local reseteada solo para la prueba, invalidada después), acceso directo por URL a `/conciliacion` → redirige a `/mi-panel` (edge gate), fetch directo a `/api/files?type=ticket` con la key de un documento de un ticket asignado a OTRO técnico → `404` (antes del fix habría sido un `307` con URL firmada real), mismo fetch con la key de su propio ticket → redirect exitoso (confirma que el fix no rompe el acceso legítimo). Typecheck/lint/tests/build limpios. **No cubierto en este alcance** (declarado explícitamente, no un olvido): matriz completa de las 24 rutas API restantes fila-por-fila, specs de Playwright formales para estos flujos (la verificación fue manual vía navegador, no quedó como test repetible en `tests/e2e/`), AuditLog transversal (punto #32 completo). |

**Matriz de permisos real (recurso × acción, roles permitidos) — G48, 2026-08-02:**

| Recurso | Acción | Permitido | Validación frontend | Validación servidor | Prueba |
|---|---|---|---|---|---|
| Tickets (interno) | Ver/crear/editar | super, supervisor | Sidebar oculta el módulo a client/tecnico | edge (`auth.config.ts`) + `requireActor` en cada action | E2E `full-ticket-flow.spec.ts` (existente) |
| Tickets (mi-panel) | Ver/actuar sobre asignados | tecnico (solo `assignedToId===self`) | Solo ve sus tickets en la lista | `where: { assignedToId: actor.effectiveId }` en la query, no solo en la UI | Pendiente (no hay test dirigido a IDOR aún) |
| Tickets (portal) | Ver/crear/aprobar | client (dueño), `isClientAdmin` para aprobar sucursal | Nav condicional | `canViewPortal()` compara `session.user.clientId` | E2E portal existente (b4fc941) |
| Trabajos (Flujo de Caja) | Ver/crear/editar/eliminar/cuotas | super, supervisor | Sidebar oculta el módulo | `requireActor(['super','supervisor'])` en las 15 actions de `flujo/actions.ts` | 183 unit tests de `job-presets.ts`/cuotas |
| Informes/Propuestas | Crear/editar/publicar | super, supervisor | Sidebar oculta el módulo | `client-documents/route.ts` chequea rol en 4/4 handlers | Sin test dirigido de permisos |
| Documentos y R2 (`/api/files`) | Descargar | según `type`: ticket→dueño+tenant (**tecnico ahora limitado a `assignedToId`**), technician/company/job→tenant, client-editor→super/supervisor | No aplica (redirect directo) | Fetch-doc-luego-compara-dueño en los 5 `type` | Test unitario nuevo (este bloque) |
| Documentos portal (`/api/portal/informes`) | Ver/descargar | client dueño del doc, super, supervisor | No aplica | `doc.clientId === session.user.clientId` | Sin test dirigido |
| Clientes y sucursales | Ver/crear/editar | super, supervisor (branch: también `isClientAdmin` vía portal) | Sidebar oculta el módulo | `requireActor`/`canViewPortal` según superficie | E2E portal self-service existente |
| Conciliación | Ver/triage | super, supervisor | Sidebar oculta el módulo | `requireActor(['super','supervisor'])` en `conciliacion/page.tsx` | Sin test dirigido |
| Finanzas (montos, OC, facturas) | Ver | super, supervisor (nunca tecnico ni client sin portal) | `/mi-panel` no renderiza campos financieros | Confirmado por grep: cero referencias a `netAmount`/`purchaseOrder`/`invoiceNumber` en `src/app/mi-panel/` | Confirmado por inspección, sin test dirigido |
| Usuarios/cuentas | Crear/editar/resetear password | super, supervisor (portal: `isClientAdmin` solo para usuarios de su propio cliente) | Sidebar/nav oculta según rol | `requireVaultActor`-equivalente en cada action; `role` nunca se toca en las actions de portal | Verificado en vivo en sesión previa (b4fc941) |
| Secretos (bóveda) | Ver metadata/crear/revelar | super únicamente, nunca bajo "ver como" | Módulo oculto a otros roles | `requireVaultActor()` — bloquea explícitamente `viewingAsName` activo | Sin test dirigido (ver G47) |

| G49 | **#32B — Auditoría transversal, base** (2026-08-02 — cierre real y estado final en **G49.1**, aceptado por el dueño; esta entrada queda como registro histórico del primer intento, que el dueño marcó insuficiente al compararlo contra el requerimiento original). Modelo `AuditLog` nuevo (migración local `20260803022952_add_audit_log`, CREATE TABLE puro, sin rebuild) — coexiste sin duplicar con `TicketHistory` (status de ticket), `SecretReveal` (quién vio qué secreto) y `Job.legacyReviewedBy` (triage legado): cada uno sigue siendo la fuente correcta para SU evento, `AuditLog` cubre lo que no tenía ningún lugar central. Helper `src/lib/audit.ts` (`logAudit()`) — `before`/`after` son objetos que el CALLER arma explícitamente con solo los campos que cambiaron (nunca una fila completa de Prisma), y un bloqueo duro por regex (`password\|hash\|token\|secret\|ciphertext\|signedurl\|apikey`) que **lanza excepción** si algún campo sospechoso llega — fail-loud, no redacción silenciosa. Página de consulta `/recursos/auditoria` (super+supervisor, mismos roles que #32A), filtros por fecha/actor/tipo de entidad/ID/acción, reutiliza `DateRangeFilter`/`FilterBar` existentes — sin módulo analítico nuevo. | confirmado-código: diff de schema vs `migration.sql`, grep de cobertura por categoría | confirmado-integración: prueba en vivo end-to-end (crear secreto → verificar en `/recursos/auditoria` que aparece actor/rol/tenant/acción correctos y **el valor del secreto NO aparece en ningún lado** → eliminar el mismo secreto → verificar segundo evento → probar filtro por acción, funciona) | 🟡 **PARCIAL — base sólida, eventos incompletos (ver G49.1).** Eventos auditados (9 call sites reales, no simulados): usuarios (`recursos/clientes/actions.ts`: create/update/reset-password/toggle-active portal user; `recursos/tecnicos/actions.ts`: los mismos 4 para técnico; `portal/[slug]/sucursales/actions.ts`: create/toggle self-service de cliente); secretos (`recursos/credenciales/actions.ts`: create/update/delete — reveal ya en `SecretReveal`, no duplicado); financiero (`flujo/actions.ts`: `markJobPaid`/`markJobPending`/`quickUpdateJob` —solo si OC/factura/monto cambió, no cada campo— /`addInstallment`/`updateInstallment`/`deleteInstallment`); propuestas (`pipeline/actions.ts:updatePipelineStatus` — solo decisiones reales: aceptada/rechazada/perdida, no cada "vista"/"enviada"); documentos (`api/client-documents/route.ts` DELETE; `tickets/actions.ts:updateTicketFields` — solo cuando `showToClient` cambia). **Deliberadamente NO auditado** (declarado, no un olvido): conciliación/triage legado (`Job.legacyReviewedBy` ya lo cubre, duplicarlo violaría la regla explícita de este bloque de "no duplicar TicketHistory si ya contiene correctamente un evento específico" — mismo criterio aplicado a este mecanismo); creación/edición de informes (solo su eliminación se audita — no hay un flujo de "publicación" propio distinto de guardar, y guardar un informe no es una decisión sensible del mismo tipo que aprobar/rechazar una propuesta); navegación, filtros, descargas ordinarias. Reglas no negociables cumplidas: creación solo en servidor (ninguna ruta de cliente puede escribir `AuditLog` directo), sin acción de editar/borrar expuesta en ningún lado (ni siquiera un botón oculto — no existe el código), aislado por tenant (`tenantScope()` en la query de `/recursos/auditoria`, igual que todo lo demás), sin secretos jamás (bloqueo por regex, probado con 3 tests unitarios + confirmado en vivo con un secreto real). Pruebas: 3 tests unitarios nuevos (`tests/unit/audit.test.ts`, 192/192 total — el bloqueo del sanitizador se prueba sin necesitar DB real, porque `sanitize()` lanza ANTES de que `prisma.auditLog.create()` llegue a ejecutarse) + verificación en vivo end-to-end descrita arriba. Typecheck/lint/tests/build limpios. Script Turso `scripts/add-audit-log.ts` (mismo patrón `IF NOT EXISTS` que G47) validado en copia local desechable: 2 ejecuciones sin error, estructura idéntica byte-a-byte a `prisma/dev.db`, conteos de `jobs/tickets/users/tenants/secrets` sin cambio, tabla nueva vacía — rollback = `DROP TABLE audit_logs` (tabla nueva, nada que perder). **No ejecutado en Turso** — sigue reservado para la ventana controlada junto con los 4 scripts de G47. Riesgos restantes: sin Playwright formal para estos flujos (verificación manual, no quedó como spec repetible en `tests/e2e/`); no hay test dirigido de "cliente no accede a auditoría de otro tenant" además de la inspección de código (mismo `tenantScope()` ya probado 670 veces en G48, no se consideró necesario reprobarlo por página). |

| G49.1 | **#32B — bloque de cierre, #32 completo** (2026-08-02): el dueño comparó G49 contra el requerimiento original y encontró eventos y criterios faltantes. No se rediseñó `AuditLog` ni se re-auditó el repositorio completo — se localizaron con Graphify + lectura dirigida solo los call sites que faltaban de los 9 puntos pedidos. **Eventos agregados** (12 call sites nuevos): `ticket.create` (`tickets/actions.ts:createTicket`); `ticket.assign_technician`/`ticket.reassign_technician` y `ticket.update` (N° OT/fecha estimada/resumen — únicos campos que el form real envía) en `updateTicketFields`; `ticket_document.create`/`.delete` (`api/tickets/[id]/documents/route.ts`); `ticket_document.replace` (`api/tickets/[id]/ot-photo/route.ts` — sube/reemplaza la OT, ya tenía ese comportamiento real, solo faltaba auditarlo); `client_document.create`/`.update` (`api/client-documents/route.ts` POST/PATCH — cubre propuesta creada + informe creado/generado/editado); `proposal.send` (`pipeline/actions.ts:updatePipelineStatus`, transición a `enviada`); `job.void` y `job.doc_client_sent` (`flujo/actions.ts:updateJob`); `job.delete` (`flujo/actions.ts:deleteJob`). Además: `ticketId` agregado al payload de todos los eventos de `Job`/`ClientDocument` que lo tienen disponible (vía `originTicketId`/`ticketId` real, no inventado). | confirmado-código: Graphify (`createTicket()`, `TECNICO_TRANSITIONS`, `updatePipelineStatus`) + lectura directa de cada route/action citado | confirmado-integración: typecheck/lint/197 tests/build limpios (una sola corrida al final); los nuevos call sites reutilizan el mismo `logAudit()` ya verificado en vivo en G49 (creación/eliminación real de un secreto, con antes/después correctos y sin fuga del valor) — no se repitió esa prueba en vivo para cada evento nuevo individualmente (la sesión de navegador se perdió a mitad de este bloque), la confianza viene de typecheck + mismo patrón exacto ya probado, no de una repetición manual por evento | 🟢 **CERRADO — #32 completo.** Criterios validados: `source` en el 100% de los eventos (nuevos y previos) ✅; orden estable `createdAt` + `id` como desempate (antes solo `createdAt`) ✅; filtro por módulo/origen agregado (`source`, contains-match) además de por entidad ✅; paginación ya existía ✅; **aislamiento por tenant con prueba específica nueva** (`tests/unit/audit-query.test.ts`, 4 tests — extraída la construcción del `where` a `src/lib/audit-query.ts` para poder probarla sin renderizar la página: confirma que `supervisor` siempre queda scopeado a su `tenantId`, `super` no, y que ningún filtro de la UI puede sobrescribir el scope) ✅; ausencia total de secretos, ya probada en G49, sin cambios ✅; **append-only probado explícitamente** (`tests/unit/audit.test.ts`, test nuevo: `audit.ts` exporta únicamente `logAudit`, confirmado además por grep que no existe ningún `auditLog.update`/`.delete` real en el código, solo en los comentarios JSDoc del cliente Prisma generado) ✅; relación con `ticketId` cuando aplica ✅ (ver arriba) — **excepción documentada**: los 3 eventos de `JobInstallment` (create/update/delete) solo llevan `jobId`, no `ticketId` resuelto — habría exigido una query extra por evento solo para ese dato de conveniencia, dado que `jobId→ticketId` ya es un salto barato desde cualquier otro lugar de la app; visibilidad de portal denegada por defecto ✅ (mismo `requireActor(['super','supervisor'])` ya verificado en vivo en G48 — no hay ruta de `client`/`tecnico` hacia esta página, no se consideró necesario re-probarlo). **Distinción actor humano/sistema/integración**: `actorId` nullable y `actorRole` como string libre ya lo permiten (`actorRole: 'system'` sin actor sería válido), pero hoy los 9 categorías de eventos pedidas son 100% acciones humanas via UI — no existe todavía un trigger de cron/integración que escriba alguno de estos 9 tipos de evento, así que este criterio queda **preparado pero no ejercido** (no se inventó un evento de sistema falso solo para probarlo). **No inventado** (declarado, no un olvido): "excluido" (documento) no tiene un endpoint propio — excluir una foto de un informe es parte de editar su `dataJson`, ya cubierto por `client_document.update`; "propuesta versionada" no es una fila nueva (confirmado: `PATCH` edita in-place, `metadata.version` si existe es un campo más dentro de esa misma edición) — no hay un mecanismo de versionado real distinto de "editar"; "informe... hecho visible" se resolvió como `Job.docClientSent` (el único estado real existente para esto), no como un "publicado" inventado; "reabierto"/"cerrado" de ticket siguen sin duplicar `TicketHistory`, que ya los captura completos (actor+timestamp+fromStatus+toStatus) — documentado como correspondencia explícita en el propio código (`tickets/actions.ts:updateTicketStatus`). Corrección manual de IDs históricos: no implementada (requiere funcionalidad nueva completa, fuera de este bloque) — `AuditLog` ya está preparado para registrar ese evento el día que exista (mismo `logAudit()` genérico, sin cambios necesarios). |

| G50 | **#2 — Modalidades PP (Presupuesto Previo) / ED (Ejecución Directa)** (2026-08-03). Hallazgo clave de la investigación (Graphify primero, luego lectura dirigida): `Job.processFlow` (`pre_quote`/`post_execution`) ya modelaba exactamente esta distinción, pero solo a nivel `Job` — nunca se elegía al crear el `Ticket`, y el form de Flujo de Caja la defaulteaba siempre a `pre_quote` sin relación real con el origen. El gap real no era "falta el concepto", era "la decisión no se toma en el momento correcto (creación del ticket) ni se propaga". Implementado: `Ticket.processFlow ProcessFlow?` nuevo (nullable, sin default — reusa el enum ya existente de `Job`, no crea uno paralelo; los 142 tickets históricos quedan `NULL` en vez de un valor inventado). Selector "Modalidad comercial" obligatorio en `new-ticket-form.tsx` (default `pre_quote`) y editable después en `TicketControls` (incl. "Sin definir" para no forzar un valor en tickets viejos). **Gate real de negocio**: un ticket PP (`pre_quote`) no puede pasar a `en_ejecucion` sin una propuesta/trabajo aprobado — aplicado en los DOS caminos reales que cambian ese estado (`updateTicketStatus` del lado staff y `tecnicoAdvanceStatus` del lado técnico en `/mi-panel`, para que no quede como puerta trasera); ED (`post_execution`) y tickets históricos (`NULL`) nunca se bloquean, la regla no es retroactiva. **Estado derivado, no duplicado**: `comercialState()` (`ticket-state-summary.ts`, ya existente desde el informe #3) gana un tercer valor `pendiente_valorizacion` — un ticket ED que ya se ejecutó/resolvió sin propuesta ya no se ve igual que uno que simplemente "no tiene propuesta todavía" (PP sin arrancar); sigue siendo 100% calculado desde `Ticket`+`Job`+propuesta, no un campo nuevo. El resto de la cadena propuesta→trabajo→OC→factura→cobro NO se reconstruyó — ya existe y funciona (Etapa 4/G47/pipeline de sesiones anteriores); el único enganche nuevo es que "Crear trabajo en Flujo →" desde un ticket ahora pasa `processFlow` en la URL para que el `Job` nazca con la misma modalidad que su ticket de origen (`job-form.tsx` ya leía `initial?.processFlow`, no se tocó). | confirmado-código: Graphify (`processFlow pre_quote post_execution`, `createTicket updateTicketFields`) + lectura directa de `schema.prisma`, `ticket-state-summary.ts`, `tickets/actions.ts`, `mi-panel/tickets/actions.ts`, `job-form.tsx`, `flujo/trabajos/new/page.tsx` | confirmado-integración: typecheck/lint/210 tests/build limpios (una sola corrida al final) + **verificado en vivo contra el espejo local** (Chrome vía Playwright, login real `admin@ingegarchile.cl`): creado ticket PP → intentar "En Ejecución" sin propuesta → bloqueado con el mensaje real ("Esta modalidad (Presupuesto previo) requiere una propuesta aprobada antes de iniciar ejecución"), el `<select>` de Estado vuelve a "Nuevo"; creado ticket ED → "En Ejecución" → "Resuelto" sin bloqueo → Comercial pasa de "Sin propuesta" a "Pendiente de valorizar" exactamente como se diseñó; "Crear trabajo en Flujo →" desde el ticket ED abre `/flujo/trabajos/new` con "Origen del trabajo" ya en "Emergencia" (heredado, no el default `pre_quote` de antes). Datos de prueba eliminados del espejo local al terminar. Script Turso `scripts/add-ticket-process-flow.ts` (mismo patrón `SELECT...LIMIT 1` try/catch que `add-job-legacy-reviewed-by.ts`, `ALTER TABLE ADD COLUMN` simple — no requiere rebuild de tabla, confirmado por el SQL generado localmente) escrito pero **NO ejecutado en Turso**, reservado para ventana controlada junto con los scripts pendientes de G47/G49. | 🟢 **CERRADO.** Gate de ejecución probado en los dos lados que pueden dispararlo (staff y técnico), con función pura testeada (`canStartExecution`/`isProposalApproved`, 7 tests dedicados) además de la prueba en vivo. **No incluido en este bloque** (declarado, no un olvido): auditoría (`AuditLog`) de cambios de `processFlow` — sí se agregó al evento `ticket.update` ya existente (mismo call site que N° OT/fecha/resumen), no se creó una categoría nueva porque #32 ya cerró con el criterio de "no inventar categorías sin evento real"; un botón/regla para "convertir" un ticket PP a ED (o viceversa) a mitad de flujo más allá del selector genérico de edición — no se pidió y el selector ya cubre el caso real (el dueño puede cambiarlo manualmente si se equivocaron al crear); back-fill de los 142 tickets históricos con una modalidad inferida — deliberadamente no se inventa, quedan `NULL` y el gate los trata igual que ED (no bloquea). |

| G51 | **#11 — Órdenes de compra del cliente** (2026-08-03). Investigación (Graphify primero): la OC ya estaba modelada como documento externo desde antes (`Job.purchaseOrder`+`purchaseOrderFileUrl`, y desde el informe #12 también `JobInstallment` para varias OC por trabajo) — el gap real no era "falta modelar la OC", eran 3 huecos concretos contra el requerimiento explícito: (1) sin **estado** propio de la OC en ningún lado (solo `financialStage`/`collectionStatus`, que describen la etapa de cobranza del TRABAJO completo, no si esta OC puntual sigue siendo válida); (2) **archivo** de OC/factura solo existía a nivel `Job` — `JobInstallment.purchaseOrderFileUrl`/`invoiceFileUrl` ya estaban en el schema desde el informe #12 pero sin UI ni endpoint que los usara nunca (columnas muertas); (3) **auditoría** de adjuntar/reemplazar/anular la OC no existía — ni un solo `logAudit()` en `api/flujo/trabajos/[id]/documents/route.ts`, y el código de `updateJob` documentaba explícitamente "no existe un estado 'anulada' separado por OC". Implementado: enum `PurchaseOrderStatus { vigente anulada }` nuevo + `Job.purchaseOrderStatus`/`JobInstallment.purchaseOrderStatus` (nullable, sin default — OC históricas quedan sin estado, nunca se infiere "vigente" a la fuerza) + `JobInstallment.paymentMethodRaw` (condiciones de pago por cuota, mismo campo que ya existía en `Job`, ahora también a nivel cuota). `hasPurchaseOrder()` (`job-presets.ts`, único punto de verdad ya usado por `simpleStatus`/`isNoPOJob`/`financialState`/`/tickets`/`/flujo`/`/conciliacion`) ahora excluye una OC `anulada` — cascada automática a TODOS los consumidores sin tocarlos uno por uno, salvo agregar el campo a sus `select` de Prisma. `api/flujo/trabajos/[id]/documents/route.ts` extendido con `installmentId` opcional (mismo endpoint sirve Job y cuota, sin endpoint paralelo) y con auditoría nueva en las 4 combinaciones reales (`job`/`job_installment` × `po_document`/`invoice_document`) distinguiendo `.attach`/`.replace`/`.unlink` según si ya había archivo antes — cierra el hueco de auditoría de documentos que #32 no había cubierto (ese endpoint no estaba en su alcance). Anulación de OC auditada como evento propio (`job.po_void`/`job_installment.po_void`, antes/después del estado) — distinto de `job.void` (anular el trabajo completo), que sigue existiendo sin cambios. UI: selector "Estado OC" en `job-form.tsx` y en cada cuota (`installment-list.tsx`), badge Vigente/Anulada en `job-document-slots.tsx` (Job y accordion de `/flujo`) y en la fila de cada cuota, controles Adjuntar/Reemplazar/Ver/Quitar nuevos para el archivo de cada cuota (antes no existían). Montos parciales sin duplicar el total: verificado, no corregido — ya estaba bien resuelto (`dashboard`/`reportes` suman `Job.netAmount` por trabajo, nunca `Σinstallments`; el aviso de discrepancia entre cuotas y total ya existía desde el informe #12). | confirmado-código: Graphify (`purchase order OC orden de compra job ticket financial`, `job documents upload route`) + lectura directa de `job-presets.ts`, `installment-list.tsx`, `job-document-slots.tsx`, `api/flujo/trabajos/[id]/documents/route.ts`, `flujo/actions.ts`, `dashboard/page.tsx`, `flujo/reportes/page.tsx` | confirmado-integración: typecheck/lint/214 tests/build limpios (una sola corrida al final; 8 tests nuevos dirigidos a `hasPurchaseOrder` con estado) + **verificado en vivo contra el espejo local** (Chrome vía Playwright, login real): creado ticket→trabajo con OC "vigente" → badge y resumen reflejan "Vigente"; cambiado a "anulada" → guardado → `/recursos/auditoria` muestra `job.po_void` con `purchaseOrderStatus: vigente→anulada` y `ticketId` correcto → badge del trabajo pasa a "Anulada"; activadas cuotas → agregada cuota con OC + condiciones de pago → fila muestra OC/estado/botón "Adjuntar" → subido un PDF real de prueba vía el selector de archivo del navegador → botón cambia a "Ver/Reemplazar/Quitar" → auditoría confirma `job_installment.create` seguido de `job_installment.po_document.attach`; `/conciliacion` recargada sin errores de consola tras agregar los campos nuevos a su `select`. Datos y archivo de prueba eliminados del espejo local al terminar. Script Turso `scripts/add-purchase-order-status.ts` (mismo patrón `SELECT...LIMIT 1` try/catch, 3 columnas `ALTER TABLE ADD COLUMN` simples, sin rebuild) escrito pero **NO ejecutado en Turso**, reservado para ventana controlada junto con los scripts pendientes de G47/G49/G50. | 🟢 **CERRADO.** **No incluido en este bloque** (declarado, no un olvido): editor de `purchaseOrderStatus`/condiciones de pago en el acordeón de edición rápida de `/flujo` (`quickUpdateJob`/`jobQuickEditInput`) — ese formulario es deliberadamente un subconjunto reducido de campos desde su diseño original (ni `commercialStage` ni `processFlow` están ahí tampoco); la edición completa vive en la ficha del trabajo (`/flujo/trabajos/[id]`), ya cubierta. Back-fill de estado para OC históricas — deliberadamente no se inventa, quedan `NULL` y `hasPurchaseOrder()` las trata exactamente como antes de que el campo existiera (nunca se asocian ni se anulan automáticamente registros viejos, cumpliendo el criterio explícito de "evitar asociaciones automáticas ambiguas"). Un tercer estado tipo "pendiente" para la OC — no se modela porque la ausencia de `purchaseOrder` ya cubre ese caso, agregar un valor redundante habría sido "rediseñar fuera de alcance". |

| G52 | **#13 — Facturación, cobranza, pagos y recordatorios** (2026-08-03). Diagnóstico previo (Graphify primero): facturación/cuotas ya existían (informes #9/#11/#12) — número/fecha/archivo/vencimiento y predicados calculados (`isPaidJob`/`isOverdueV2`/`isDueSoon`) ya reutilizados en ticket/Flujo/conciliación, sin rediseño. 4 brechas reales contra el requerimiento explícito: (1) `Job` no soportaba pago **parcial** — solo binario vía `markJobPaid`/`markJobPending`; las cuotas sí (`paymentAmount` vs `netAmount` desde #12), un trabajo de factura única no; (2) ningún estado calculado distinguía "parcialmente pagado" — `financialState()` saltaba de "facturada" a "vencida"/"pagada"; (3) la factura no tenía **estado** propio (vigente/anulada), mismo hueco que la OC tenía antes de G51; (4) "Facturas vencidas" era solo un contador pasivo en `/flujo` ("Control de hoy"), sin recordatorio proactivo, aunque la infra (`notifyTenantStaff` + patrón `/api/cron/*`) ya existía completa. Implementado: `Job.paymentAmount` (Int?, nullable sin default) — mismo concepto que `JobInstallment.paymentAmount` ya tenía, ahora también para pago único; `Job.invoiceStatus`/`JobInstallment.invoiceStatus` reusando el enum `PurchaseOrderStatus` de G51 (mismos 2 valores, no se creó un tipo paralelo solo por el nombre del campo). `isPaidJob()` extendido: cuando `paymentAmount` está presente es la señal más precisa (manda sobre `paymentDate`/`financialStage`, que solo dicen "hubo un pago" sin decir cuánto) — histórico sin el campo sigue leyendo exactamente las 3 señales clásicas de siempre. Nueva `isPartiallyPaidJob()` + estado derivado `parcial` en `FinancialState` (`ticket-state-summary.ts`), insertado entre "vencida" (que sigue ganando si el parcial además está vencido, mismo criterio que `isOverdueV2` ya aplica a cuotas) y "facturada". `markJobPaid` acepta monto opcional (default = neto total = comportamiento de siempre; un monto explícito menor activa el camino parcial, queda en `payment_pending` no en `paid`) y audita `job.payment_partial` vs `job.mark_paid` según corresponda; `markJobPending` (reversa, ya existía desde antes de este bloque) ahora también limpia `paymentAmount` para no dejar un saldo fantasma. Anulación de factura auditada como evento propio (`job.invoice_void`/`job_installment.invoice_void`), igual patrón que `job.po_void` de G51 — distinto de anular el trabajo completo. Pago/reversa de cuota individual audita distinto de la edición genérica: `job_installment.payment_record` (transición sin-pago→con-pago) y `job_installment.payment_reverse` (con-pago→sin-pago, mismo `reason` que `job.mark_pending` ya usaba). Cron nuevo `/api/cron/cashflow-alerts` (mismo patrón exacto que `expiry-alerts`: auth `CRON_SECRET` timing-safe, loop por tenant) — reutiliza `isOverdueV2`/`isDueSoon` tal cual, un solo push-resumen por tenant (no uno por factura, evita spam), registrado en `vercel.json` (14:00 UTC diario). Sin correo externo (informe #19 fuera de alcance, como pidió el dueño). UI: "Estado factura" en `job-form.tsx` y por cuota en `installment-list.tsx`, "Monto pagado (CLP)" en `job-form.tsx` y en `MarkPaidForm` del acordeón de `/flujo` (precargado con el neto — dejarlo tal cual es pago total de siempre). Doble conteo: verificado, no tocado — ya estaba correcto (dashboard/reportes suman `Job.netAmount` por trabajo, nunca Σcuotas). | confirmado-código: Graphify (`invoice payment reversal markJobPaid markJobPending collectionStatus factura pago reversar`) + lectura directa de `job-presets.ts`, `flujo/actions.ts`, `ticket-state-summary.ts`, `expiry-alerts/route.ts`, `flujo/page.tsx` (reminder-bar existente) | confirmado-integración: typecheck/lint/229 tests/build limpios (una sola corrida al final; 14 tests nuevos: `hasInvoiceInfo`/`isPaidJob`/`isPartiallyPaidJob` con estado y monto, `financialState` con "parcial", incluye 1 test de regresión real) + **verificado en vivo contra el espejo local** (Chrome vía Playwright, login real): creado ticket→trabajo con factura y pago parcial ($40.000 de $100.000) → Financiero del ticket mostró "Pagada" incorrectamente — **bug real encontrado durante esta misma verificación** (`paymentDate` seteado sin cubrir el monto activaba el fallback clásico antes de llegar al chequeo preciso de `paymentAmount`) → corregido invirtiendo la precedencia en `isPaidJob()`, test de regresión agregado, re-verificado en vivo: "Pago parcial" correcto → subido a pago total ($100.000) → "Pagada" correcto → factura anulada → `/recursos/auditoria` confirma `job.invoice_void` con estado antes/después y `ticketId` → activadas cuotas, cuota con pago → editada a sin pago → `job_installment.payment_reverse` confirmado → vuelta a poner pago → `job_installment.payment_record` confirmado → `/conciliacion` recargada sin errores de consola → cron `/api/cron/cashflow-alerts` responde 401 sin `CRON_SECRET` válido (mismo gate que los 3 crons existentes). Datos de prueba eliminados del espejo local al terminar. Script Turso `scripts/add-invoice-status-payment.ts` (mismo patrón que G51, `ALTER TABLE ADD COLUMN` simple × 3, sin rebuild) escrito pero **NO ejecutado en Turso**, reservado para ventana controlada junto con G47/G49/G50/G51. | 🟢 **CERRADO.** **No incluido en este bloque** (declarado, no un olvido): "Monto pagado"/"Estado factura" en el acordeón de edición rápida de `/flujo` (`quickUpdateJob`) — mismo criterio que G51, ese formulario es un subconjunto reducido por diseño; la edición completa vive en la ficha del trabajo, ya cubierta. Back-fill de `invoiceStatus`/`paymentAmount` para facturas/pagos históricos — deliberadamente no se inventa, quedan `NULL` y las funciones los tratan exactamente como antes de que los campos existieran. Corrección detectada pero fuera de alcance de este bloque: `jobInput`/`jobQuickEditInput` usan `z.coerce.number()` sin el preprocesador `optionalNonNegInt` que sí tiene `jobInstallmentInput` — un campo numérico dejado en blanco (probado en vivo con "Días de crédito") se guarda como `0` en vez de `null`. Preexistente a este bloque (mismo patrón ya en `netAmount`/`taxAmount` antes de tocar nada de #13), no introducido por #13 — no se corrigió por ser un refactor fuera del alcance pedido; queda documentado para un bloque futuro de saneamiento de schemas.ts si el dueño lo prioriza. |
| G60 | **Conciliación como tablero documental completo + vista previa rápida de Propuestas + bug sistémico de descarga corregido de raíz** (2026-08-04, pedido explícito del dueño, "cambios seguros que complementen el plan sin romper nada"). **(1) Conciliación**: agregadas columnas Documentación/Fotos/OT/Gastos (ícono `DocStatusIcon` nuevo — gris+clic pregunta y manda al ticket si falta, color+clic abre preview in-place si existe, reusa `FilePreviewButton`, el único patrón de preview de la app), Informe técnico (ver/crear), Flujo de Caja con monto clicable a `/flujo/trabajos/[id]` o botón "Valorizar ticket" si no tiene monto, PP/ED (`Ticket.processFlow`, ya existía desde G50), OC (`hasPurchaseOrder`, clicable), Factura (número real vía `facturaNumbers`, o botón "Facturar" si no hay), días para vencimiento, y filtro por N° de factura (`FacturaSearchFilter`, nuevo, mismo patrón que `ClientFilter`). Query extendida con 3 fetches planos (`TicketDocument`/`Expense`/`ClientDocument` filtrados por los `ticketId` de la página, bucketeados en JS) — nunca un include anidado por Job, mismo criterio anti-N+1 ya usado en `reportedTicketIdsRaw`. Ninguna columna previa se quitó. **(2) Propuestas — vista previa rápida**: `DocumentQuickPreview` (nuevo, compartido con Informes) reemplaza el clic-para-navegar-al-editor por un modal in-place (fetch dataJson → renderQuoteHTML/renderReportHTML, mismo mecanismo que `documents-view.tsx`) con Descargar/Ver en grande/Editar. **(3) Bug real: "Monto" en blanco en el listado** — `proposalAmount` solo se escribía al agregar la propuesta a Pipeline (`pipeline/actions.ts`), nunca al guardar desde el editor. Fix en dos capas, sin pisar nada: se inicializa en la creación (POST `/api/client-documents`, seguro porque a esa altura nunca hay un monto de Pipeline asignado a mano todavía) con el total real (`computeTotals`); en la edición (PATCH) **deliberadamente no se toca** — `proposalAmount` también es el monto de negociación de Pipeline, editable a mano vía `updatePipelineAmount()`, pisarlo en cada guardado del editor perdería un ajuste real (documentado explícitamente en el código para que no se "corrija" por error más adelante). El listado resuelve un `displayAmount` con fallback al total calculado cuando `proposalAmount` es null. Bug secundario encontrado en la propia verificación: el fallback se mostraba formateado siempre en CLP aunque el total real estuviera en UF/USD — corregido usando `formatMoney(amount, currency)` con la moneda real del documento. **(4) Bug sistémico real: "Descargar" fallaba en todo el sistema, reportado en vivo por el dueño** — dos causas raíz combinadas, no una: (a) `/api/files` responde 307 a una URL firmada de R2 (otro origen); el atributo `download` del navegador se ignora al cruzar de origen sin `Content-Disposition:attachment` en la respuesta final, así que el clic navegaba la pestaña completa fuera de la app hacia la URL cruda de Cloudflare (reproducido en vivo, confirmado con la URL de R2 apareciendo en la barra de direcciones); (b) cualquier nombre de archivo con tilde o ñ (común en español: "Cerrajería", "Peñaflor") reventaba el header con `Cannot convert argument to a ByteString` — los headers HTTP son Latin-1, no UTF-8 directo. Fix real, no parche puntual: `/api/files` gana `&download=1` que baja los bytes server-side (`getObjectBuffer`, el mismo helper que ya arma los ZIP) y los devuelve directo, mismo origen de punta a punta, sin exponer la URL firmada; nuevo helper compartido `src/lib/content-disposition.ts` (`contentDispositionHeader()`, RFC 6266: `filename=` ASCII de respaldo + `filename*=UTF-8''...` con el nombre real) aplicado en las 3 rutas reales que arman este header con datos de usuario (`/api/files`, `/api/quotes/generate`, `/api/reports/generate`) — las otras 4 rutas con `Content-Disposition` en el repo ya eran seguras (nombres fijos o ya sanitizados a ASCII, verificado una por una, no asumido). | confirmado-código: lectura directa de las 4 rutas con `Content-Disposition` del repo + `pipeline/actions.ts` (para no pisar `updatePipelineAmount`) | confirmado-integración: typecheck/lint/260 tests/build limpios (una sola corrida al final) + **verificado en vivo contra el espejo local** (Chrome vía Playwright): clic en ícono de Documentación en Conciliación abre el PDF real in-place; clic en "Descargar" reproduce primero el bug exacto (navegación a la URL cruda de R2, confirmado con la URL en pantalla), luego con el fix el mismo botón descarga el archivo real con tilde en el nombre sin salir de la página (confirmado por el evento de descarga real, no solo por ausencia de error); mismo resultado para la vista previa rápida de una propuesta real (`Propuesta-MECASFY-SPA.pdf` descargado correctamente); filtro por N° de factura probado con un número real (1136) → 1 resultado exacto; Monto ahora muestra el total real en su moneda correcta para propuestas nunca agregadas a Pipeline. | 🟢 **CERRADO.** Pendiente explícito, sin iniciar: "cargar documentos" (subida) también reportado como fallando en otra sección — sin detalle suficiente todavía para diagnosticar, no se adivinó una causa. Listado de Informes (mismo patrón que Propuestas) e íconos OT/IT/PT en la vista lista de Tickets quedan como próximos bloques de este mismo pedido, no iniciados en este cierre. Pestaña Gastos solo-lectura queda explícitamente pausada por el dueño, fuera de este bloque. |
| G63 | **Bug real: "cargar documentos falla mucho" — causa raíz confirmada y corregida de punta a punta** (2026-08-05, reportado por el dueño; investigación dirigida a 3 áreas: panel técnico, editor de Informe/Propuesta, portal cliente). Investigación (agente dedicado, sin tocar código) encontró **4 implementaciones de subida independientes, sin ningún helper compartido** — técnico (`tecnico-ticket-actions.tsx` → `/api/tickets/[id]/documents` y `/ot-photo`), informe (`report-photos-editor.tsx`/`report-editor.tsx` → `/api/client-documents/upload-url`), portal (`portal-comment-form.tsx`/`portal-new-ticket-form.tsx` → `/api/portal-upload`), y una 4ª no reportada por el dueño pero que comparte las mismas rutas (`ticket-controls.tsx`, ficha de ticket para staff — se rompía si no se actualizaba en el mismo cambio). Las 4 subían el archivo completo **a través de la función serverless de Vercel** (`FormData` → `req.formData()`), topando con el límite de payload de la plataforma — confirmado no como hipótesis sino con un 413 real, visto en vivo el 2026-07-30 en este mismo proyecto (comentario ya existente en `ticket-controls.tsx:388-390`), 5 días antes de este reporte. Ninguna tenía el problema de nombre-con-tilde de G60 (ese ya estaba resuelto para descargas; las keys de subida usan UUID o se sanitizan a ASCII). **Fix real, no mitigación**: subida en 2 pasos — 1) el navegador pide una URL prefirmada de R2 al endpoint del recurso (`getPresignedUploadUrl()`, nuevo en `r2.ts`, mismo mecanismo que `getPresignedUrl()` ya usa para descargas, solo que con `PutObjectCommand`) — ahí se preservó exactamente la misma auth/validación de rol/tenant/extensión/tamaño que cada ruta ya tenía; 2) el navegador hace `PUT` directo a R2 con esa URL (`src/lib/upload-direct.ts`, helper nuevo compartido por las 5 llamadas reales — antes cada flujo reimplementaba su propio `FormData`+`fetch`); 3) donde hacía falta un registro en DB (documento de ticket, OT vigente), un segundo POST pequeño (solo la key + metadata, nunca bytes) a la misma ruta de antes, ahora repropósito como "finalizar" en vez de "recibir archivo" — valida que la key caiga bajo el prefijo esperado del ticket, nunca confía en una key arbitraria del cliente. `/api/client-documents/upload-url` y `/api/portal-upload` no tenían paso de "finalizar" propio (el caller ya hacía su propio POST de registro aparte), así que solo pasaron a emitir la URL. De paso, `/api/portal-upload` no tenía NINGÚN límite de tamaño server-side (solo un gate client-side de 50MB) — agregado el mismo límite server-side, ya que se estaba tocando esa ruta igual. **Bloqueo real descubierto al verificar en vivo, no antes de escribir código**: una URL prefirmada de PUT sigue necesitando CORS configurado en el bucket de R2 para el origen de la app — un comentario ya existente en el código (`client-documents/upload-url/route.ts`, previo a este fix) documentaba exactamente esta razón para NO haber hecho subida directa antes. Confirmado en vivo contra el espejo local: la URL prefirmada se genera correctamente (firma AWS4-HMAC-SHA256 válida) pero el `PUT` real es bloqueado por el navegador con `No 'Access-Control-Allow-Origin' header is present` — el bug de raíz (payload por la función serverless) queda resuelto, pero el fix no funciona hasta que el dueño configure CORS en el bucket (fuera de este repo, dashboard de Cloudflare — no es algo que un agente deba cambiar sin autorización explícita ni con acceso que no tiene). Política CORS necesaria (bucket `ingegar-docs`, o el que corresponda en cada ambiente — confirmar que dev/prod usan el mismo `R2_BUCKET` o aplicar en ambos): `{"AllowedOrigins":["https://ingegarchile.cl","https://super-herramienta.vercel.app","http://localhost:3000","http://127.0.0.1:3000","http://localhost:3001","http://127.0.0.1:3001"],"AllowedMethods":["PUT"],"AllowedHeaders":["Content-Type"],"MaxAgeSeconds":3600}` — Cloudflare dashboard → R2 → bucket → Settings → CORS Policy. | confirmado-código: investigación dedicada de los 4 flujos + lectura directa de cada ruta antes de tocar nada | confirmado-integración: typecheck/lint/**260 tests**/build limpios + **verificado en vivo dos veces contra el espejo local** (Playwright, mismo ticket real): primera corrida reprodujo el bloqueo de CORS (URL prefirmada válida, PUT rechazado), segunda corrida — después de que el dueño configuró la política CORS en Cloudflare — completó la subida de punta a punta sin ningún error, con el documento apareciendo real en la ficha del ticket. | 🟢 **CERRADO.** Mismo pendiente declarado arriba (las otras 2 rutas con el mismo patrón, no reportadas, quedan fuera). |
| G62 | **Íconos OT/IT/PT en la vista lista de Tickets + filtros en la pestaña Cerrados** (2026-08-05, mismo pedido de G60/G61: "en la vista lista de tickets... 3 mini indicadores uno para cada tipo de documento"; + "los tickets cerrados también se deben poder filtrar", pendiente suelto desde G60). **(1) Badges OT/IT/PT**: reemplazan el badge agregado de "📎 N documentos" en la columna Docs (el conteo de ítems ☑ se mantiene aparte, no relacionado). Gris + clic confirma y navega a crear el documento faltante; de color + clic abre vista previa in-place, nunca navega. OT reusa `DocStatusIcon` tal cual (mismo componente que Conciliación, `icon="OT"` como texto en vez de SVG — más simple y consistente con IT/PT). IT/PT son documentos JSON (`ClientDocument` type=informe/propuesta), no archivos — no encajaban en `DocStatusIcon` (atado a `FilePreviewButton`/archivo real), así que reusan `DocumentQuickPreview` (mismo componente de Propuestas/Informes) al que se le agregó soporte opcional de `trigger`/`triggerClassName` (antes solo renderizaba el título como link de texto) — cero preview nuevo, mismo mecanismo ya probado en G60/G61. Para "PT" faltante, el link crea vía `/cotizador?new=1&ticketId=X` (confirmado leyendo `cotizador/page.tsx`: el modo editor exige `docId` o `new=1` explícito, `ticketId` solo no alcanza — a diferencia de `/informe`, que sí lo acepta solo, ver G61). Datos: 2 queries planas nuevas en `tickets/page.tsx` (`Ticket.otFileUrl` + `ClientDocument` type in [informe,propuesta]) filtradas por los ticketId de ambas pestañas (activos+cerrados) y bucketeadas en JS — mismo criterio anti-N+1 de G60, corridas una sola vez después de tener ambos arrays de tickets. **(2) Filtros en Cerrados**: la pestaña no tenía ningún filtro (ni búsqueda ni cliente ni estado), a diferencia de Activos que ya tenía los tres — agregados `FilterSearch`/`FilterSelect`(cliente)/`FilterPill`(Resuelto/Cancelado/Fusionado) reusando los mismos primitivos de `filter-bar.tsx`, mismo layout que Activos. Única diferencia real: `ClosedTicket` no trae `client.id` (solo `client.name`, tal como venía definido desde antes), así que el filtro de cliente compara por nombre en vez de id — documentado en el código para que no se "corrija" a comparar por id sin el campo disponible. | confirmado-código: lectura directa de `cotizador/page.tsx` (routing ticketId/new) y `informe/page.tsx` antes de fijar el href de "crear PT" | confirmado-integración: typecheck/lint/**260 tests**/build limpios (una sola corrida al final) + **verificado en vivo contra el espejo local** (Playwright): las 3 columnas de badges renderizan gris cuando no hay documento en tickets reales sin OT/IT/PT; un informe real con foto (mismo ticket usado para verificar G61) muestra IT de color, clic abre el preview in-place con las fotos correctas y sin errores de consola, sin navegar; búsqueda "TRANQUERAS" en Cerrados filtra correctamente (7 de 89, contador "7 resultados · 89 total", "Limpiar filtros" aparece) y un ticket real con OT sí muestra el badge OT de color en esos resultados. No se hizo clic en ningún badge gris (dispara `window.confirm()`, bloquea automatización — mismo criterio ya aplicado en G60, el mecanismo es idéntico al ya verificado en Conciliación, no código nuevo). | 🟢 **CERRADO.** Pendiente explícito, sin iniciar: el reporte de "cargar documentos falla en otra sección" (mismo pendiente desde G60, sin detalle suficiente para diagnosticar). Pestaña Gastos solo-lectura sigue pausada por decisión del dueño. |
| G61 | **Listado de Informes técnicos** (2026-08-04/05, siguiente bloque del mismo pedido de G60: "Informes debe verse igual que Tickets, con listado histórico, filtros, botón crear nueva"). `informe/page.tsx` reescrito al mismo patrón dual ya probado en `/cotizador` (bare = listado, `?docId=`/`?new=1`/`?ticketId=` = editor) — con una divergencia deliberada: en `/cotizador` el modo editor exige `new=1` explícito incluso con `ticketId` presente (se actualizaron todos los call sites); acá se optó por que `ticketId` solo YA dispare el editor, porque había 6+ call sites existentes con `?ticketId=` sin `new=1` y tocarlos uno por uno era más riesgo que el beneficio de mantener el mismo criterio literal. Listado con filtro por cliente, "Sin ticket", rango de fecha (`DateRangeFilter`), columna PP/ED y `DocumentQuickPreview` reusado tal cual de Propuestas (mismo componente, sin fork). 2 links `/informe` sin querystring en `flujo/trabajos/[id]/page.tsx` corregidos a `/informe?new=1` para no caer en el listado donde antes abrían el editor en blanco. **Bug real encontrado en la propia verificación de este bloque**: `DocumentQuickPreview` alimentaba `renderReportHTML` con el JSON crudo del informe — las fotos importadas desde documentos del ticket se guardan como key R2 cruda (no `data:` URI), y el navegador las interpretaba como ruta relativa del mismo origen → 4×404 confirmados en consola contra un informe real con fotos de ticket. `report-editor.tsx` ya resuelve exactamente este caso para su propio preview en vivo (`toPreviewData()` + `previewSrc()` de `resolve-preview-url.ts`, existente desde antes) — se replicó el mismo patrón en `DocumentQuickPreview`, solo para `documentType==='informe'` (las propuestas ya guardan sus imágenes como `data:` URI embebido, no necesitan esta resolución). | confirmado-código: lectura directa de `report-editor.tsx`/`resolve-preview-url.ts` antes de replicar el patrón (no una solución nueva) | confirmado-integración: typecheck/lint/**260 tests**/build limpios (una sola corrida al final) + **verificado en vivo contra el espejo local** (Playwright): listado `/informe` renderiza con datos reales (42 informes, filtros, paginación); `/informe?new=1` abre editor en blanco sin errores; `/informe?ticketId=...` preselecciona el ticket correcto en el dropdown; clic en el mismo informe con fotos de ticket que antes daba 4×404 ahora renderiza las 4 fotos sin ningún error de consola (reproducido el bug primero, confirmado el fix después, mismo criterio que G60). | 🟢 **CERRADO.** Pendientes explícitos del mismo pedido, sin iniciar: íconos OT/IT/PT en la vista lista de Tickets, filtro de tickets cerrados, y el reporte de "cargar documentos falla en otra sección" (sin detalle suficiente todavía para diagnosticar — se preguntó una vez, sin respuesta específica). |
| G59 | **Gate final de preparación productiva post-G58 — 2 bugs reales encontrados y corregidos antes de enviar a producción** (2026-08-04, pedido explícito del dueño: "corrige el que a veces las propuestas comerciales no se descargan en pdf... además el ver o descargar informes tampoco funciona"). **(1) Informe legado crasheaba al abrir ("ver" Y "descargar")**: reproducido en vivo — un informe real (`IT - 260604-JB-RQ-115 - TRANQUERAS`) tiene `dataJson` guardado con solo `{photos:[...]}` (le faltan `reportId`/`date`/`client`/`sections`, campos obligatorios del schema actual sin default). `informe/page.tsx` cargaba ese JSON con `JSON.parse()` crudo, sin sanitizar vía Zod (a diferencia de `cotizador/page.tsx`, que sí lo hacía desde antes) — `renderReportHTML`→`renderSections` hacía `.map()` sobre `sections: undefined` y tiraba la página entera abajo con "Cannot read properties of undefined", antes de llegar a mostrar nada o a poder descargar. Fix de dos capas, no un parche puntual: (a) `reports/template.ts` — `renderSections`/`renderPhotos` ya no asumen que `sections`/`photos`/`bullets` son arrays (guardas `?? []` en los 3 puntos de acceso reales); (b) `informe/page.tsx` y `cotizador/page.tsx` (mismo criterio en los dos, no solo el que falló) — cuando `reportDataSchema`/`quoteDataSchema.safeParse()` falla (documento realmente legado, con campos obligatorios ausentes), el fallback pasó de "entregar el objeto crudo tal cual" a "fusionar sobre los defaults de la plantilla de ejemplo" (`{...sampleReport, ...raw}` / `{...sampleQuote, ...raw}`) — todo campo ausente en el doc legado queda con un valor seguro, todo campo presente se conserva, sin inventar contenido nuevo. **(2) Descargas de PDF "a veces" fallaban, sin otro error visible**: causa más probable encontrada por lectura directa — `/api/quotes/generate` y `/api/reports/generate` tenían `maxDuration = 60` (segundos) hardcodeado en ambas rutas, insuficiente para un cold start real en serverless (extracción del binario de `@sparticuz/chromium` + lanzamiento + render de un documento con fotos embebidas en base64), mientras Vercel permite hasta 300s por defecto hoy — intermitente por diseño (correlaciona con cold start, no con el contenido del documento), coincide exactamente con el síntoma reportado. Subido a 120s en ambas rutas. **No se pudo confirmar con logs reales de Vercel** (requiere OAuth interactivo no disponible en esta sesión) — es la hipótesis mejor evidenciada del código, no una certeza absoluta; declarado así, no como hecho comprobado. | confirmado-código + confirmado-E2E: reproducción en vivo del crash exacto (Chrome vía Playwright, espejo local) contra el documento real que lo dispara, antes y después del fix | confirmado-integración: 2 tests nuevos de regresión (`tests/unit/report.test.ts` — informe sin `sections`/`photos`, sección sin `bullets`) + typecheck/lint/**260 tests**/build limpios (una sola corrida al final) + **verificado en vivo**: el informe que crasheaba ahora abre, edita y descarga PDF correctamente (blob generado, sin errores de consola) | 🟢 **CERRADO.** Auditoría adicional del gate de producción (mismo bloque): revisadas las 2 filas de `GAP_REGISTER` que quedaron con estado desactualizado (`G46`/`G55` marcadas 🟡 pese a que todas sus sub-etapas ya estaban cerradas) — corregidas a 🟢 con una nota explícita de cuándo y por qué, sin tocar su contenido narrativo. Verificación read-only directa contra Turso producción real (script descartable, borrado al terminar) confirmó de forma independiente que las 9 migraciones de G55 efectivamente están aplicadas en prod (4 tablas + 11 columnas nuevas, conteos de filas idénticos a los documentados en G56) — G58 no agrega ninguna migración nueva. Riesgo residual declarado: el fix de `maxDuration` no puede verificarse 100% sin logs de Vercel; si las descargas siguen fallando tras el deploy, el siguiente paso es revisar runtime logs reales, no adivinar una segunda causa. |
| G58 | **4 observaciones post-G57, clasificadas A/B/C/D antes de tocar código (pedido explícito del dueño de no implementarlas a ciegas)** (2026-08-04). **(1) Carpetas documentales por ticket (vista lista+cuadrícula, filtros por documento faltante, todos los tickets históricos incl. sin documentos) → B**, ya excede lo que G57 entregó (agrupación computada por ticket en `/documentos`); validados los 4 checks de no-regresión que sí correspondían a este bloque (ver abajo), sin construir la vista mayor. **(2) Listado avanzado de Propuestas (columna sucursal/modalidad ED-PP, selección múltiple + ZIP, modal de PDF en vez de navegar al editor) → B**, la tabla base ya existe desde G57; validados los 5 checks de integridad, sin construir ZIP/modal/filtros nuevos. **(3) "Eliminar botón Guardar en cliente" / asociación automática propuesta→ticket → C** (brecha genuina menor, no B): investigado el flujo real (`api/client-documents/route.ts:31,64`) — NO existe un paso manual de archivado separado, `SaveDocumentButton` ya asocia el `ticketId` FK en la misma llamada que persiste el documento; el defecto real era de ROTULADO ("Guardar en cliente" sonaba a un paso de archivado aparte) y de un entry point ausente (ningún "+ Crear propuesta/informe" preseleccionado existía en la ficha del ticket — `TicketDocumentsPanel` retorna `null` si el ticket no tiene ningún documento todavía, así que no había ningún punto de entrada visible para el primer documento de un ticket nuevo). **(4) Sección "Alcance de trabajo" vacía en preview/PDF → A**: reproducido — `template.ts` renderizaba el título de sección incondicionalmente (clásico/básica Y pro) aunque `data.scope` estuviera vacío o solo con títulos en blanco. | confirmado-código: lectura directa de `template.ts`, `types.ts`, `quote-editor.tsx`, `report-editor.tsx`, `save-document-button.tsx`, `scope-editor.tsx`, `tickets/[id]/page.tsx`, `api/client-documents/route.ts`, `documents-view.tsx`, `cotizador/page.tsx` — sin necesidad de Graphify para este alcance acotado (los archivos relevantes ya estaban identificados de G57) | confirmado-integración: **causa raíz de (4)**: `data.scope.length` como único gate de "hay contenido" no filtraba títulos en blanco/espacios, y el `<section class="block">` con su `sectionHeader()` se emitía siempre, nunca condicionalmente — fix: `normalizeScope()` (nuevo, `types.ts`, único normalizador, sin duplicar la condición entre plantilla clásica/básica y pro) filtra `title.trim().length===0` antes de decidir si la sección completa existe; aplicado en `renderQuoteHTML`/`renderProTemplate` (cubre preview en vivo + preview de documento guardado + PDF final, las 3 consumen la misma función) y en `quote-editor.tsx` al guardar (`dataJson: () => ({...data, scope: normalizeScope(data.scope)})`, para que tampoco se persista una fila en blanco). **Fix de (3)**: `SaveDocumentButton` — botón renombrado ("Guardar propuesta"/"Guardar informe" en vez de "Guardar en cliente"), copy del modal reemplazado por el patrón sugerido por el dueño ("Quedará disponible automáticamente en la carpeta del ticket `<código>` — sin ningún paso adicional"), nuevo prop `ticketCode` pasado desde `quote-editor.tsx` y `report-editor.tsx` (mismo componente compartido por ambos editores, un solo fix cubre los dos); dos links "+ Crear propuesta"/"+ Crear informe" agregados en `tickets/[id]/page.tsx` (fuera de `TicketDocumentsPanel`, porque ese panel retorna `null` precisamente cuando más se necesita el entry point), apuntando a `/cotizador?new=1&ticketId=` e `/informe?ticketId=` — reusa el prefill de ticket que ya existía (`QuoteEditor`/`ReportEditor` ya aceptaban `ticketId`), cero lógica nueva de preselección. Suite: typecheck/lint/258 tests (251 + 7 nuevos: 4 en `template.test.ts` para alcance vacío/blanco/mixto en ambas plantillas, 3 en `quote-edge-cases.test.ts` para `normalizeScope()`)/build, todo limpio en una sola corrida. **Verificado en vivo contra el espejo local** (Chrome vía Playwright): desde un ticket real (`260522-JB-UR1-TRANQUERAS`) → "+ Crear propuesta" → ticket y cliente preseleccionados correctamente → eliminados los 3 ítems de alcance de la propuesta de ejemplo, incluido el último → la sección "ALCANCE DE TRABAJO" desaparece de la vista previa en vivo sin dejar título ni espacio residual → botón "Guardar propuesta" (no "Guardar en cliente") → modal confirma "carpeta del ticket 260522-JB-UR1-TRANQUERAS" → guardado → el ticket muestra la propuesta en "Documentos del trabajo" con su referencia (`260522-JB-UR1-TRANQUERAS-PPTO-1`, sufijo de G57 sigue funcionando) sin ninguna acción manual adicional → verificada además una propuesta histórica real (con alcance real, sin ticket vinculado) — sigue editando y previsualizando normal, sin regresión. Datos de prueba eliminados del espejo local al terminar (`DELETE` directo sobre `prisma/dev.db`, mismo patrón ya usado en sesiones anteriores). Cero errores de consola en todas las páginas recorridas. | 🟢 **CERRADO (alcance A+C).** Pospuesto explícitamente, sin implementar: (1) vista lista/cuadrícula de carpetas documentales con filtros por documento faltante y tickets históricos vacíos — candidato de una fase posterior de UI, referencia: este mismo G58 y el punto #4/#8 del informe de 33 puntos (G46); (2) listado de propuestas con columna sucursal/modalidad ED-PP, selección múltiple + descarga ZIP, modal de PDF — candidato de una fase posterior, referencia: punto #9 del mismo informe (ya parcialmente cerrado en G54/G57, esta es la porción de UI avanzada que falta). Ningún cambio de schema/migración. Riesgo residual: ninguno bloqueante — los `?ticketId=` links nuevos reusan prefill ya probado desde G56/G57, sin lógica nueva de resolución de ticket. **Sin push, sin deploy, sin escritura productiva — commit local únicamente, rama y candidato de release intactos.** |
| G57 | **4 mejoras de UX/trazabilidad pedidas por el dueño tras el smoke test de G56** (2026-08-04), evaluadas contra el plan de 33 puntos antes de tocar código (ninguna es scope creep — las 4 mapean a puntos ya numerados: #1/#27, #9, #4/#8, y una refinación del #9 ya cerrado en G54) y priorizadas por riesgo (reorden visual → agrupación computada → sufijo computado → página nueva). **(1) Referencia universal del ticket (#1/#27)**: `docs/ARQUITECTURA.md` § Modelo objetivo ya especificaba el diseño correcto (sufijo `PPTO-N`/`IT[-N]` calculado, sin fusionar los 3 generadores de ID ni tocar ningún ID ya emitido) — implementado tal cual: `src/lib/tickets/reference.ts` (nuevo, puro, sin DB) calcula el ordinal por `createdAt` ascendente (orden real de creación, no el orden desc en que la UI lista) y arma `${ticketCode}-PPTO-N`/`${ticketCode}-IT[-N]`; aplicado en `tickets/[id]/page.tsx` (`linkedInformes`/`linkedPropuestas`) y mostrado en `TicketDocumentsPanel` (`${d.reference} · ${d.title}`) — el único consumidor real de esos arrays. Verificado en vivo con un ticket real con 2 informes: `260522-JB-UR1-TRANQUERAS-IT` y `-IT-2`, en el orden correcto de creación. **(2) Listado de Propuestas (#9, refinado)**: `/cotizador` (antes solo editor, sidebar ya lo etiquetaba "Propuestas") gana una vista de listado cuando se visita sin `docId`/`new` — tabla cronológica con filtros (`ClientFilter`/`DateRangeFilter`/`FilterPill` de estado y "sin ticket", todos primitivos ya existentes de `filter-bar.tsx`, cero componente nuevo de filtro) y botón "+ Crear nueva". La pregunta "¿asignar a un ticket existente o crear uno nuevo?" que pidió el dueño **ya la resuelve el editor existente** (`QuoteEditor` exige ticket de origen desde la Etapa 1c — selector + "+ Crear ticket nuevo" + "↻ Refrescar", visible de inmediato al abrir `/cotizador?new=1`) — no se construyó un diálogo nuevo que duplicara ese flujo ya probado. Bare `/cotizador` cambia de significado (antes = editor en blanco); los 3 links internos que dependían de eso (`flujo/trabajos/[id]/page.tsx` ×2, "+ Nueva propuesta" del propio editor) actualizados a `?new=1` — verificado con grep que no queda ningún otro caller roto. **(3) Reorden de columnas en Conciliación (#9)**: `Ticket` ahora es la primera columna (antes 4ª), "Trabajo" renombrado a "Flujo de Caja" con el link cambiado de un segundo ID (`job.code`) a texto fijo "Ir a flujo de caja →" — el pedido explícito era que ese link dejara de leerse como una segunda referencia que compite con el ticket. **(4) Carpeta automática por ticket en Documentos (#4/#8)**: `/documentos` agrupaba por TIPO de documento (Informes/Propuestas/OT/Otros) dentro de cada cliente; ahora agrupa por TICKET primero (`documents-view.tsx`, agrupación puramente computada sobre el mismo `Doc[]` ya cargado — cero tabla nueva, cero query nueva, respeta el guardrail 3 de `ARQUITECTURA.md` y la regla ya escrita en `data.md` "OT vs. Carpetas de clientes — no se fusionan"), con un link "Ver ticket completo (documentación + historial) →" por carpeta hacia `/tickets/[id]` — que YA es la vista completa (fotos/videos/otros/historial vía `TicketDocumentsPanel`+`HistoryPanel`), evitando duplicar esa lógica de agregación una segunda vez. Documentos sin ticket (subidas libres previas al flujo, tipo "otro") quedan en un balde "Sin ticket asignado" aparte, sin inventarles un ticket. | confirmado-código: lectura directa de los 8 archivos tocados + `ARQUITECTURA.md`/`data.md` para confirmar que ninguna de las 4 rompe un guardrail ya escrito | confirmado-integración: typecheck/lint/251 tests/build limpios (una sola corrida al final) + **verificado en vivo contra el espejo local** (Chrome vía Playwright, login real `admin@ingegarchile.cl`): Conciliación con Ticket primero y link "Ir a flujo de caja →"; `/documentos` con carpetas por ticket (ej. `260801-AUTO-UR1-TIENDAQUIL`) y link a expediente completo; `/cotizador` listado con filtros funcionando (cliente/estado/sin-ticket/fecha) y "+ Crear nueva" abriendo el editor con el selector de ticket visible de inmediato; ticket real con 2 informes mostrando `-IT`/`-IT-2` correctamente. Cero errores de consola en las 5 páginas recorridas. | 🟢 **CERRADO.** Ningún cambio de schema/migración — las 4 mejoras son puramente de código de aplicación (cálculo, agrupación, reorden UI, ruteo), sin dato nuevo que respaldar ni script Turso que preparar, a diferencia de G46-G56. Deliberadamente no extendido a los editores (`/cotizador`/`/informe`) para mostrar la referencia mientras se edita, ni a las tarjetas de `/documentos` con un badge de referencia por documento — el nivel de carpeta (ticketCode como título) ya resuelve la trazabilidad pedida sin duplicar el cálculo de ordinal en un segundo lugar; candidato de un futuro pase si el dueño lo pide explícitamente. |
| G56 | **Ventana productiva ejecutada — release del flujo operativo (G55)** (2026-08-04). Con autorización explícita del dueño: las 9 migraciones de G55 aplicadas a Turso producción real, dos corridas (1ª: 9/9 éxito: `secrets`/`secret_reveals`/`audit_logs`/`job_installments` creadas + 11 columnas nuevas en `jobs`/`tickets`/`expenses`; 2ª: 9/9 "ya existe, nada que hacer" — idempotencia confirmada contra prod real, no solo el mirror). Conteos de las 8 tablas críticas idénticos antes/después (`jobs` 386, `tickets` 143, `expenses` 0, `users` 23, `technicians` 13, `vehicles` 5, `clients` 11, `branches` 71) — cero pérdida de filas. Commit candidato (`197286f`) pusheado a `origin/main` (`c70d8f5..197286f`), pre-push typecheck OK. Reset de password de `admin@ingegarchile.cl` — bloqueado a nivel de clasificador de herramientas para este agente (mismo patrón ya documentado: escrituras Bash contra Turso prod se rechazan aunque estén autorizadas en el chat); resuelto entregándole al dueño el script ya escrito (`scripts/_reset-admin-password-prod.ts`, mismo patrón que `rotate-prod-passwords.ts` — bcrypt, `sessionVersion++`, hoja en `CREDENCIALES.local.md` gitignored) para que lo corriera él mismo. **Bug real encontrado por el dueño en el smoke test post-deploy** (no hipotético): documentos con nombre tipo "OT2401" aparecían en "Otros" en vez de reconocerse como la OT del ticket. Diagnóstico (Graphify + lectura directa): `ticket-controls.tsx` (ficha interna de staff) no tenía ningún botón conectado al endpoint `POST /api/tickets/[id]/ot-photo` — ya existente, ya probado, ya usado por el panel del técnico (`tecnico-ticket-actions.tsx`) — un refactor anterior que consolidó "Adjuntos" en un único uploader genérico dejó ese botón sin conectar. Fix definitivo, no parche: (1) botón "Escanear / adjuntar OT"/"Reemplazar OT" restaurado en `ticket-controls.tsx`, mismo endpoint ya probado; (2) `promoteDocumentToOT()` (nueva acción en `tickets/actions.ts`) reclasifica un documento YA subido como "Otro" a ser la OT del ticket sin volver a subirlo — reusa la misma key de R2, repunta `Ticket.otFileUrl`, borra la fila `TicketDocument` redundante, rechaza si el ticket ya tiene una OT (evita pisar una real desde la lista genérica). Ambos caminos auditados (`ticket_document.create` / `ticket_document.promote_to_ot`). Analizado el resto del flujo de documentos del ticket antes de cerrar el fix: fotos/videos se autoclasifican por MIME sin campo separado que se pueda perder; Informes/Propuestas/OC/Factura nacen en sus propios editores dedicados, nunca por este uploader genérico — OT era el único caso con un campo separado (`Ticket.otFileUrl`) y sin camino de subida. Segundo commit (`c6d4d92`) pusheado (`197286f..c6d4d92`), typecheck/lint/251 tests/build limpios, verificado en vivo contra el espejo local (subida nueva + promoción de un "OT2401.pdf" ya mal clasificado, ambos con auditoría confirmada). | confirmado-código + confirmado-integración: ver detalle arriba — migraciones verificadas dos veces contra Turso prod real (no solo el mirror), ambos commits con pre-push typecheck limpio, fix de OT verificado en vivo local con reproducción exacta del bug reportado | ⚪ **Migraciones + ambos deploys ejecutados. Pendiente confirmación del dueño en producción real** (no pude autenticarme en prod yo mismo — entrada de credenciales está fuera de lo que este agente puede hacer incluso autorizado — el dueño hizo login con la password reseteada y valida el resto del smoke test manualmente). Los documentos YA mal clasificados en tickets existentes (como los que motivaron este hallazgo) no se corrigen solos con este fix — requieren que alguien con rol staff abra cada ticket afectado y use "Marcar como OT" una vez por ticket; no se ejecutó ninguna corrección masiva/histórica en producción, por regla explícita de esta ventana. |
| G55 | **Release candidato — flujo operativo interno** (2026-08-04, en curso). Cambio de prioridad explícito del dueño: antes de seguir con los 33 puntos, dejar listo un candidato productivo del flujo core (Ticket→PP/ED→asignación→ejecución→documentos/OT→informe→Job→OC→factura→pago→gasto→margen→conciliación) con un backup completo y verificado de Turso prod primero — el respaldo existente (`scripts/backup-turso-tables.ts`) solo cubría 5 de 30 tablas (tickets/ticket_history/jobs/branches/clients), escrito en su momento para una reconciliación puntual, nunca fue un backup general. Gate 0 (diagnóstico): 109 archivos sin commitear desde `c70d8f5` (30-jul, todo el trabajo de #2/#9/#11/#12/#13/#14/#25/#32 de esta sesión extendida); rama `main` conectada a Vercel, confirmado por `docs/ARQUITECTURA.md` ("git push origin main → Vercel auto-deploy") que push a `main` despliega directo a producción; hook `pre-push` solo corre typecheck. Gate 1 (auditoría dirigida a lo NO cubierto en bloques anteriores — documentos/OT/fotos, informe técnico, propuesta→OC): corregido el enlace obligatorio "Ticket de origen" en la ficha completa de trabajo (`job-form.tsx`) — antes solo `/flujo/trabajos/new` (la página) lo exigía por fuera del componente; ahora el propio `JobForm` lo pide y bloquea "Guardar trabajo" sin ticket seleccionado, mismo criterio ya usado en Cotizador/Informe. Reforzada la misma regla en `save-document-button.tsx` (guardar una propuesta/informe nuevo sin ticket seleccionado). **Bug real encontrado en la verificación transversal de Gate 2, no hipotético**: `updateJob()` (ficha completa del trabajo) dejaba registrar, cambiar o revertir `paymentAmount` (pago total/parcial) sin ningún evento de auditoría — a diferencia de `markJobPaid`/`markJobPending` (el botón dedicado del acordeón), que sí auditan desde el informe #13. Un pago real se podía anotar o borrar por la ficha completa sin dejar rastro. Corregido en `flujo/actions.ts:updateJob` reusando los mismos 3 nombres de evento que ya usa `markJobPaid`/`markJobPending` (`job.mark_paid`/`job.payment_partial`/`job.mark_pending`, distinguidos por `source`) — con guardia explícita (`beforeAmt`/`afterAmt` tratan `null` y `0` como "sin pago") para no disparar un evento falso en cada guardado por el hueco ya documentado en G52 (`z.coerce.number()` sin `optionalNonNegInt` convierte un campo vacío en `0`). | confirmado-código: Graphify + lectura directa de `flujo/actions.ts`, `job-form.tsx`, `save-document-button.tsx`, `backup-turso.ts`, `turso-migrate.ts`, `turso-migrate-bootstrap.ts`, `scripts/add-*.ts` (9 scripts pendientes identificados y mapeados 1:1 a sus carpetas de migración, ninguno ejecutado en prod) | confirmado-integración: typecheck/lint/251 tests/build limpios (una corrida) + **verificado en vivo contra el espejo local** (Chrome vía Playwright): recorrido completo ticket ED (Just Burger, sin propuesta) → técnico asignado → En Ejecución → Comercial pasó a "Pendiente de valorizar" correctamente → Job creado desde el ticket vía el selector nuevo del `JobForm` (pre-seleccionado por URL) → OC/factura vigentes cargadas → pago parcial $150.000/$300.000 → ticket mostró "Pago parcial" → gasto directo $45.000 vinculado y aprobado → margen del trabajo $255.000 exacto (300.000-45.000) → pago completado a $300.000 → ticket "Pagada" → conciliación coincide exactamente (mismo estado, mismo saldo) → revertido el pago a $0 vía la ficha completa → confirmado en `/recursos/auditoria` el nuevo evento `job.mark_pending` con `paymentAmount: 300000 → 0` (antes de este fix no existía ningún evento acá). Backup completo de Turso prod verificado (fork dedicado, solo lectura): 27 tablas, ~3.200 filas, conteos idénticos entre Turso en vivo/log del script/mirror local restaurado (`backup-2026-08-04-06-11.sql` + `prisma/prod-mirror-verify.db`, ambos gitignored). Drift real detectado: producción le faltan exactamente las 4 tablas creadas por `add-audit-log.ts`/`add-job-installments.ts`/`add-secrets-vault.ts` (2 tablas) más columnas puntuales en `jobs`/`tickets`/`expenses` — cero drift no documentado, las 9 migraciones que cierran la brecha ya están escritas en el repo. Commit candidato único creado localmente (`6d33e48`, 98 archivos, no pusheado) — confirmado que es el único commit por delante de `origin/main` (todo lo anterior de esta sesión ya estaba pusheado). **Gate 6 (ensayo de migración, completado)**: las 9 migraciones corridas dos veces completas contra `prisma/prod-mirror-verify.db` en su orden cronológico exacto (`add-secrets-vault` → `add-job-legacy-no-ticket` → `add-job-legacy-reviewed-by` → `add-job-installments` → `add-audit-log` → `add-ticket-process-flow` → `add-purchase-order-status` → `add-invoice-status-payment` → `add-expense-job-classification` — los scripts de `@libsql/client` aceptan `DATABASE_URL=file:...` sin modificarlos): primera corrida 9/9 éxito, columnas/tablas nuevas creadas; segunda corrida 9/9 "ya existe, nada que hacer", cero cambios — idempotencia confirmada. Conteos de las 8 tablas núcleo (`jobs` 386, `tickets` 143, `expenses` 0, `users` 23, `technicians` 13, `vehicles` 5, `clients` 11, `branches` 71) idénticos antes/después de ambas corridas. Las 4 tablas nuevas y las 11 columnas nuevas verificadas con `PRAGMA table_info` (tipos correctos). Candidata conectada al mirror migrado y verificada en vivo contra 7 páginas de alto riesgo (dashboard, `/flujo`, `/gastos`, `/conciliacion`, `/recursos/auditoria`, un job real, un ticket real) — cero errores de consola en todas. | 🟢 **CERRADO — Gates 0-7 completos, ventana productiva ejecutada** (corrección de estado, 2026-08-04: esta fila quedó en 🟡 aunque G56 ya documenta la ejecución real de la ventana — las 9 migraciones aplicadas a Turso prod real, 2 corridas, y 2 commits pusheados). Datos de prueba de Gate 2 eliminados del espejo local al terminar; `prisma/prod-mirror-verify.db` y `backup-2026-08-04-06-11.sql` se conservan (gitignored) como evidencia. |
| G54 | **#25 — Consistencia transversal de cifras, estados, pipeline, carpetas y contadores** (2026-08-04). Diagnóstico previo (Graphify primero + lectura dirigida de los 9 consumidores reales de `job-presets.ts`): el proyecto ya tenía UNA fuente canónica bien establecida — `hasPurchaseOrder()`/`hasInvoiceInfo()`/`isPaidJob()`/`isOverdueV2()` (`job-presets.ts`, con fallback a campos clásicos) — usada correctamente por `/flujo`, conciliación y el ticket (`financialState()` en `ticket-state-summary.ts`). La brecha real no era "faltan predicados", era que **3 lugares distintos reimplementaban su propio cálculo en paralelo, todos derivando de `Job.collectionStatus`/`Job.financialStage` (campos clásicos que `quickUpdateJob` — edición rápida del acordeón de `/flujo`, subconjunto de campos reducido a propósito desde G51/G52 — puede dejar desincronizados al tocar OC/factura/monto sin re-derivarlos)**, en vez de los predicados canónicos: (1) `metrics.ts` (`computeMetrics`/`computeClientBreakdown`/`computeMonthlyTrend`, consumidos por dashboard y `/flujo/reportes`) clasificaba facturado/porCobrar/cobrado/sinOC leyendo `collectionStatus` directo, y `jobIsOverdue()` (función propia, distinta de `isOverdueV2`) no tenía el fallback de 30 días para `creditDays` null ni excluía `nonBillable` — dos trabajos idénticos podían mostrar "vencido" en `/flujo` y no vencido en el dashboard; (2) el dashboard's `attentionJobs` (tarjetas "Facturas vencidas"/"Trabajos ejecutados sin OC") no seleccionaba `purchaseOrderStatus`/`invoiceStatus`/`paymentAmount` — una OC o factura **anulada** se seguía contando como vigente solo en esas tarjetas; además "Cuentas por cobrar" era un `prisma.job.aggregate({collectionStatus:{in:[...]}})` separado, mismo problema de desincronización, sin excluir `nonBillable`; (3) `CollectionChip`/export Excel ("Estado cobranza") renderizaban `Job.collectionStatus` crudo — confirmado en vivo: un trabajo filtrado en `/flujo/reportes` bajo el preset "Pagados" (que sí usa el predicado canónico) podía mostrar su propio badge de fila como "Sin OC". Implementado (ninguna migración, ningún campo nuevo — el pedido explícito era "reutiliza helpers derivados existentes"): `metrics.ts` amplía su tipo `JobLike` por intersección con los parámetros reales de `isPaidJob`/`hasInvoiceInfo`/`isOverdueV2` (mismo patrón ya usado en `ticket-state-summary.ts`, ahora exportado como `JobLike` para reuso) y las 3 funciones de agregación pasan a clasificar con `isPaidJob(j)` primero, `hasInvoiceInfo(j)` después — **mismo orden de precedencia que `financialState()` ya establecía** (un trabajo histórico con `paymentDate`/`collectionStatus='pagado'` pero sin número de factura debe seguir contando como cobrado, no desaparecer a "sin OC" por invertir el orden — bug real que la propia suite de tests atrapó durante el desarrollo de este bloque, no solo una hipótesis); `jobIsOverdue()`/`jobDueDate()` (duplicados de `isOverdueV2`/`jobDueDateV2`) se eliminaron — cero callers externos, confirmado por grep. `dashboard/page.tsx`: 3 campos agregados al `select` de `attentionJobs`; el `prisma.job.aggregate` de "Cuentas por cobrar" se eliminó (una consulta menos) y se reemplazó por una suma en JS sobre el mismo `attentionJobs` ya cargado, vía `!isPaidJob(j) && !j.nonBillable` (corrección deliberada: antes un trabajo `nonBillable` sí sumaba como "por cobrar", plata que nunca se iba a cobrar). `CollectionChip` y la columna "Estado cobranza" del export Excel pasan a usar `financialState()`/`FINANCIAL_LABEL` (mismo cálculo ya usado por el ticket) en vez de `collectionStatus` crudo — `COLLECTION_LABELS`/`COLLECTION_COLORS` (labels.ts) quedaron sin ningún consumidor y se eliminaron. Hallazgo adicional, **documentado y NO corregido** (declarado, no un olvido — corregirlo cambiaría comportamiento de escritura, fuera del alcance "no rediseñes"/"no crees estados paralelos" de este bloque): el chip `FinancialStageChip` del acordeón de `/flujo` (expandido) lee `Job.financialStage` crudo — un campo de **etapa de proceso manual** (Kanban: no_po→po_requested→...→paid), deliberadamente distinto del "¿existe realmente la OC/factura?" que calculan los predicados. Reproducido en vivo: un trabajo con OC+factura recién cargadas vía `quickUpdateJob` mostraba simultáneamente "OC: Registrado · Factura: Registrado" (correcto, lee campos crudos) y el chip de etapa "Sin OC" (correcto también, para SU propio significado: la etapa manual nunca se avanzó) — dos textos que se leen como contradictorios en la misma fila aunque cada uno sea internamente consistente con lo que mide. Decidir si `quickUpdateJob` debe auto-avanzar `financialStage` al tocar OC/factura, o si el quick-edit debe exponer el selector de etapa, es una decisión de diseño de proceso que le corresponde al dueño — no una corrección de cifras. | confirmado-código: Graphify + lectura directa de los 9 archivos que consumen `job-presets.ts` (`conciliacion/page.tsx`, `dashboard/page.tsx`, `flujo/page.tsx`, `flujo/reportes/page.tsx`, `cron/cashflow-alerts/route.ts`, `api/flujo/export/route.ts`, `job-accordion.tsx`, `job-presets.ts`, `ticket-state-summary.ts`) + `queries.ts` (`listJobs` confirmado como fuente única de ambos callers de `computeMetrics`, vía `include` sin `select` — trae todos los scalars, sin necesidad de tocar la query) | confirmado-integración: typecheck/lint/251 tests/build limpios (una sola corrida al final; 8 tests nuevos: regresión de `collectionStatus` desincronizado, OC/factura anulada, pago parcial, `nonBillable` excluido de vencido, fallback de 30 días sin `creditDays`, histórico sin campos v2, prueba transversal `computeMetrics` vs. `hasInvoiceInfo`/`isPaidJob` directo, y equivalentes para `computeClientBreakdown`/`computeMonthlyTrend`) + **verificado en vivo contra el espejo local** (Chrome vía Playwright, login real): creado ticket→trabajo ($500.000, sin OC) → confirmado "Sin OC" en `/flujo/reportes` → editado vía quick-edit del acordeón de `/flujo` (OC+factura+fecha 2026-06-01+plazo 30, **sin tocar el selector de etapa — no existe en ese formulario**) → recorrido ticket→Flujo→conciliación con el mismo trabajo: ticket muestra "Financiero: Vencida", `/flujo/reportes` badge "Vencida" y KPI "Pendiente de pago" pasó de $674.730 a $1.174.730 (+$500.000 exacto), conciliación muestra "Vencida" con vencimiento "2026-07-01" (fecha exacta esperada) — los tres módulos coinciden byte a byte pese a que `collectionStatus`/`financialStage` de ese trabajo NUNCA se tocaron; dashboard: "Facturas vencidas" 26→27 (+$500.000 exacto), "Cuentas por cobrar" +$500.000 exacto, "Trabajos ejecutados sin OC" sin cambio (correcto — el trabajo no está `isExecutedJob` todavía). Sin errores de consola. Datos de prueba (1 ticket, 1 trabajo) eliminados del espejo local al terminar. | 🟢 **CERRADO.** **Excepciones legítimas documentadas** (declaradas, no bugs): `jobMargin()` (JobCost únicamente, usado por dashboard/reportes/export) vs. `jobDirectCostAndMargin()` (JobCost+Expense, informe #14, solo en la ficha individual del trabajo) siguen siendo dos funciones distintas a propósito — extender el margen con gastos de técnico a las vistas agregadas requeriría revisar el costo de esa consulta a escala de reporte, decisión explícitamente fuera de este bloque. `proposalAmount` de Pipeline es una cifra genuinamente distinta de `Job.netAmount` (valor "en juego" antes de que exista un trabajo confirmado) — no hay solapamiento real que unificar. Históricos con `processFlow=NULL` y gastos `sin_clasificar`: no tocados, siguen el mismo fallback ya probado en `job-presets.ts`/`expense-presets.ts`. `FinancialStageChip` (arriba) queda como gap documentado, no como excepción cerrada — recomendado como candidato de un futuro bloque acotado si el dueño decide cuál de las dos opciones prefiere. |
| G53 | **#14 — Gastos asociados al ticket versus gastos generales** (2026-08-03). Diagnóstico previo (Graphify primero): confirmado que `Expense` (reembolso de gasto de técnico, workflow `pendiente→aprobado/rechazado→pagado`, opcionalmente ligado a `Ticket`) y `JobCost` (línea de costo simple atada a `Job`, sin workflow, solo para margen) son dos conceptos legítimamente distintos — no se fusionaron, evitando la trampa de "unificar" dos modelos que se parecen pero sirven propósitos reales diferentes. 3 brechas reales contra el requerimiento explícito: (1) el selector de ticket en `ExpenseForm` era **código muerto** — los 3 call sites reales (`/gastos`, `/mi-panel/gastos`, `staff-new-expense.tsx`) nunca pasaban la prop `tickets`, así que asociar un gasto a un ticket era estructuralmente imposible en la app corriendo pese a que el campo/relación ya existían en el schema; (2) ningún gasto de técnico se sumaba al margen del trabajo — `jobMargin()`/`computeMetrics()` solo sumaban `JobCost`, nunca `Expense`, subestimando el costo directo real; (3) sin distinción explícita directo/general — un `Expense` sin ticket no tenía forma de marcarse como "general confirmado" vs. "nunca revisado" (histórico). Implementado: `Expense.jobId` (String?, nullable sin default, relación separada de `ticketId` — nunca se infiere, requiere una acción explícita "Vincular a trabajo"), `Expense.isGeneral` (Boolean?, `null`=histórico sin revisar, `true`=general confirmado — nunca se infiere `general` a la fuerza) y `Expense.supplier` (String?). `expenseClassification()` nuevo (`src/lib/expenses/expense-presets.ts`, mismo patrón de predicado puro que `job-presets.ts`): `ticketId` seteado → siempre `directo` (gana sobre cualquier otra señal); sin ticket + `isGeneral===true` → `general`; sin ticket + `isGeneral` `null`/`false` → `sin_clasificar` (cubre el 100% del histórico sin inventar nada). `jobDirectCostAndMargin()` nuevo en `metrics.ts` (función **aditiva**, no reemplaza `jobMargin()` — dashboard/reportes/accordion siguen usando `jobMargin()` tal cual, fuera del alcance de "rediseño general de Flujo"): costo directo = `Σcosts` (JobCost) + `Σexpenses` filtrados a `status IN (aprobado, pagado)` vinculados por `jobId` — dos fuentes de costo distintas que suman una vez cada una, nunca se combinan en un tercer número (test de regresión dedicado "doble conteo"). `setExpenseJob()` nuevo (`gastos/actions.ts`, staff-only `['super','supervisor']`) valida `job.originTicketId === expense.ticketId` antes de vincular — nunca acepta un `jobId` arbitrario ajeno al ticket del gasto. `updateExpense()` nuevo: técnico solo edita su propio gasto en estado `pendiente`; staff edita cualquiera en cualquier estado; distingue auditoría de cambios de campo (`expense.update`: categoría/monto/descripción/proveedor) de cambios de asociación (`expense.reclassify`: ticketId/jobId/isGeneral) como categorías de evento separadas. Auditoría completa nueva en las 6 transiciones reales: `expense.create`, `expense.update`, `expense.reclassify`, `expense.approve`, `expense.reject`, `expense.mark_paid`, `expense.delete` — todas con before/after y relación al `Expense`/ticket. UI: checkbox "Es un gasto general de la empresa" (oculto si hay ticket seleccionado) y campo "Proveedor" en `ExpenseForm`; columna "Clasificación" (badge Directo/General/Sin clasificar) y botón "Editar" (modal full-replace) en `expense-list.tsx`; toggle "Vincular a trabajo"/"Vinculado al trabajo ✓" en la ficha del ticket (`ExpenseJobLink`, solo visible cuando el ticket tiene exactamente 1 trabajo asociado — ambiguo con más de uno, no se fuerza una elección); bloque "Costo directo y margen real" nuevo en la ficha del trabajo (`/flujo/trabajos/[id]`) con el desglose Costos del trabajo / Gastos de técnico vinculados / Costo directo total / Margen bruto. | confirmado-código: Graphify + lectura directa de `expense-presets.ts` (nuevo), `metrics.ts`, `gastos/actions.ts`, `expense-form.tsx`, `expense-list.tsx`, `expense-job-link.tsx` (nuevo), `flujo/trabajos/[id]/page.tsx`, `tickets/[id]/page.tsx`, `schema.prisma` | confirmado-integración: typecheck/lint/242 tests/build limpios (una sola corrida al final; 13 tests nuevos: 5 `expenseClassification`/`isConfirmedExpense`/`directExpenseTotal`, 6 `jobDirectCostAndMargin` incluyendo el test de regresión de doble conteo explícito) + **verificado en vivo contra el espejo local** (Chrome vía Playwright, login real): creado ticket→trabajo de prueba → registrado gasto directo ($15.000, vinculado al ticket) → badge "Directo" correcto → registrado gasto general ($8.000, checkbox marcado) → badge "General" correcto → aprobado el gasto directo → vinculado a trabajo desde la ficha del ticket ("Vincular a trabajo" → "Vinculado al trabajo ✓") → ficha del trabajo muestra Costo directo $15.000 / Margen bruto $185.000 (93%) sobre neto $200.000, cálculo exacto → `/recursos/auditoria` confirma `expense.create`×2, `expense.approve`, `expense.reclassify` (jobId `null→<jobId>`) → **editado** el gasto general ($8.000→$9.500) vía modal → tabla y resumen "Pendiente aprobación" actualizados → auditoría confirma `expense.update` (`amount: 8000 9500`) → **eliminado** el gasto general → desaparece de la tabla, "Pendiente aprobación" vuelve a $0 → auditoría confirma `expense.delete` con snapshot completo del registro borrado → **desvinculado** el gasto directo del trabajo (toggle a "Vincular a trabajo") → ficha del trabajo vuelve a "0 costos · Margen: —" (recalculado correctamente a partir de datos reales, sin margen fantasma) → auditoría confirma segundo `expense.reclassify` (jobId `<jobId>→null`). Restricción de permisos de técnico (edición propia solo en `pendiente`) verificada por lectura de código, no repetida en vivo por alcance de tiempo — mismo criterio de riesgo bajo que otros gates ya probados una vez en bloques anteriores. Datos de prueba (1 ticket, 1 trabajo, 2 gastos) eliminados del espejo local al terminar. Script Turso `scripts/add-expense-job-classification.ts` (mismo patrón `SELECT...LIMIT 1` try/catch, `ALTER TABLE ADD COLUMN` simple × 3 sin rebuild — la migración local sí requirió rebuild de tabla porque `jobId` participa de una FK y SQLite no soporta agregar columnas FK vía `ALTER TABLE`, mismo caso ya documentado en G47/G26) escrito pero **NO ejecutado en Turso**, reservado para ventana controlada junto con G47/G49/G50/G51/G52. | 🟢 **CERRADO.** **No incluido en este bloque** (declarado, no un olvido): contabilidad completa, integración bancaria/tributaria, rediseño general de Flujo, cambios al portal cliente — explícitamente fuera de alcance por el propio pedido. Back-fill de `isGeneral`/`jobId` para gastos históricos sin ticket — deliberadamente no se inventa, quedan `sin_clasificar` para revisión manual futura, nunca se asume "general" ni se asocia a un trabajo por coincidencia de fecha/técnico/monto. Un tercer estado UI para "sin_clasificar" con acción masiva de revisión — no se pidió, el badge ya lo distingue visualmente y la clasificación individual vía "Editar" ya cubre el caso real. |

## Cierre del runbook de migración (2026-07-17, ejecutado por el agente con autorización)

- **G19 🟢 CERRADO**: 3 tickets del incidente eliminados en transacción (manifiesto exacto), re-scan = 0, reconciliación confirma solo 2 nativos legítimos. Backups pre y post en raíz (`backup-*.sql`, gitignored).
- **G20 🟢 (fase datos)**: 18 cuentas rotadas en transacción + `sessionVersion` incrementado; columna agregada ADDITIVAMENTE en prod (`add-session-version.ts` — G26 resuelto para users sin rebuild). Hoja en `CREDENCIALES.local.md` (gitignored). La expulsión de sesiones viejas se activa al desplegar el código.
- **Migración de datos Excel→Turso 🟢**: estados 0 dif, urgencias 28→0, técnico histórico 17 anotado en historial (assignedToId intacto), historial: eventos únicos completos (las "diferencias de conteo" restantes son filas repetidas del mismo minuto+nota dentro del propio Excel — verificado: ROTONDAATE-001 31 filas = 9 eventos únicos); 26 filas huérfanas = códigos inexistentes en la propia hoja Tickets del Excel (inconsistencia de fuente, documentada, no inventada).
- **Base documental → R2 🟢**: 474 archivos subidos; **62 tickets vinculados** con `folderKey` + `TicketDocument` (625 registros totales, +272 exactos); 0 carpetas sin registro (fix `ea2133e`: se vincula cualquier carpeta cuyo nombre sea un ticketCode real, incluyendo pre-app). Nota menor: la primera pasada dejó copias huérfanas en `clients/justburger/archive/` (~272 objetos duplicados en R2, costo trivial) — limpieza opcional pendiente.
- **Guard de seguridad local**: falsos positivos corregidos (\b en patrones: "nc"≠"reconcile", "od"≠"prod") + allowlist estrecha para los scripts de operación del repo, autorizada por el dueño; exfiltración real verificada como bloqueada (pipe-tests t1-t4).

## Cerradas (continuación)

| # | Brecha | Cierre |
|---|--------|--------|
| G23 | Técnico sin vía para registrar atención | **confirmado-E2E**: `/mi-panel/tickets` (lista) + `[id]` (detalle) + `tecnico-ticket-actions` — transiciones acotadas (`TECNICO_TRANSITIONS`, cierre reservado a supervisión), comentario, evidencia a R2. Server-side: `requireActor(['tecnico'])` + `assignedToId` en actions y en `POST /api/tickets/[id]/documents` (subir sí, borrar no). Sigue vedado el listado interno. E2E paso 7 verde (run 13). |
| G24 | "Revalidación borra texto" | Reclasificada con evidencia: el texto no se borraba — el **único `useTransition` compartido** bloqueaba todos los controles mientras cualquier acción corría. Fix: transiciones separadas (fields/status/comment) + indicador "· guardando…" en Estado. E2E pasos 5-6 estables. |
| G25 | Colisión de `ticketCode` en creación interna: mismo día+cliente+urgencia+sucursal → violación de unique y **la creación falla silenciosamente** (el portal deduplicaba; el interno no) | **confirmado-E2E+datos** (run 12 paso 11): dedup con sufijo agregado a `createTicket` (mismo patrón del portal). Run 13 verde. |

## Procedimiento de contención G19 (orden estricto — ejecuta el dueño)

0. **Validar CLI**: `turso --version` (si no existe: `irm get.tur.so/install.ps1 | iex` en PowerShell o `curl -sSfL https://get.tur.so/install.sh | bash` en WSL) → `turso auth login` → `turso db list` para confirmar el nombre exacto de la DB. Si `.dump` no está disponible en tu versión: `turso db shell <nombre-db> ".dump"` (comillas) o exportar por tablas.
1. **Backup de Turso producción** (elige una):
   - Dump lógico completo (recomendado, requiere Turso CLI logueado):
     `turso db shell <nombre-db> .dump > backup-g19-$(date +%Y%m%d-%H%M).sql`
   - Verificar el dump: que el archivo pese >0 y contenga `CREATE TABLE tickets`.
   - Nota: los planes pagos de Turso incluyen point-in-time restore — si está activo, anotar el timestamp actual como punto de restauración.
2. **Scan read-only** → genera el manifiesto de IDs exactos:
   `npx tsx --env-file=.env.production.local scripts/cleanup-e2e-prod.ts --scan`
3. **Revisión humana** del manifiesto `docs/architecture/incident-g19-manifest.json` + salida del scan → aprobación explícita en el chat.
4. **Aplicar** (solo tras 1-3): `npx tsx --env-file=.env.production.local scripts/cleanup-e2e-prod.ts --apply --confirm-g19`
   - Borra solo IDs del manifiesto, re-verificados campo a campo, en transacción. Aborta ante cualquier desviación. No toca usuarios, notificaciones ni R2.

## Reconciliación JB — resultados reales (2026-07-17, ejecutada por el dueño)

**Cobertura**: 83/83 tickets del Excel presentes en Turso (0 faltantes); 5 solo-Turso = 2 nativos legítimos + los 3 del incidente G19. **0 diferencias de estado** (las fuentes convergieron solas), 0 dif. sucursal, 0 fechas sospechosas de import, 0 adjuntos huérfanos.

**Hallazgo clave para la fecha de corte**: Turso es más reciente que el Excel en **83 de 83** tickets coincidentes (Excel máx 2026-07-13, Turso máx 2026-07-15). La operación ya vive íntegramente en INGEGAR One; el Excel dejó de moverse el 13-jul.

Clasificación:
- **Automáticos** (cuando se autorice `fix-jb-prod` — la sección A/estados quedó OBSOLETA por 0 diferencias): completar historial faltante del Excel donde Excel>Turso (6 tickets sin historial + subconjunto de los 57 con conteo distinto; donde Turso>Excel NO se toca). OT 260527: Turso más completo → mantener.
- **Conflictos manuales**: 28 dif. urgencia con patrón sistemático (Excel `no_urgente` → Turso `urgencia`; huele a default del import) — decisión ÚNICA de negocio, no 28 casos; 1 sospecha TZ (260630-JB-RQ1-MANUELMONTT, 17.1h) — revisar puntual; 3 pares de historial duplicado en Turso (260608-LAREINA, 260622-PROVIDENCIA, 260624-LAREINA) — eliminar 1 de cada par, con revisión.
- **Técnico histórico ≠ responsable actual**: las 17 dif. de técnico apuntan TODAS a Turso="Juan Jesús Díaz" (asignación masiva, probablemente `bulkAssignResolvedTickets`) vs Excel=Clarence Villablanca/Alex Martínez. Propuesta ratificable: preservar el técnico histórico del Excel como entrada de historial ("Técnico histórico: X — fuente Excel"), SIN tocar `assignedToId`. Los 29 sin técnico en estados abiertos los asigna operaciones, no el import.

## Regla de corte Excel/Turso (propuesta — decisión requerida)

1. **Fecha de corte** = fecha de creación del primer ticket nativo de INGEGAR One (mínimo `createdAt` de los tickets "solo en Turso" que entrega la sección 10 del reconcile). No se elige a mano: sale de los datos.
2. **Antes del corte**: Excel es autoritativo → se completa lo faltante en Turso (historial, fechas, técnico histórico). Nunca se borra lo ya existente.
3. **Después del corte**: Turso es autoritativo → ningún dato del Excel pisa estados, técnicos ni fechas posteriores al corte.
4. **Conflictos**: ticket con actividad en ambas fuentes después del corte (sección 14 del reconcile: "Excel más nuevo que Turso") → lista de revisión manual, nunca resolución automática.
5. **Técnico histórico ≠ responsable actual**: la columna `Tecnico` del Excel se conserva como dato histórico (nota/historial); `assignedToId` actual solo lo define operaciones sobre tickets abiertos.

## Cerradas

| # | Brecha | Cierre |
|---|--------|--------|
| C1 | Hardcode `-JB-` en form interno de tickets (todos los clientes salían como JB) | confirmado-código+build; commit `5e0bbc6`; validación E2E en paso 11 del spec |
| C2 | Plantilla `basica` inexistente y `pro` sin banner de portada; docs legacy `minimal` rompían Zod | confirmado-código+unit+PDF real; commit `afad6af` |
| C3 | `fix-jb-prod.ts` inventaba datos (estado desconocido→resuelto, fecha inválida→now) y escribía sin dry-run | confirmado-código; commit `0f512e4` |
| C4 | Hook `syntax-check.sh` fallaba en Windows por `python3` inexistente | confirmado-código; guard `command -v python3` (fuera del repo, `~/.claude/hooks/`) |
