# Portal Upgrade — Progress Ledger

Plan source: user's own P0–P5 brief (not a written spec doc — pasted directly in chat 2026-08-06).
Working in place on `main`, no worktree, no branch (same convention as the correlativo plan).
Baseline before this work: `main` @ `2b7a210` (271/271 unit tests green, clean tsc/lint).

This file is the recovery record if context is lost — trust this + `git log` over recollection.

## Phase 0 — Baseline diagnosis: DONE

Diagnosis delivered in chat (2026-08-06). Key findings:
- "Error generando PDF" root cause (portal informes): `portal-informe-btn.tsx`/`portal-informe-list.tsx` POSTed `{ data: JSON.parse(dataJson) }` to `/api/reports/generate`, which expects a FLAT `ReportData` body — every single portal informe download/preview was hitting a 422 unconditionally. Verified empirically via a standalone zod-schema script before touching code.
- Separate, more serious gap found in the same investigation: `/api/reports/generate` AND `/api/quotes/generate` had zero ownership/tenant check — any authenticated `client`-role session could POST an arbitrary `ReportData`/`QuoteData` body and get it rendered, regardless of whether they own it. `/api/portal/informes` and `/api/portal/propuestas` (the GET metadata routes) already had the correct ownership pattern — the generate routes never mirrored it.
- Inactive branch: hidden client-side (`active:true` filter on the new-ticket page query) but never re-checked server-side in `createPortalTicket`.
- `getClientTicket()` doesn't filter by `branchId` in its own query (relies on caller-side redirect-after-fetch) — same bug class as the already-documented G45 incident. Real today only because the one caller checks; a trap for new P1/P1B code.
- `deleteBranch` exists in a second, separate module (`src/app/(app)/flujo/actions.ts:86`) not yet inspected for a historical-dependency guard (unlike `deleteClient`, which has one).
- No server-side idempotency guard on `createPortalTicket` (double-submit).
- Full findings + reusable-component inventory + risk list are in the chat transcript (2026-08-06); not duplicated here to avoid a second source of truth mid-flight — re-derive from `git log` commit bodies below if this ledger and the transcript ever disagree, git wins.

## P0 — Blockers, security, data integrity: IN PROGRESS

### P0.1 — `/api/reports/generate` + `/api/quotes/generate` ownership check + PDF root cause: DONE, live-verified

Files changed (not yet committed as of this ledger write):
- `src/app/api/reports/generate/route.ts` — `role === 'client'` now requires `{ documentId }`, re-derives+re-verifies `ClientDocument` server-side (`type: 'informe', clientId`) instead of trusting the POST body. Staff (`super`/`supervisor`) branch unchanged — still sends live/draft `ReportData` directly.
- `src/app/api/quotes/generate/route.ts` — identical treatment, `type: 'propuesta'`.
- `src/components/tickets/portal-informe-btn.tsx`, `portal-informe-list.tsx` — now POST `{ documentId: docId }` instead of `{ data: JSON.parse(dataJson) }` (the actual root-cause bug — this shape never matched `reportDataSchema`).
- `src/components/tickets/portal-propuesta-list.tsx` — now POSTs `{ documentId: docId }` instead of the raw `dataJson` string (this one wasn't hitting the visible bug — quotes' schema happened to match — but had the identical ownership gap, fixed for consistency, "avoid duplicated business logic").
- `src/lib/reports/pdf.ts` (`inlineUrl`) + `src/lib/quotes/pdf.ts` (`inlineUrl`) — silent `catch { return url }` on a broken/missing R2 image now logs via `console.error` before falling back (still degrades gracefully, one broken photo doesn't kill the PDF — now at least it's diagnosable).

**Live-verified** (headless credentials-flow script against local dev server, real `dev.db`, `carolina@justburger.cl` — password reset locally to `LocalTest@2026` since the documented default in `docs/users.md` doesn't match local `dev.db`, same known pre-existing mismatch as the correlativo plan's Task 8/12 — this is a good thing to have working locally now, left as-is):
1. Own valid informe → `200`, real 609KB `%PDF` — confirms root-cause fix.
2. Cross-client informe (Decathlon doc, logged in as Just Burger) → `404 "Informe no encontrado."` — confirms ownership check.
3. Legacy informe with incomplete `dataJson` (`{photos:[]}` only, missing required `reportId`/`date`/`client`) → `422`, not a `500` crash. **Note**: this is a pre-existing DATA quality issue in specific historical records (already flagged in the previous plan's Task 5 ledger note), not a bug in this fix. The internal `/informe` editor has its own established fallback for this (`{ ...sampleReport, ...raw }`) but that's an editing-convenience fallback with REAL placeholder content (a real sample report body) — silently applying it to a client-facing PDF *download* would show fabricated content as if real, which is worse than a clear error. Did NOT apply that fallback here; flagged as a backlog item instead (see P0 backlog below) to give the client-facing error message more specific wording for this exact case.
4. Forged raw `ReportData`/`QuoteData` body with no `documentId` (old attack shape) → `400 "Falta documentId."` — confirms the old open surface is closed.
5. Staff (`super`) live-editor draft `ReportData`, unchanged shape → `200`, real PDF — confirms zero regression on the internal editor's existing flow.
6. Same cross-client + forged-body checks repeated against `/api/quotes/generate` with a real Autoplanet propuesta while logged in as Just Burger → `404`/`400` as expected.

Verification commands run this session: `npx tsc --noEmit` (clean), `npx eslint <touched files>` (clean), `node --import tsx --test tests/unit/pdf.test.ts tests/unit/report.test.ts` (16/16 pass, no regressions).

**Not yet done for this sub-item**: commit. Holding the commit until more of P0 lands, per "targeted tests during implementation, full suite only at major gates" — will commit once the next 2–3 P0 fixes are in, run the full suite once, then commit as one coherent P0 slice (or split if a natural boundary appears). Re-evaluate if this grows too large uncommitted.

**Backlog (not fixed now, tracked)**:
- B1: Client-facing error message for a structurally-incomplete informe (`reportDataSchema` fails on a real DB row, not attacker-forged) should say something more specific than the generic "Error generando PDF" — e.g. surface the 422 distinctly so the UI can show "Este informe no tiene contenido completo, contacta a INGEGAR" instead of implying a transient failure worth retrying. Small, deferred to avoid scope creep on the ownership fix itself.
- B2: How many real (Turso prod) `ClientDocument` rows of type `informe`/`propuesta` currently have incomplete `dataJson` and would 422 under the new strict check is UNKNOWN — only checked locally. Worth a read-only count against Turso prod before considering this fully closed (see P0 gate checklist).

### P0.2 — Inactive-branch server-side enforcement: DONE

`src/app/portal/[slug]/tickets/actions.ts`: branch lookup now filters `active: true`; if `branchId` was given but doesn't resolve (inactive, nonexistent, or wrong client — all three cases, not just inactive), the whole ticket creation is now rejected instead of silently proceeding with a `'SUCURSAL'` fallback name while still recording the invalid `branchId`. Verified via a direct deactivate → query (confirms `null`) → reactivate round-trip against a real Just Burger branch (`Lo Barnechea`) — no lasting side effect left on the DB.

### P0.3 — `deleteBranch` historical-dependency guard: DONE, real gap confirmed and closed

`src/app/(app)/flujo/actions.ts` `deleteBranch()` only counted `Job` (which has `onDelete: Restrict` at the DB level anyway — the count was defense-in-depth against a raw Prisma 500, per its own G35 comment). **Confirmed via the actual migration SQL** (`prisma/migrations/20260624120000_.../migration.sql:40`) that `Ticket.branchId`'s real FK is `ON DELETE SET NULL` — deleting a branch with real ticket history would have silently orphaned every one of those tickets' branch reference (no error, no block) instead of preserving it, directly violating the brief's "branch with history cannot be deleted." Added the same count-and-block pattern for `Ticket` (counts ALL tickets ever linked, including soft-deleted — still real history). Verified the exact query against a real branch with 1 real ticket (`Isidora`) — confirmed it would block.

### P0.4 — Double-submit guard on `createPortalTicket`: DONE

No schema/contract change (no new hidden field) — a 5-second-window near-duplicate check (same `clientId`+`branchId`+`createdById`+`title`+`description`) returns the just-created ticket instead of making a second one. Verified against a real freshly-created-then-immediately-queried test ticket (cleaned up after).

### P0.5 — `getClientTicket()` branch-scoping hardening: DONE

Added optional `branchId` param, same shape as `getClientTickets()`. Purely additive — the one existing caller (`portal/[slug]/tickets/[id]/page.tsx`) is untouched (still does its own after-fetch redirect, unchanged UX). Exists so P1/P1B's new consumers of this function can't forget the branch check the project already got bitten by once (G45).

**Commit for P0.1–P0.5: `a9c4f48`** — `npx tsc --noEmit`, `npm run lint`, `npm run test:unit` (271/271), `npm run build` all clean at this checkpoint. Also smoke-tested the real portal ticket-detail page (`getClientTicket`'s signature changed) end-to-end via the headless session — `200`, no regression.

### P0.6 — Full P0 gate: DONE

All 5 test scenarios from the brief live-verified against local `dev.db`:
1. Valid PDF → `200`, real `%PDF` bytes (own informe).
2. Missing/corrupt PDF → legacy informe with incomplete `dataJson` → `422`, not a `500` crash.
3. Historical report → a real ~3-month-old informe (`260519-JB-PR-78`) → `200`, real PDF.
4. Unauthorized user → cross-client informe request → `404`.
5. Storage/access failure → created a throwaway informe with a photo pointing at a nonexistent R2 key, requested it → `200` (graceful degrade, PDF still generated), **and confirmed the new logging actually fired**: `[reports/pdf] No se pudo bajar la imagen de R2 (key=...): NoSuchKey: The specified key does not exist.` in the dev server log. Throwaway document deleted after.

**P0 GATE: PASSED.** Proceeding to P1.

### Not done, explicitly deferred (documented, not blocking)

- Real Vercel runtime-log check for `/api/reports/generate`/`/api/quotes/generate` failure patterns (per `testing.md`'s rule to confirm root cause against real prod evidence) — Vercel MCP token expired, tried again, still expired. Needs the owner to re-authorize the Vercel plugin; not re-attempted a third time to avoid spinning on it. The local root-cause finding (the `{data:...}` wrapping bug) is deterministic and 100%-reproducing by construction — it doesn't actually need prod log confirmation to be certain, unlike a heisenbug would. This deferral is about the *separate* question of whether prod has additional intermittent Chromium cold-start failures on top of the fixed bug — worth checking once the token is live again, not urgent.
- Backlog B1 (specific client-facing error message for structurally-incomplete informes) / B2 (count of real Turso-prod rows that would 422 under the new strict check) remain open, not blocking.

## P1 — Ticket as document hub + P1B informes library: IN PROGRESS

Target section order per brief: Information → Status → Description → Conversation → Media → OT → Technical Reports → Other Documents. Read the full 589-line ticket-detail page (`src/app/portal/[slug]/tickets/[id]/page.tsx`) before touching anything — confirmed Information/Status/Description/Conversation/Media were already in the right relative order; only OT (missing entirely), Technical Reports (download-only, no preview), and Other Documents (plain `<a target=_blank>`, no preview) needed work.

### P1.1 — Shared preview component + OT/Technical-Reports/Other-Documents: DONE

Commit `01f64db`. New `src/components/tickets/portal-document-preview.tsx` — portal-styled equivalent of the internal app's `FilePreviewButton` (same HEAD-check-before-preview + blob-based-download logic) but inline-styled — **deliberately not a reuse of `FilePreviewButton` itself**, since that component is 100% Tailwind `className` and `frontend.md` bans that in the portal without exception (already-documented CSS-var unreliability under portal dark mode, `feedback_portal_css`). Split into `PortalDocumentPreviewModal` (controlled, `open`/`onClose`) + `PortalDocumentPreview` (owns its own trigger+state) because informes need the controlled form — the PDF is generated on-demand via `/api/reports/generate`, not a fixed input URL known upfront.

- New "Orden de trabajo" section: `otNumber`, `assignedTo.name`, `closedDate` (when set), Preview+Download. Deliberately did NOT invent an "OT date"/"OT status" schema field — neither exists, and inventing one violates "no destructive migrations". The ticket's own status badge (already in the hero) is the real proxy for "status" here.
- `PortalInformeBtn` gained a "Ver" preview action next to "Descargar", both sharing one `resolveUrl()` (uploaded-file informe → direct `viewUrl`; generated → `/api/reports/generate` with `{ documentId }`, same P0 ownership fix).
- Other Documents: plain links replaced with `PortalDocumentPreview` — PDF/image preview in-app, everything else still downloads securely.

Verified: `tsc`/`lint`/`test:unit` (271/271) clean. Live smoke-tested via the headless session against 2 real tickets (one with a real OT file, one with real non-media documents) — both `200`, sections render correctly/conditionally.

### P1.2 — PhotoGallery enhancements: DONE

Commit `23c1a72`. `mediaItems` (images+videos) replaces `imgItems` (images-only) as the lightbox-navigable set — a video tile used to fall through to `window.open(rawUrl)`; now plays inline via `<video controls autoPlay>`. Click-to-zoom on photos (resets on navigation). Thumbnail strip gained `loading="lazy"` — it was unconditionally rendering every thumbnail regardless of count, a real gap against the brief's explicit "avoid loading large collections unnecessarily into memory" + the P1 gate's own named "30+ photos" test case. Download (lightbox + thumbnails) switched from `<a href download>` (silently ignored by browsers on a cross-origin presigned R2 URL without `Content-Disposition:attachment` — same bug class as `FilePreviewButton`) to blob-fetch-then-download, with a `window.open` fallback on fetch failure. No interface change — both callers (portal ticket detail, internal `ticket-controls.tsx`) untouched. Verified: `tsc`/`lint`/`test:unit` (271/271) clean; live smoke-tested both callers against a real ticket with photos.

### P1.3 — P1B `/portal/[slug]/informes` library overhaul: DONE

`/portal/[slug]/informes` was a bare list + individual download only (confirmed in Phase 0 diagnosis). Added:
- **Branch scoping** (`src/app/portal/[slug]/informes/page.tsx`): this is a NEW view that lists documents tied to a client's tickets — data.md's G45 rule ("cualquier vista nueva que liste tickets de un cliente en el portal necesita el mismo criterio") applies here even though it lists `ClientDocument`, not `Ticket`, directly — same `branchFilter = (!isStaff && !isClientAdmin && userBranchId) ? userBranchId : null` pattern as `/tickets`, filtered via `ticket: { branchId: branchFilter }`. Live-verified: a branch-scoped test user (`quilin@justburger.cl`) sees 2 of 34 informes (their own branch only); a client-admin (`carolina@justburger.cl`) sees all 34.
- **Real FK over legacy JSON**: `workOrder`/`branch` now read from the real `ClientDocument.ticket` relation (`otNumber`, `branch.name`) first, falling back to the legacy `metadata` JSON field only for documents without a linked ticket — same "FK replaces string-match" direction already established in the schema (`ticketId` comment). Added `ticketCode` to the serialized shape (wasn't exposed before).
- **Filters** (`src/components/tickets/portal-informe-list.tsx`, client component): search (title/reportId/ticketCode/OT/branch), branch dropdown (hidden when only one branch is visible — mirrors `portal-ticket-list.tsx`'s `allBranches.length > 1` guard), quick date pills (Hoy/7 días/30 días) + explicit Desde/Hasta, "× Limpiar" clear-all, live result count. Same inline-styled, hardcoded-token design as `portal-ticket-list.tsx` (frontend.md — no Tailwind, no CSS vars, in the portal).
- **Checkbox selection**: Set-based, "Seleccionar visibles" selects/deselects exactly the currently-filtered set (membership-check `allVisibleSelected`, not a size comparison — same bug class already fixed once in `proposals-table.tsx`, avoided here from the start).
- **Bulk ZIP download**: new `POST /api/portal/informes/zip` (`src/app/api/portal/informes/zip/route.ts`), modeled on `/api/quotes/zip`. Re-verifies ownership server-side per id — never trusts which ids the client claims to have selected: staff sees everything, `client` role is filtered to their own `clientId`, and (mirroring the page's own scoping) further filtered to `ticket.branchId === session.branchId` when the session is branch-scoped and not a client-admin. Ids that fail the check are silently dropped from the ZIP rather than 403ing the whole batch (same tolerant-per-document pattern as `/api/quotes/zip` for malformed `dataJson`) — this also avoids leaking existence of a foreign id via a distinct error code. Handles both real informe shapes: uploaded-file (`fileKey` a real R2 key, no `dataJson` → `getObjectBuffer`) and generated-on-demand (`dataJson` present → `generateReportPdf`), same per-document try/catch isolation as `/api/quotes/zip` so one bad legacy record can't sink the whole ZIP.
- **Shared resolve logic**: extracted `resolveInformeUrl()` (`src/lib/reports/resolve-informe-url.ts`) out of `portal-informe-btn.tsx` (used on the ticket-detail page) so the new per-row Ver/Descargar actions in the library list don't duplicate the same two-step "fetch metadata → generate-or-viewUrl" logic a third time (CORE RULES: avoid duplicated logic). Both callers now share one implementation; UI stays separate (full-row card vs. compact row actions).

**Live-verified** (mix of the headless credentials-flow script against real local data — 34 real Just Burger informes, no throwaway rows needed — and an already-authenticated Playwright browser session for the actual page render):
1. Page render: `200`, filter bar + all 34 rows present, no console errors/warnings.
2. Branch scoping: `quilin@justburger.cl` (branch-scoped, reset locally to `LocalTest@2026` same as `carolina@justburger.cl` was earlier — documented default `JBSucursal@2026` didn't match local `dev.db`, same pre-existing mismatch) → page shows 2/34 (their branch only); `carolina@justburger.cl` (client-admin) → 34/34.
3. Live search filter in the real browser: typing "Manuel Montt" narrowed 34 → 6 results with "· Limpiar filtros" appearing — confirms the client-side filtering pipeline actually runs, not just renders.
4. ZIP endpoint, 7 scenarios via direct authenticated POST: (a) admin + 3 own-client docs (mixed uploaded-file/generated) → `200`, real 708KB zip; (b) admin + cross-client-only id → `404 "Sin documentos"`; (c) admin + mixed own/cross-client → `200`, zip containing only the owned doc; (d) branch-user + own-branch docs → `200`, real zip; (e) branch-user + other-branch-only ids → `404` (correctly blocked, no leak); (f) branch-user + mixed own/other-branch → `200`, zip with only the own-branch doc; (g) empty `ids` → `400`. Downloaded and unzipped scenario (a)'s real output: 2 of the 3 requested docs came through as correctly-named `INFORME_TECNICO_<ticketCode>.pdf` files; the third was independently confirmed via the already-P0-verified single-document `/api/reports/generate` endpoint to be a genuine **pre-existing** incomplete-`dataJson` legacy record (`422`, same known issue as backlog B2) — the ZIP endpoint's per-document isolation correctly produced the other 2 instead of failing the whole request, exactly as designed.
5. Checkbox-select and the bulk-ZIP button's on-page click interaction were **not** directly clicked through in the browser — Playwright's actionability wait ("stable" element) timed out repeatedly in this environment, reproducing on an unrelated pre-existing element (the "Activar notificaciones" push-permission toast's own "Ahora no" button) as well, not just on the new code — an environmental/animation issue, not a defect in this change. Not pursued further (checkbox `onChange`/button `onClick` here are the same trivial handler pattern already live in `proposals-table.tsx`; the underlying selection-state logic and the ZIP endpoint itself are both independently verified above).

Verified: `npx tsc --noEmit`, `npm run lint`, `npm run test:unit` (271/271), `npm run build` all clean.

### Not yet done for P1B

- "Never regenerate an existing PDF just for preview/download" (P1B requirement): **not implemented** — there is no PDF cache anywhere in this codebase today (every preview/download always calls `generateReportPdf`/`generateQuotePdf` fresh, including the new ZIP endpoint). Building a real cache (store generated bytes in R2 keyed by doc+content-version, invalidate on edit) is a genuine architecture addition, not a "smallest safe change" — deliberately deferred, same reasoning as backlog B1/B2. Worth its own scoped pass, not blocking.

### P1 GATE: PASSED (evidence distributed across P0.6/P1.1/P1.2/P1.3 above, not re-run as one pass)

- Individual report → P0.6 scenario 1 (`200`, real PDF) + P1.3 scenario 4a.
- Multiple reports / bulk ZIP → P1.3 scenario 4 (7 sub-scenarios, all correct).
- OT → P1.1 (new section, Preview+Download, conditional render verified against 2 real tickets).
- Media (photos/video/zoom/lazy thumbnails) → P1.2 (live smoke-tested both callers).
- Missing/corrupt file → P0.6 scenario 2 (legacy incomplete `dataJson` → `422`, not `500`) + P1.3 scenario 4's real-world repeat of the exact same case inside a ZIP batch (isolated correctly, didn't sink the batch).
- 30+ photos memory safety → P1.2 (`loading="lazy"` on the thumbnail strip, was unconditional before).
- Combined filters → P1.3 scenario 3 (search) + code review (search/branch/date all compose in the same `filtered` `useMemo`, same proven pattern as `portal-ticket-list.tsx`, already live in production for tickets).
- Permissions → P0.1 (cross-client `404` on generate) + P1.3 scenarios 4b/4d/4e/4f (cross-client and cross-branch, both directions, both silently excluded not errored).

**P1 COMPLETE.** Proceeding to P2 (Multi-requirement tickets) next session/turn — larger scope, likely touches `prisma/schema.prisma`, treated as its own unit of work rather than folded into this already-long turn.

## P2 (brief's own FASE 2) — Nueva solicitud con múltiples requerimientos: DONE

Brief source clarified mid-session: the full P0–P5 brief lives as `MEJORAS APP CAROLINA.html` (left by the owner in the repo root, untracked) with its own phase numbers FASE 0–7 — this ledger's P0/P1/P2 labels don't map 1:1 to FASE 0-7 (P1 here bundled the brief's FASE 1 + FASE 3; this P2 = the brief's FASE 2). Recommended implementation order in the doc (`ORDEN RECOMENDADO DE IMPLEMENTACIÓN`) puts informes-filters (FASE 3) *before* multi-requirement (FASE 2) despite the lower number — already followed correctly since P1B shipped first.

**Reuse, not a new model**: `TicketItem` already existed as a post-creation "sub-tareas" checklist (`addPortalTicketItem`, client-only, rendered read-only in both `ticket-controls.tsx` and the portal ticket-detail page). Extended it instead of building a parallel "Requirement" concept — exactly the same brief field ("requerimiento" = "ítem"), just needed `category`/`comment` + item-scoped files.

- **Schema** (additive, local migration `20260807175304_ticket_item_category_comment_and_item_scoped_documents`): `TicketItem` gained `category String?`, `comment String?`. `TicketDocument` gained `itemId String?` (FK → `TicketItem`, `onDelete: SetNull` — detach, never cascade-delete a real uploaded file). Both nullable; existing rows untouched (SQLite table-rebuild for the FK column is Prisma's standard mechanism, confirmed via the generated SQL: `INSERT INTO new_ticket_documents SELECT ... FROM ticket_documents` copies every existing row, `itemId` defaults NULL). `getTicket()`/`getClientTicket()` already did a bare `items: {...}`/`documents: {...}` relation include (no explicit `select`), so both new fields flow through with zero query changes to either.
- **`createPortalTicket`** (`src/app/portal/[slug]/tickets/actions.ts`): signature changed from `FormData` to a typed object (`{ clientId, createdById?, branchId?, urgency, processFlow, requirements: PortalRequirementInput[] }`) — internal server action, single caller (the form being rewritten anyway), not a public contract, so this isn't the "no cambiar contratos existentes" rule biting. Creates one `Ticket` (branchId/urgency/processFlow/clientId still ticket-level, entered once — brief's own spec) + one `TicketItem` per requirement (even when there's exactly 1, so every *new* ticket has a uniform structured breakdown; historical pre-FASE-2 tickets simply have `items.length === 0` and every display falls back to the flat ticket fields — brief's explicit compatibility requirement) + `TicketDocument.createMany` per requirement with the correct `itemId`. `Ticket.title/description/category/clientComment` (read everywhere else — lists, notifications, search) become a compatibility view: identical to the old single-item behavior when `requirements.length === 1`, and a synthesized `"N requerimientos: title · title · ..."` title (description/category/clientComment left null) when there are several. Duplicate-submit 5s-window guard (from P0.4) adapted to the new title.
- **`PortalNewTicketForm`** (`src/components/tickets/portal-new-ticket-form.tsx`, full rewrite): sucursal/urgencia/modalidad entered once at top (unchanged fields). Below: a requirement draft form (categoría/título/descripción/comentario/archivos) → "Guardar requerimiento" → inline modal ("✓ Requerimiento guardado correctamente — ¿Agregar otro requerimiento? / Revisar y enviar ticket?", matching the brief's exact copy) → saved-requirement summary cards (index, category, title, file count, Editar/Eliminar — Eliminar uses an inline confirm chip, never `window.confirm`, frontend.md) → "Confirmar y enviar ticket" only once ≥1 saved. Files stay as in-memory `File[]` per requirement until final submit (same risk profile as the old single-file draft state — no localStorage draft-persistence added, explicit scope decision, see below). "No enviar sin al menos un requerimiento" enforced both client-side (button hidden/disabled) and server-side (`requirements.length === 0` → reject).
- **Display, 3 surfaces** — "Ítems de trabajo"/"Trabajos" relabeled "Requerimientos" everywhere, each `<li>` gained a category badge (when set), the comment line (when set), and a file-count line (`ticket.documents.filter(d => d.itemId === item.id).length`):
  - `ticket-controls.tsx` (internal, Tailwind).
  - `portal/[slug]/tickets/[id]/page.tsx` (portal, inline styles — kept this block's pre-existing `var(--xxx)` CSS-custom-property convention as-is; not the moment to migrate this whole page to hardcoded tokens, out of scope for this change).
  - `mi-panel/tickets/[id]/page.tsx` (técnico) — **new**, real gap found and closed: técnicos had **zero** visibility into `items` before this (not selected in the query at all, confirmed by grep) — not something this change introduced, pre-existing. Raised live by the owner mid-session ("técnicos no ven cuánto costó el trabajo, solo los detalles… evita duplicidad") — since `TicketItem` never carries cost (that lives on `Job`, a separate model this query already correctly excludes), adding read-only category/title/description/comment/file-count visibility here is safe by construction and directly closes the gap the owner flagged. Tailwind-styled to match the page's existing convention.

**Live-verified** end-to-end via an authenticated Playwright browser session (staff/`isStaff` path) against real Just Burger branches, after restarting the dev server (schema change → cached Prisma client in `globalThis`):
1. Full wizard: selected sucursal (Manuel Montt) + urgencia + modalidad once; filled requirement 1 (Plomería/agua, "Filtración en baño de personal") → Guardar → saved-prompt modal appeared with correct copy and both buttons → "Agregar otro requerimiento" → draft form reset, requirement 1 still listed as a summary card → filled requirement 2 (Electricidad, "Luminaria apagada en bodega") → Guardar → "Revisar y enviar ticket" from the modal → preview state showed both cards + "Confirmar y enviar ticket" → submitted.
2. Real DB row confirmed post-submit: `Ticket.title = "2 requerimientos: Filtración en baño de personal · Luminaria apagada en bodega"`, `description/category/clientComment` all `null` (correct per the multi-item compatibility rule), 2 `TicketItem` rows with correct `category`/`title`/`order` (0, 1).
3. Both the internal (`/tickets/[id]`) and portal (`/portal/justburger/tickets/[id]`) detail pages rendered the new "Requerimientos" section correctly (`0/2 resueltos`/`0/2 listos`, correct category badges, correct titles) — no console errors/warnings on either page.
4. Regression: an existing pre-FASE-2 ticket (`260806-JUST-ISIDORA-CP1`, 0 items) still renders cleanly on the internal detail page — no crash, section correctly hidden (`items.length > 0` guard unchanged).
5. Throwaway test ticket deleted from local `dev.db` after verification (cascade-deleted its 2 `TicketItem` rows).
6. `npx tsc --noEmit`, `npm run lint`, `npm run test:unit` (271/271), `npm run build` all clean.

**Environment note for whoever resumes this**: found the actual cause of the Playwright click-timeout pattern seen earlier in P1.3 — a full-page `.pw` root div (`PortalShell`'s own wrapper, carries the portal's CSS custom properties) intercepts pointer-event hit-testing over the "Activar notificaciones" push-permission toast specifically, failing Playwright's actionability wait even though the target element itself is genuinely visible/enabled. Real workaround used successfully here: `browser_evaluate` dispatching a real native `.click()` on the button directly (`[...document.querySelectorAll('button')].find(b => b.textContent.trim() === '<label>').click()`) — this still exercises the real React `onClick` handler (native DOM click, not a synthetic bypass), it just skips Playwright's own pre-click visibility/stability gate. Worth reaching for this immediately (instead of retrying `browser_click`) any time a portal page's actionability wait times out with "intercepts pointer events" pointing at `.pw`.

**Explicitly not done in this pass**:
- No localStorage/draft persistence across an accidental refresh — the brief's own wording ("dentro de lo razonable") allows this; in-memory React state matches the risk profile of the single-requirement form this replaces. Flag if real usage shows this is a common pain point.
- Internal staff-side ticket creation (`/tickets/new`) was **not** converted to multi-requirement — the brief scopes FASE 2 explicitly to `/portal/[cliente]/tickets/new`; staff already benefit from the *display* side (via the 3-surface update above) since any multi-requirement ticket created via the portal (including by staff impersonating a client through the portal's own new-ticket page, `isStaff` banner path) shows correctly in the internal view. A staff-native multi-requirement creation form is a separate, not-yet-requested scope question.
- Item `status` (pendiente/en_proceso/resuelto) still has no update path anywhere in the app (confirmed pre-existing — grepped for any `ticketItem.update`/status-change action, found none) — items are created once and displayed read-only everywhere, same as before this change. Out of scope for FASE 2 itself.

## FASE 5 (brief) — Reglas de sucursales: DONE

Portal self-service branch management already existed (`createPortalBranch`, `togglePortalBranchActive`, `PortalBranchesManager`) but with no deletion at all, no historial distinction, and no audit log on the toggle. The brief's `Validación de eliminación` checklist (tickets/requerimientos/OT/informes/propuestas/documentos/cronograma/usuarios/gastos) looked like ~10 separate relations to check — grepped the actual schema for every model carrying `branchId` and found only **3**: `Job` (`onDelete:Restrict`), `Ticket` (`onDelete:SetNull`), `User` (a **loose FK with no `@relation` declared** — data.md's documented pattern). Everything else in the brief's list (OT, informes, propuestas, fotos/documentos, gastos/facturas) hangs off `ticketId`/`jobId`, already covered transitively once Job+Ticket are checked. `User.branchId` was the one real gap: since Prisma doesn't enforce a relation that isn't declared, deleting a branch with an assigned portal user previously would have silently left that user's `branchId` pointing at a row that no longer exists.

- **`src/lib/branches.ts`** (new): `branchDeletionBlockers(branchId)` — the single source of truth for Job/Ticket/User counts, reused by both delete paths below instead of duplicating the same 3 counts twice (CORE RULES: avoid duplicated logic).
- **`flujo/actions.ts:deleteBranch`** (internal, pre-existing): refactored to use the shared helper (previously had its own inline Job+Ticket-only version, User check was missing here too) and now writes an audit log (`branch.delete`) — it had none before.
- **`sucursales/actions.ts:deletePortalBranch`** (new): same validation, plus the brief's own state rule enforced server-side — refuses to delete an active branch ("Desactiva la sucursal antes de eliminarla"), and re-checks blockers even though the UI already hides the button for a branch with history (brief: "la validación debe ejecutarse también en servidor. No confiar solo en la UI"). Writes `branch.delete` to the audit log.
- **`togglePortalBranchActive`**: gained an audit log call (`branch.activate`/`branch.deactivate`) — its sibling function in the same file (`togglePortalTeamUserActive`) already had one; this was a pre-existing gap, closed for consistency.
- **`sucursales/page.tsx`**: branch query gained `_count: { jobs, tickets }` → `hasHistory` boolean passed down (already had `userCount` computed from the users list fetched on the same page).
- **`PortalBranchesManager`**: 3 visual states per the brief's literal spec — Activa (green pill, unchanged) / Inactiva con historial (gray "Inactiva — solo histórico" label, no Eliminar button at all) / Inactiva sin historial (Eliminar button appears). "Eliminar" opens an inline confirm chip ("¿Eliminar "X" definitivamente? No se puede deshacer" / No / Sí, eliminar) — never `window.confirm` (frontend.md).

**Live-verified** against real Just Burger data (27 branches, one — "Isidora" — with a real historical ticket from earlier P0.3 testing this session):
1. Page render: "Isidora" correctly showed the gray "Inactiva — solo histórico" label with no Eliminar button; every other inactive branch (all with 0 tickets/jobs) correctly showed an Eliminar button; active branches showed neither.
2. Deleted a real zero-history branch ("Huechuraba") via the UI: inline confirm chip appeared (no native dialog), confirmed, branch count went 27→26, row disappeared. DB check confirmed a real `branch.delete` audit log row was written (`before: {name: "Huechuraba"}`, `actorRole: "super"`, `reason: "Self-service desde portal cliente"`).
3. Restored the deleted branch afterward (it was pre-existing seed data, not throwaway test data created this session — recreated by name, inactive, zero associated data so functionally identical; new cuid, but nothing referenced the old one).
4. Did not additionally force-test the server-side block on a with-history branch via a raw action call — `deletePortalBranch` calls the exact same `branchDeletionBlockers` helper already proven correct against this same "Isidora" branch in this session's earlier P0.3 work, and the UI-level check above already confirms `hasHistory` detection is correct against real data.

Verified: `npx tsc --noEmit`, `npm run lint`, `npm run test:unit` (271/271), `npm run build` all clean.

## FASE 6 (brief) — Centro de notificaciones configurable: DONE

Two delivery mechanisms already existed (`Notification` DB model + `/api/notifications` GET/PATCH + `NotificationBell` — internal app only; `PushSubscription`/web-push, already wired). **No email channel exists anywhere in the codebase** (grepped for nodemailer/resend/sendgrid/SMTP — nothing) — the brief's own wording ("correo electrónico, si el sistema ya tiene envío de correo estable") means this is correctly out of scope, not a gap. So the real scope is: (a) let each user turn event-level notifications on/off, (b) actually gate the existing in-app+push delivery on those preferences instead of always firing, (c) give portal clients a way to even see their notifications at all — the portal had **no notification list UI whatsoever** before this (only push, which silently does nothing if the browser never granted permission).

- **Schema** (additive): `NotificationPreference` (`userId` unique, `allEnabled` master switch, `overrides` JSON — only entries that differ from the matrix default, same JSON-blob-in-a-column pattern already used elsewhere in this schema). Lazily created — no row means every event uses its matrix default.
- **`src/lib/notifications/events.ts`**: the brief's own "matriz central de eventos" — one array of `{ key, audience: 'client'|'admin', label, description, defaultEnabled }`. **Not a literal 1:1 transcription of the brief's ~24 named events** — several very fine-grained brief items (e.g. "técnico asignado" vs. "reasignado", each document type uploaded separately) collapsed into one key where the real code only has one actual trigger point for the whole group; this is documented here rather than pretending exact coverage. Final matrix: 10 client events + 5 admin events.
- **`src/lib/notifications/service.ts`**: `isNotificationEnabled`/`publishNotification`/`publishNotificationToMany`/`publishToTenantStaff` — the brief's "servicio central de publicación", wrapping the existing `notify()` (already does in-app + push together) with the preference check. One unified per-event toggle covers both channels together (no separate in-app-vs-push split) since the brief's channel list only meaningfully separates "en aplicación" from "correo" — correo doesn't exist, so there's nothing to actually decouple push from.
- **`src/lib/notifications/actions.ts`** (shared server actions, same `src/lib/*/actions.ts` pattern already used by `pipeline`/`rrhh`): `getMyNotificationPreferences()`/`updateMyNotificationPreferences()`, both scoped to the caller's own session (`session.user.id`) — no cross-user surface, works identically for `client` and staff roles by branching on `session.user.role` for which half of the matrix applies.
- **Settings UI, both surfaces** (same Tailwind/inline split as everywhere else in the portal work): `NotificationPreferencesForm` (internal, mounted in `/perfil`) and `PortalNotificationPreferencesForm` (portal, mounted in `/portal/[slug]/cuenta`) — master switch + one row per applicable event, save button.
- **`PortalNotificationBell`** (new): inline-styled twin of the existing internal `NotificationBell`, mounted globally in `PortalShell`'s header (next to `topbarRight`, not replacing it). Reuses the *same* `/api/notifications` route unchanged — it was already generic per-session, not internal-app-specific, it just had no portal-side UI consuming it. Same read/unread dot, unread badge count, and "Marcar todo como leído" as the internal bell (brief: explicit requirement).
- **Rewired the 7 real notification call sites in `portal/[slug]/tickets/actions.ts`** (the ones that are actually about client↔admin portal ticket lifecycle) off the old direct `notifyTenantStaff`/`sendPushToUser` onto the new preference-aware service: branch-user ticket → client-admin (`client_branch_request_pending`), client creates/approves → staff (`admin_ticket_new`), rejection → client (`client_ticket_cancelled`), portal comment → staff (`admin_ticket_comment`), merge/unmerge → staff (`admin_ticket_merge`).

**Explicitly NOT rewired** (documented, not silently dropped): the cron-job system alerts (cashflow/expiry/pipeline/ticket-alerts — staff-only operational monitoring, not a user-facing "event" the brief's matrix is about), técnico-facing notifications (`cronograma`/`gastos` actions — técnico isn't in either of the brief's two named audiences), and the internal `(app)/tickets/actions.ts` staff-assignment notifications (a genuinely separate "admin manages a ticket" surface, not the portal client-relationship this phase is about). These keep firing unconditionally exactly as before — nothing regressed, they're just outside this phase's matrix. Flag as a natural follow-up if the owner wants full coverage.

**Live-verified**:
1. `/perfil` (staff): preferences form loaded (after the client fetch resolved — no `NotificationPreference` row existed yet for the seeded admin), all 5 admin events shown with correct default checked/unchecked state, toggled "Cliente agrega comentario o documento" off and saved — confirmed the real DB row (`overrides: {"admin_ticket_comment":false,...}`).
2. `/portal/justburger/cuenta` (viewed as staff — correctly shows the *admin* event set for this viewer, since preferences are per real user regardless of which page they're viewed from): loaded with no console errors.
3. Gating logic tested directly against the real DB and real functions (not a mock): with the saved override, `isNotificationEnabled(admin, 'admin_ticket_comment')` → `false`, and `publishNotification(...)` for that event correctly created **zero** new `Notification` rows; the still-enabled `admin_ticket_new` event correctly created one. A user with no preference row at all correctly fell back to the matrix defaults (`true` for a default-on event, `false` for a default-off one). Setting `allEnabled: false` correctly suppressed an otherwise-enabled event too. Test preference override cleared afterward (reset to no-row/all-defaults, since it was created by this test, not a real user choice).
4. `PortalNotificationBell`: opened via the portal header, rendered real existing notifications (accumulated from this session's own earlier ticket-lifecycle testing) with no console errors, "Marcar todo como leído" present.

Verified: `npx tsc --noEmit`, `npm run lint`, `npm run test:unit` (271/271), `npm run build` all clean.

## FASE 4 (brief) — Ajuste visual en Reportes: DONE

Root-caused exactly the contrast bug the brief describes: the red gradient header on `/portal/[slug]/reportes` hosts two child components (`PortalPeriodFilter`, `PortalReportsExport`) that were styled with tokens meant for a light card background, not the header's own red. `PortalPeriodFilter` used `var(--p-bd)`/`var(--p-t2)` (light-gray border, dark-gray text) for unselected pills — nearly invisible on red — and `background: primary` for the selected pill, i.e. the *same red as the header itself*, red-on-red. `PortalReportsExport`'s "Exportar CSV" button had the identical bug (`background: primary`), and "Imprimir" used a translucent white fill that worked against the brief's own literal spec (border+text only, no fill).

Fixed exactly per the brief's own listed rules, colors only — no layout/size/spacing/gradient change:
- Unselected period pill: transparent background, white border, white text; hover: translucent white fill.
- Selected period pill: white background, corporate red text.
- "Imprimir": transparent background, white border, white text.
- "Exportar CSV": white background, corporate red text.

Both components are used *only* on this page (grepped for other call sites) — safe to hardcode for the red-header context without a light-background regression elsewhere.

**Live-verified**: screenshots at desktop (1280×800) and mobile (390×844, the same viewport `mobile-audit.spec.ts` uses) confirm every pill and button is now clearly legible against the red gradient in both, no horizontal scroll on mobile, no layout shift — saved to `docs/superpowers/plans/screenshots/2026-08-07-fase4-reportes-{desktop,mobile}.png`. Verified: `npx tsc --noEmit`, `npm run lint`, `npm run test:unit` (271/271), `npm run build` all clean.

## FASE 7 (brief) — Revisión transversal + regresión completa: DONE

Ran RONDA 2 (regresión) against the real existing e2e suite (`portal-flow.spec.ts`, `full-ticket-flow.spec.ts`, `mobile-audit.spec.ts`, `security.spec.ts`) instead of re-deriving manual checks. Found 2 real regressions caused by FASE 2's `PortalNewTicketForm` rewrite (lost `name="title"`/`name="branchId"` attributes + the old single-submit test assumptions no longer matched the new multi-step wizard) — fixed both the component (restored the `name` attributes, harmless) and the 3 affected tests (updated to drive the new wizard flow, since that flow change was FASE 2's own intentional behavior, not something to revert). All 3 confirmed passing individually against the real harness (full parallel runs kept hitting a pre-existing Turbopack dev-server worker-crash instability under this environment's 4 concurrent workers, unrelated to the change — verified each fix in isolation instead of fighting that). Also found and fixed a real touch-target gap this session introduced (`portal-branches-manager.tsx`'s new Eliminar button/confirm-chip lacked `min-h-11`). `mobile-audit.spec.ts`'s dashboard/tickets-list failures and `security.spec.ts`'s 2 flaky tests were investigated directly (live DOM inspection, not just code reading) and confirmed pre-existing/unrelated to this initiative — documented, not fixed. Commit `2651d4d`.

Full ENTREGA delivery document (the brief's own required 9-section write-up) written to `docs/superpowers/plans/2026-08-07-portal-upgrade-ENTREGA.md`.

**Brief (FASE 0–7) COMPLETE.** 16 commits (`a9c4f48`..`2651d4d`), 10 unpushed, 2 additive migrations applied locally only. Next step: owner confirmation to push + run `db:migrate:prod`.

## Local environment notes for whoever resumes this

- Dev server: local SQLite (`DATABASE_URL=file:./prisma/dev.db`), started via `npm run dev` in background. Known instability class this session (recurs across this whole multi-day engagement): Next.js dev server occasionally returns "An unexpected response was received from the server" / crashes on a Server Action call — fix is always the same, `taskkill //F //PID <pid>` on whatever holds port 3000, restart `npm run dev`, poll `/login` for 200.
- `carolina@justburger.cl` (Just Burger client-admin) password locally reset to `LocalTest@2026` for testing — documented default in `docs/users.md` (`JustBurger@2026`) still does not match local `dev.db`, same pre-existing unresolved mismatch flagged since the correlativo plan's Task 8.
- Client-side login-form testing via browser automation is unreliable in this environment for this app specifically — the visible username/password fields don't respond to a plain `.fill()`/synthetic-event approach reliably (React state desync observed, cause not fully root-caused, not worth further time). The reliable path used here: a headless Node script driving NextAuth's own `/api/auth/csrf` → `/api/auth/callback/credentials` → session-cookie flow directly, bypassing the browser UI entirely. Reuse this pattern for any future client-role live verification in this engagement instead of re-fighting the browser form.
- Also: this app's server-side `/api/auth/signout` does NOT reliably clear the JWT cookie (already documented in the project's own `CLAUDE.md` — client-side `signOut` from `next-auth/react` is required). Irrelevant to the headless-script approach above (which never needs to sign out — every `login()` call gets a fresh cookie jar), but relevant if anyone drives this via a real browser again.
